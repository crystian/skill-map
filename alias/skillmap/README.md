# skillmap (alias — common typo)

This is **not** a real package. It is a placeholder published to reserve the un-hyphenated spelling against typo-squatting.

The project is spelled **`skill-map`** (with a hyphen). The official command-line tool is **`@skill-map/cli`**.

## Install the real one

```bash
npm install --global @skill-map/cli
```

## Documentation

- Project home: https://skill-map.dev
- Repository: https://github.com/crystian/skill-map
