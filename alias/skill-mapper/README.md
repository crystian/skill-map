# skill-mapper (alias — lookalike)

This is **not** a real package. It is a placeholder reserved against name-squatting on the lookalike `skill-mapper`.

The project is **`skill-map`** (no `-er`). The official command-line tool is **`@skill-map/cli`**.

## Install the real one

```bash
npm install --global @skill-map/cli
```

## Documentation

- Project home: https://skill-map.dev
- Repository: https://github.com/crystian/skill-map
