{
  "name": "@skill-map/web",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "description": "skill-map.dev public site source. Static HTML + i18n; deployed via Caddy/Docker.",
  "scripts": {
    "dev": "bun scripts/dev-server.js",
    "demo:build": "bun run --filter ui build && bun scripts/build-demo-dataset.js && cp -R ../ui/dist/ui/browser/. demo/ && bun scripts/patch-demo-mode.js",
    "demo:serve": "bun scripts/serve-demo.js",
    "build": "bun run demo:build && bun scripts/build-site.js",
    "docker:build": "docker build -t skill-map-test ..",
    "docker:run": "docker run --rm -p 8080:8080 skill-map-test",
    "docker": "bun run docker:build && bun run docker:run",
    "validate": "bun run build"
  },
  "engines": {
    "bun": ">=1.2"
  }
}
