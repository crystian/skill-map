# skill-map

> Design document and execution plan for `skill-map`. Architecture, decisions, phases, deferred items, and open questions. Target: distributable product (not personal tool). Versioning policy, plugin security, i18n, onboarding docs, and compatibility matrix all apply.

## Project overview

The project description, problem statement, target audience, and philosophy live in the README. Both language variants carry the same content:

- **English (default)**: [README.md](./README.md).
- **Español**: [README.es.md](./README.es.md).

Each README also ships a short essentials-only glossary with a pointer back to the full [§Glossary](#glossary) below. This document (`ROADMAP.md`) is the design narrative, architecture decisions, execution plan, decision log, and deferred work, and sits beneath the READMEs; it is maintained in English only.

---

## Table of contents

1. [Project overview](#project-overview), language variants, document scope.
2. [Glossary](#glossary), canonical vocabulary (domain, extensions, modes, architecture, jobs, states, plugins, refresh, safety, enrichment, scope, CLI/UI).
3. [Visual roadmap](#visual-roadmap), ASCII timeline of every Step.
4. [Spec as a standard](#spec-as-a-standard), repo layout, properties, distribution.
5. [Architecture: Hexagonal (Ports & Adapters)](#architecture-hexagonal-ports--adapters), layering, ports, adapters, package layout.
6. [Persistence](#persistence), scopes, zones (`scan_*` / `state_*` / `config_*`), naming, data-access, migrations, DB management.
7. [Job system](#job-system), model, lifecycle, TTL, duplicate prevention, runners, nonce, preamble, atomicity, concurrency, events, `sm jobs` surface.
8. [Plugin system](#plugin-system), six kinds, drop-in install, loading, qualified ids, Provider catalog, Extractor channels, scan cache, Hook trigger set, plugin storage, default pack.
9. [Summarizer pattern](#summarizer-pattern), schemas, storage, probabilistic refresh, report base.
10. [Frontmatter standard](#frontmatter-standard), base (universal), per-kind (Provider-owned), validation tiers, DB denormalization.
11. [Enrichment](#enrichment), two enrichment models, hash verification, stale tracking, refresh commands.
12. [Reference counts](#reference-counts), link-count denormalization.
13. [Trigger normalization](#trigger-normalization), six-step pipeline, examples.
14. [Configuration](#configuration), file hierarchy, key reference.
15. [CLI surface](#cli-surface), every verb, the `sm` binary contract, exit codes.
16. [Skills catalog](#skills-catalog), built-in and bundled skills.
17. [UI (Step 14 full)](#ui-step-14-full), Flavor B + the Hono BFF.
18. [Testing strategy](#testing-strategy), pyramid, coverage targets.
19. [Stack conventions](#stack-conventions), runtime, language, deps, formatting.
20. [Execution plan](#execution-plan), Step-by-step status with the completeness marker.
21. [Decision log](#decision-log), pointer to the canonical numbered table in `context/roadmap-history.md`.
22. [Deferred beyond v1.0](#deferred-beyond-v10), Steps and features intentionally pushed past the first stable release.
23. [Discarded (explicitly rejected)](#discarded-explicitly-rejected), pointer to the rationale of dropped proposals in `context/roadmap-history.md`.

> **Step vs Phase glossary**: a **Step** (e.g. `Step 9`, `Step 14.4.b`) is an atomic feature milestone, one PR or a tightly-related sequence. A **Phase** (e.g. `Phase A`, `Phase B`, `Phase C`) is a multi-Step release target. Phase A = `v0.5.0` → `v0.6.0` (deterministic kernel + CLI + Web UI), Phase B = the stabilization run that ships **Beta** (`v0.52.0`: multi-runtime lens, Signal IR, annotations, extensible inspector, fused workspace, hardening), Phase C = real-time exploration (watch execution as it happens; shipped, the milestone before the LLM layer), Phase D = the `v0.8.0` target (job subsystem + findings; shipped in practice as `0.89.0` stable, 2026-07-23), Phase E = `v1.0.0` (surface + distribution). Execution prose mixes both: `Step 14 ships v0.6.0 inside Phase A` is correct shorthand.

---

## Glossary

> Canonical vocabulary of the project. The rest of the roadmap uses these terms without ambiguity.

### Domain and graph

| Concept | Description |
|---|---|
| **Node** | Markdown file representing a unit (skill, agent, command, markdown, for the Claude built-in catalog; other Providers may declare their own kinds). Identified by path relative to the scope root. |
| **Link** | Directed relation between two nodes (replaces the term "edge"). Carries `kind` (invokes / references / mentions / points), confidence (high / medium / low), and sources (which Extractors produced it). |
| **Issue** | Problem emitted by a deterministic analyzer when evaluating the graph. Has severity (warn / error). |
| **Finding** | Judgment recorded by a probabilistic finder Analyzer through the queue (plus the kernel-derived safety rows synthesized from any probabilistic report's `safety` block), persisted in `state_findings` and read via `sm findings`. Advisory by construction: findings never drive exit codes. The deterministic sibling is the Issue. (Decision #137.) |
| **Node kind** | Category of a node, declared by the classifying Provider. Open by design, built-in Claude Provider catalog: `skill` / `agent` / `command` / `markdown`; built-in OpenAI Codex Provider: `agent` (TOML envelopes) + `skill` (the open `.agents/skills/` standard, inherited from `agent-skills` by manifest composition); neutral `agent-skills` Provider: `skill`; built-in Antigravity Provider: `workflow` (`.agent/workflows/*.md`, its own) + `skill` (the open `.agents/skills/` standard, inherited from `agent-skills` by manifest composition). The retired Gemini Provider declared `agent` / `skill` / `markdown` and was removed when Google sunset Gemini CLI 2026-05; the historical entries above survive in `context/roadmap-history.md`. External Providers MAY declare their own. Field `node.kind` in the spec. Distinct from **link kind** (value of `link.kind`) and **extension kind** (plugin category, see next table). All three are polysemic specializations of the generic term "kind"; the prefix is used when context is not obvious. |

### Extensions (6 extension kinds)

"Extension kind" is the category of a plugin piece, distinct from **node kind** in the previous table. The ecosystem exposes six, and they form the stable kernel contract. Three kinds are dual-mode (deterministic / probabilistic, see §Execution modes below); three kinds are deterministic-only because they sit on the deterministic scan path.

| Concept | Description |
|---|---|
| **Provider** | Extension kind. Recognizes a platform (today's built-in catalog: `claude`, `codex` for Codex, `antigravity` (its own `.agent/workflows/*.md` workflows plus the open-standard skills it adopted), `agent-skills` for the vendor-neutral `.agents/skills/<n>/SKILL.md` layout, plus the `core` fallback that owns `markdown`), classifies each file into its node kind, and declares its on-disk `kinds` catalog (per-kind frontmatter `schema` + `ui` presentation block). **Deterministic-only**. The retired `gemini` Provider was removed when Google sunset Gemini CLI on 2026-06-18 and replaced it with Antigravity (which declares its own `workflow` kind for `.agent/workflows/` and reuses the open-standard `.agents/skills/` classifier via the `agent-skills` Provider, so under its lens those skills classify as `antigravity`/`skill`). |
| **Extractor** | Extension kind. Reads a node's body and emits work through three callbacks: `ctx.emitLink(link)`, `ctx.enrichNode(partial)`, `ctx.store.write(...)`. **Deterministic-only**: runs synchronously inside `sm scan`. LLM-driven enrichment of a node is an Action concern, not an Extractor concern. |
| **Analyzer** | Extension kind. Evaluates the graph and emits issues. **Dual-mode**: deterministic Analyzers run in `sm check`; probabilistic Analyzers run only as queued jobs (opt-in via `sm check --include-prob`). |
| **Action** | Extension kind. Operation executable over one or more nodes. **Dual-mode**: `deterministic` (plugin code, in-process) or `probabilistic` (rendered prompt the runner executes against an LLM). |
| **Formatter** | Extension kind. Serializes the graph into ascii / mermaid / dot / json. **Deterministic-only** (snapshot diffability). |
| **Hook** | Extension kind. Reacts declaratively to one of nine curated lifecycle events, seven pipeline-driven (`scan.started`, `scan.completed`, `extractor.completed`, `analyzer.completed`, `action.completed`, `job.completed`, `job.failed`) plus two CLI-process-driven (`boot` before verb routing, `shutdown` after the verb's exit code resolves). **Dual-mode**. Reaction-only: a Hook cannot mutate, block, or steer the pipeline. |

### Execution modes

The dual-mode capability is the meta-property that lets the same extension model scale from `pre-commit` (deterministic only) to nightly enrichment (deterministic + probabilistic). Mode is a property of the extension as a whole, not of an individual call.

| Concept | Description |
|---|---|
| **Deterministic mode** | Pure code. Same input → same output, every run. Runs synchronously inside `sm scan` / `sm check`. Fast, free, CI-safe. |
| **Probabilistic mode** | Executed by an LLM OUTSIDE the kernel: the extension contributes a prompt + report schema, the kernel renders and queues a job, an external agent processes it (claim/record). Output may vary across runs. NEVER participates in `sm scan`. The kernel rejects probabilistic extensions that try to register scan-time hooks at load time. |
| **Per-kind capability** | Three kinds are dual-mode (declared in manifest's `mode` field): **Analyzer**, **Action**, **Hook** (Action requires the field; the others default to `deterministic`). Three kinds are deterministic-only because they sit on the deterministic scan path: **Provider** (filesystem-to-graph), **Extractor** (parsed-node-to-callbacks), **Formatter** (graph-to-string). The `mode` field MUST NOT appear on Provider, Extractor, or Formatter manifests. |

The full normative contract lives in [`spec/architecture.md`](./spec/architecture.md) §Execution modes.

### Architecture

| Concept | Description |
|---|---|
| **Kernel** | Domain core. Pure logic; performs no direct IO. Exposes use cases. |
| **Port** | Interface declared by the kernel. Enables adapter injection. |
| **Driving adapter** | Primary adapter, consumes the kernel from the outside. CLI, Server, Skill agent. |
| **Driven adapter** | Secondary adapter, implements a kernel port. SQLite storage, FS, Plugin loader, LLM runner. |
| **Hexagonal** | Ports & adapters pattern. Canonical name of this project's architecture. |

### Job runtime

| Concept | Description |
|---|---|
| **Action (type)** | Defined by a plugin. What the user can invoke. |
| **Job** | Runtime instance of an Action over one or more nodes (replaces the term "dispatch"). Lives in `state_jobs`. |
| **Job content** | Rendered prompt (preamble + action template + interpolated node content) for a job, stored DB-only in `state_job_contents` keyed by `content_hash`. No on-disk artifact; `sm jobs preview` and `sm jobs claim --json` read it from the DB. Ephemeral. |
| **Agent process protocol** | THE execution path for probabilistic jobs: an external agent (any LLM runtime) loops `sm jobs claim` (reap-first, atomic, returns the rendered prompt + nonce) → executes with its own model → `sm record` (nonce-validated callback). skill-map never invokes an agent; the removed `sm jobs run` / `RunnerPort` spawn path (decision 2026-07-13) is documented in `spec/architecture.md` §Execution handover. |
| **Report** | JSON produced by a job, validated against the schema declared by the action. |
| **Callback** | Call to `sm record` that closes a job: status, tokens, duration. |
| **Nonce** | Unique per-job token (`state_jobs.nonce`), handed back by `sm jobs claim --json`. Required by `sm record` to prevent callback forgery. |
| **Content hash** | Hash identifying a job for deduplication: `sha256(actionId + actionVersion + bodyHash + frontmatterHash + promptTemplateHash)`. |
| **Atomic claim** | `UPDATE ... RETURNING id` operation letting a runner take a queued job without a race. |
| **Reap** | Automatic process at the start of every `sm jobs claim` that detects `running` jobs with expired TTL and marks them `failed` (reason `abandoned`). |

### States

| Concept | Description |
|---|---|
| **queued** | Job created, awaiting a runner. |
| **running** | A runner claimed it; execution in flight. |
| **completed** | The runner finished successfully and the report validated. |
| **failed** | The runner reported an error, or the job was abandoned by TTL. |
| **abandoned** | Sub-state of failed: runner died without a callback. |
| **stale** | Data computed over an older `body_hash`; the file has changed since. |
| **orphan** | Node with DB history but no file on disk. |

### Plugins and storage

| Concept | Description |
|---|---|
| **Plugin** | Distributable unit registering one or more extensions. Drop-in at `<scope>/.skill-map/plugins/<id>/`. |
| **Extension** | One of the 6 categories (provider, extractor, analyzer, action, formatter, hook) a plugin contributes. |
| **Drop-in** | Installation mode: place files in the right folder and they appear. No `sm plugins add`. |
| **Spec-compat** | Semver range in the plugin manifest against the spec version. Checked at load. |
| **Plugin storage** | Plugin declares `storage: { mode: 'kv' }` and uses `ctx.store.{get,set,list,delete}`, persisted in the kernel table `state_plugin_kvs`. |

### Refresh and analysis

| Concept | Description |
|---|---|
| **Deterministic refresh** | Re-scan of a node: recomputes bytes, tokens, hashes, links. Synchronous, no LLM. `sm scan -n <id>`. |
| **Probabilistic refresh** | Enqueues an LLM-backed action (summarizer, what, cluster). Async. `sm jobs submit <action> -n <id>`. |
| **Summarizer** | Action that produces a structured semantic summary of a node. ONE universal built-in (`core/ai-summarizer-action`, no precondition: every node body is markdown prose); the per-kind / per-Provider summarizer roster was dropped (2026-07-13). Any Action becomes a summarizer by extending the canonical node-summary schema (`summaries/markdown.schema.json`) from its own report schema. |
| **Meta-skill** | Conversational skill (`/skill-map:explore`) that was to consume `sm … --json` verbs and maintain follow-ups with the user. DISCARDED 2026-07-23 with the graph-scoped second wave; term kept for historical citations. |

### Safety and content

| Concept | Description |
|---|---|
| **User-content delimiter** | XML tags `<user-content id="...">...</user-content>` that wrap user content inside rendered job content. The kernel escapes any literal `</user-content>` inside the content. |
| **Prompt preamble** | Canonical block auto-prepended by the kernel to every rendered job content. Instructs the model to treat user-content as data, not instructions. |
| **Safety object** | Block in probabilistic reports (sibling of `confidence`): `injectionDetected`, `injectionType`, `contentQuality`, `injectionDetails`. |
| **Injection detection** | Detection (by the model) of prompt-injection attempts inside node content. Categorized as direct-override / role-swap / hidden-instruction / other. |

### Enrichment and provenance

| Concept | Description |
|---|---|
| **Enrichment** | Fetching external data (GitHub stars, last activity) to augment node info. Action with a refresh TTL. |
| **Provenance** | Frontmatter section: `metadata.source` (canonical URL) + `metadata.sourceVersion` (tag or SHA). |
| **Hash verification** | Comparison of local `body_hash` against the hash computed over raw GitHub content to set `verified: true/false`. |

### Scope and persistence

| Concept | Description |
|---|---|
| **Scope** | Skill-map operates exclusively on the project scope. DB at `<cwd>/.skill-map/skill-map.db`; config at `<cwd>/.skill-map/settings.json` + `settings.local.json`. There is no opt-in global scope (see `spec/cli-contract.md` §Scope is always project-local). To extend the scan beyond the project root the user passes positional roots to `sm scan [roots...]` (per-invocation, not persisted). |
| **Zone scan_** | Prefix for **regenerable** tables: `sm scan` truncates and repopulates them. E.g. `scan_nodes`, `scan_links`. |
| **Zone state_** | Prefix for **persistent** tables: jobs, executions, summaries, plugin_kv. Back up. |
| **Zone config_** | Prefix for user-owned tables: plugin import-trust grants, preferences, schema versions. |
| **Migration** | Versioned `.sql` file (`NNN_snake_case.sql`) that evolves the schema. Up-only. |
| **user_version** | Built-in SQLite PRAGMA. Fast tracking of the kernel schema. |
| **Auto-backup** | Automatic copy of the DB to `.skill-map/backups/…db` before applying migrations. |

### CLI and UI

| Concept | Description |
|---|---|
| **Introspection** | Property of the CLI to emit its own structure (`sm help --format json`), consumed by docs, completion, UI, agents. |
| **Graph view** | Main UI view: nodes + links, interactive. |
| **List view** | Tabular view of nodes with filters and sort. |
| **Scan corpus vs render cap** | Two separate knobs. The scan walks + analyzes + reference-validates the full corpus up to `scan.maxScan` (default 5000), so links resolve across a large monorepo regardless of what is drawn; the graph map renders only a selected folder branch capped at `scan.maxNodes` (default 256, a Foblex projection bound). The folders tree is the full-corpus navigator: it lists everything with per-folder issue badges and scopes the map to a chosen branch. The render cap is surfaced on both surfaces: `sm scan` prints an info advisory when the corpus exceeds it, and the in-map banner fires only when the CURRENT selection exceeds the cap (user decision 2026-07-28, reverting the earlier corpus-wide fallback: once the operator narrowed to a fitting scope the message read as noise, and the auto-opened folders rail already signals that the map is scoped). **Seniority fill (2026-07-28, user request while testing the 1000-node perf fixture)**: with the root excluded and 2+ includes, the cap fills by SELECTION ORDER, oldest include first (path order within each), so a small folder picked early keeps its nodes when a later, larger selection overflows the cap; every other scope shape keeps plain path order (fast path, byte-identical query). The ordinal is the override Map's insertion order made load-bearing (persisted as an array-of-pairs localStorage shape; `compileOverridesToWire` stops sorting includes; the refetch key derives from the wire projection), the wire carries it as the repeated `path=` order (significant per spec §Map scope overrides · Seniority fill), and SQL ranks rows by a CASE ladder over the same per-include admission terms as the WHERE. Two semantic decisions, both falling out of the canonical delete-then-append model: re-checking a folder is a NEW selection (seniority lost, goes to the end; no ghost state), and selecting a parent after a child swallows the child's entry (everything re-enters with the parent's newest seniority). Benign: nothing is dropped, only the projection paginates. |
| **Inspector panel** | UI section showing detail of the selected node: metadata, weight, summary, links, issues, findings. |
| **Issues panel** | UI section fed by `sm check` (deterministic). |
| **Findings panel** | UI section fed by `sm findings` (probabilistic). |
| **WebSocket** | Bidirectional protocol between server and UI. Push of events (job lifecycle, scan updates) + user commands (rescan, submit, cancel). |

---

## Visual roadmap

Mirrors the interactive timeline on `skill-map.ai` (driven by `web/modules/roadmap.js` `PHASES`). Seven segments (0 / A / B / C / D / E / F): six phases to 1.0 (0 through E), plus F beyond; 0 ships highlights, A/B/D/E ship numbered steps, C/F ship sketches.

```text
═══════════════════════════════════════════════════════════════════════════
  PHASE 0 · DEFINITION (project shape and the standard)
═══════════════════════════════════════════════════════════════════════════
● Hexagonal architecture · kernel + ports + adapters + 6 plugin kinds
● Persistence model · 1 project scope × 3 zones
● Job subsystem · atomic claim, nonce, kernel-enforced preamble
● Plugin model · plugin-scoped KV storage
● Frontmatter standard · universal base · provider-owned kind schemas
● Trigger normalization · 6-step pipeline
● Config hierarchy · defaults → project → project-local → override
● Versioning policy · changesets, independent semver per package
● Spec as a standard · separable from reference impl
● 38 schemas + 14 prose contracts + conformance suite
● 293 architectural decisions, logged
● @skill-map/spec published on npm
  ────────────────────────────────────────────────────────────────────────
   ▶ @skill-map/spec released

═══════════════════════════════════════════════════════════════════════════
  PHASE A · DETERMINISTIC CORE (scan, model, query, no LLM)
═══════════════════════════════════════════════════════════════════════════
●  0b   Implementation bootstrap     workspace, kernel shell, CLI binary, conformance harness, CI green
●  0c   UI prototype (Flavor A)      Angular + Foblex Flow + PrimeNG, mock collection, list / graph / inspector
●  1a   Storage + migrations         SQLite via node:sqlite, kernel migrations, auto-backup, sm db * verbs
●  1b   Registry + plugin loader     six kinds enforced, drop-in discovery, sm plugins list/show/doctor
●  1c   Orchestrator + dispatcher    scan skeleton, full Clipanion verb registration, sm help, autogen reference
●  2    First extensions             claude provider · 3 extractors · 3 analyzers · ASCII formatter · validate-all
●  9.7  Multi-provider rollout       declarative kernel walker (parser registry) · gemini + agent-skills providers · `classify(): string \| null` · per-Provider painting · `note` → `markdown` rename (Gemini was later retired 2026-05-19 when Antigravity replaced Gemini CLI under the open `.agents/skills/` standard)
●  3    UI design refinement         node cards, connection styling, inspector layout, dark mode parity
●  4    Scan end-to-end              sm scan persists · per-node tokens · external-url-counter · --changed · sm list/show/check
●  5    History + orphans            scan_meta · sm history + stats · auto-rename heuristic · sm orphans · canonical-YAML hash
●  6    Config + onboarding          settings(.local).json · 6-layer loader · sm config * · .skillmapignore · sm init · scan strict
●  7    Robustness                   sm watch (chokidar default, parcel selectable) · link-kind-conflict analyzer · sm jobs prune · trigger normalization
●  8    Diff + export                sm graph · sm scan compare-with · sm export with mini query language
●  9    Plugin author UX             plugin runtime · plugin migrations · author guide
●  ALm  Active-lens migration        Phases 1–6 (2026-05-19→05-23): active-provider lens · Signal IR scaffold · numeric `Confidence` · MCP virtual nodes + `core/mcp-tools` extractor · OpenAI Codex provider (`.codex/agents/*.toml`) · Antigravity onboarded + Gemini retired · lens-only extractor gating · provider-aware confidence bump on resolved links · reserved-name catalog + analyzer + confidence downgrade · observable link analysis (`core/link-counts` chips, in/out per-kind tooltip) · lens-drift warning · db-version skew detection · auto-detect on first scan
  ────────────────────────────────────────────────────────────────────────
   ▶ deterministic core + Web UI shipped · skill-map@0.6

═══════════════════════════════════════════════════════════════════════════
  PHASE B · STABILIZATION (deterministic core hardened into Beta)
═══════════════════════════════════════════════════════════════════════════
● Plugin model settled · six kinds · Audit absorbed into Analyzer · open node kinds
● Active-lens multi-provider · claude · codex/codex · agent-skills · antigravity (gemini retired)
● Signal IR + cross-extractor collision detection · core/extractor-collision
● Annotations · co-located .sm sidecars · drift detection · git-author · write-consent
● MCP virtual nodes + observable link analysis · core/link-counts chips · edge opacity
● Reserved-name catalog + analyzer
● Naming diagnostics · core/name-mismatch (per-kind identifierMismatch knob; since 2026-07-22 every built-in kind declares warn, the info tier stays for external providers, and the core/ai-name-action fixer settles the dual identity like ai-reference-action settles broken links) · name-collision two tiers (error declared-vs-declared, warn declared-vs-file-derived shadowing)
● Extensible inspector · plugin action buttons · per-plugin sections · parametrized actions
● Fused workspace · files rail + graph + inspector · map curation · isolate-chain
● Hardening · loopback-only serve · node caps · opt-in off-by-default telemetry
  ────────────────────────────────────────────────────────────────────────
   ▶ deterministic core complete and stabilized into Beta · skill-map@0.52 (the Codex body extractor, Step 13, was the last pre-v1.0 deterministic deliverable)
  ────────────────────────────────────────────────────────────────────────
   ▶ Beta

═══════════════════════════════════════════════════════════════════════════
  PHASE C · REAL-TIME EXPLORATION (shipped, watch execution as it happens)
═══════════════════════════════════════════════════════════════════════════
●       Live node activity           map + spine glow per invocation, four runtimes (claude / codex / antigravity / opencode)
●       MCP client                   config-side discovery (mcpConfig) + live tool-invocation glow · four runtimes · read-only /mcp server (opt-in)
●       Execution stats + spawns     ephemeral per-node counters · spawn edges · session capsules · camera follow
●       Live conversations           consent-gated threaded agent dialogs (opt-in, ephemeral)
  ────────────────────────────────────────────────────────────────────────
   ▶ real-time exploration shipped · skill-map@0.87

═══════════════════════════════════════════════════════════════════════════
  PHASE D · LLM AS AN OPTIONAL LAYER (summaries, findings)
═══════════════════════════════════════════════════════════════════════════
●  9.6  Foundation refactors         Open node kinds · storage port promotion (5 namespaces) · universal enrichment · incremental scan cache
●  10a  Queue infrastructure         state_jobs + content-addressed state_job_contents · atomic claim · sm jobs submit/list/show/preview(--last)/claim/cancel/fail/status · sm record + nonce
●  10b  Agent process protocol         claim (reap-first) + record (synthetic ndjson envelope) · runner=agent · sm jobs run/RunnerPort built then REMOVED (pull-only decision 2026-07-13)
●  10c  First probabilistic ext      ai-summarizer-action (Action) · state_summaries write-through · preamble-bitwise-match conformance
●  10d  Doctor + actions verbs       sm doctor (7 checks; the runner probe died with the pull-only cut) · sm actions list/show
●  10e  Phase G polish               sm-process-jobs skill + sm agent verbs · HTTP + Settings install button · github/enrichment (Model A, io:[network] + allowNetworkActions) · extension-mode-routing · (MCP queue-aware deferred to the UI arc)
●  11   Findings + finder analyzers  state_findings + findings/report envelope · sm findings lifecycle (fixed / human-decision / dismissed · supersede) · analyzers join the queue (extension_id rename) · finder/fixer pairs via Modelo B (redundancy · contradiction · incoherence · 5 optimization pairs · security + suspicion) · auto-fix chain · pair toggle (LLM verbs dropped, Decision #137; the /skill-map:explore meta-skill died with the discarded graph-scoped wave, 2026-07-23)
●  16   UI: findings workbench v1    Inspector findings card + finder/fixer/standalone launchers · per-row fix / dismiss / resolve · hidden buckets · issue-row fixers · queue rail tab · BFF endpoints
  ────────────────────────────────────────────────────────────────────────
   ▶ LLM optional layer SHIPPED STABLE · @skill-map/cli 0.89.0 + @skill-map/spec 0.81.0 on npm latest (2026-07-23, rc.0→rc.4 cycle promoted via PR #164; rc dist-tags retired, release/rc deleted) · MCP queue-aware /mcp SHIPPED 2026-07-23

═══════════════════════════════════════════════════════════════════════════
  PHASE E · SURFACE & DISTRIBUTION (full web UI, deeper AI surfaces, single-package release)
═══════════════════════════════════════════════════════════════════════════
  Legend: ● done · ◐ partial (substantial progress, named residue) · ○ not started
●  14a  Web UI: BFF + transport      Hono BFF · WebSocket /ws · single-port mandate · Angular SPA + REST + WS under one listener · sm serve --port N
●  14b  Web UI: Flavor B slice       Inspector with enrichment + summaries + findings · command submit from UI · chokidar live updates · MD body renderer pick
●  14c  Web UI: polish & budgets     URL-synced filter state · responsive scope · bundle budget · dark mode tri-state · Foblex types reassessment
●  17   UI: LLM surfaces v2          queue inspector · findings management · settings + plugins page · settings-hierarchy viewer · connected agents driving the queue over MCP · WCAG AA pass
●  15a  Single package distrib       @skill-map/cli with UI bundled · sm + skill-map binary aliases · settings loader · CI validate + e2e smoke + web/demo deploy
●  15b  Documentation surface        llms.txt + llms-full.txt (AI ingestion) · skill-map.ai live
●  15c  Release infrastructure       GH Actions release + changelog · telemetry opt-in · breaking-changes policy · sm doctor diagnostics
  ────────────────────────────────────────────────────────────────────────
   ▶ v1.0 SHIPPED · @skill-map/cli + @skill-map/spec on npm latest (1.0.0 pair burned on release, restarted at 1.0.1) · standard SemVer is the live regime from here

  Cut from 1.0 (2026-07-23/24): the `sm ui` verb (UI is served by `sm serve`),
  the 5 named semantic flows + cost dashboard (redundant with the generic
  AI-actions surface; self-reported tokens), the Astro Starlight docs site,
  the compatibility matrix, and the Claude Code plugin wrapper (vendor-specific,
  breaks runtime-neutrality). Responsive scope is desktop-first by design.

═══════════════════════════════════════════════════════════════════════════
  PHASE F · BEYOND (post-v1.0, to evaluate)
═══════════════════════════════════════════════════════════════════════════
○       Marketplace ?                plugin discovery and distribution, to evaluate
○       Multi-host: Copilot etc.     Copilot provider · generic frontmatter-driven Provider · per-host sm-<host>-* namespace (moved out of 1.0 scope 2026-07-23; Codex body extractor + adapter conformance already landed, four lenses onboarded)
○       PrimeNG exit / Angular       migrate 23 primeng modules → CDK/custom (MIT) to unblock Angular majors past 21 (PrimeNG went commercial "PrimeUI" at v22; decision 2026-07-20 stays on 21.1.9 MIT + Angular 21.2.17); ~weeks; INTERNAL tech-debt, kept OFF the public marketing site by design
═══════════════════════════════════════════════════════════════════════════

  Analyzer: the LLM is never required. Product is complete offline through Phase A.
```

---

## Spec as a standard

`skill-map` is a reusable standard, not only a tool. The **spec** is separated from the **reference implementation** from day zero. Anyone can build a UI, a CLI, a VSCode extension, or an entirely new implementation (any language) using only `spec/`, without reading the reference source.

**Conformance made real (2026-07-30, preparing the v1.0.0 gate).** Auditing the coverage matrix against the v1.0 release gate ("every row 🟢 covered or 🟠 deferred to v1.1") turned up something worse than missing cases: `file-matches-schema`, the assertion that would let a row claiming a schema is covered actually MEAN it, had shipped as a permanently-failing stub ("requires ajv; lands with Step 2") and no case used it. Zero usage meant zero red, so the gap stayed invisible for the project's whole life. Its stdout twin, which is the form a CLI's `--json` surface actually needs, did not exist at all. Both now work, reading the spec tree directly rather than borrowing the kernel's closed schema list, because a conformance runner checks an implementation against the spec AS PUBLISHED and inheriting the kernel's list would scope it to "the schemas this implementation already knows".

Three defects surfaced in the same pass, each of the same family, a declared contract nobody exercised. The vocabulary's stability clause omitted `stdout-contains-verbatim`, which had shipped in both schema and runner. Three assertions advertised glob support in their normative descriptions that no implementation ever honoured and no case ever used; the claim was removed rather than implemented on the eve of freezing the contract. And `json-path` THREW instead of failing when its expression matched nothing, because `JSON.stringify(undefined)` is not a string, which fired exactly when a case probed a key an implementation does not emit, i.e. the moment the author most needed a readable failure.

The matrix went from 33 red and 11 partial rows to zero of either. Grouping the survivors by what actually blocked them, rather than listing them, exposed that most shared three causes, so the vocabulary was extended and ten of them closed before the release instead of after: **`each`** validates every element of a list surface (and REJECTS an empty array, since validating zero elements is the vacuous pass list rows hide behind), **`schemaPointer`** targets a `$def` inside a schema, **`expectExit`** lets a staged step declare a refusal, and **`capture`** binds a value from one step's stdout into a later invocation, which is what finally made the whole `sm record` family reachable: the nonce is minted at claim time and no static case could carry it. Substitution deliberately never touches `verb` or `sub`, so a captured value can never redirect which command runs.

Three of the fourteen were not blocked at all, which is its own lesson about deferral notes. `history-stats` had inherited its sibling's "output is a list" diagnosis and is a single object. And `job.schema.json` was waiting on a `sm jobs show --json` surface that already existed, except it can never satisfy that schema: the schema requires `nonce` and every READ surface projects through the nonce-stripping `PublicJob` view, so submit is the only surface that can close the row. And `report-base-deterministic` was already covered transitively by `bump-report-schema`, which extends it via `allOf` and asserts the one field the base owns. A deferral note is a claim, and these three were never checked.

One more was closed by building the missing piece instead of deferring around it. `conformance-result.schema.json` was blocked because a case invoking the suite would run the whole suite including itself, so `sm conformance run` gained `--case <id>`: the case now runs ONE other case and validates the envelope, and the recursion is broken by construction. An id matching nothing exits 2 rather than reporting a clean sweep of zero cases, which is the failure mode worth designing against, a typo in CI going green forever. The flag is normative (`cli-contract.md`) precisely because without it no implementation can exercise its own report shape end-to-end.

The three server-flavoured rows closed on 2026-07-31 with the server-capable runner. `setup.serve` boots the implementation's server (`sm serve --no-open --no-watcher --port 0`) on an ephemeral port inside the scope, observes readiness via the `serve.json` the contract already mandates, and holds it through the main invoke AND assertion evaluation before an awaited SIGTERM/SIGKILL teardown, so the child never outlives the case; that window is what lets `http-matches-schema` validate a live `GET /api/nodes` against `api/rest-envelope.schema.json` and a plain `file-matches-schema` observe `serve-info.schema.json` in the only moment the file exists. The third row was another deferral-note lesson: the `run.*` envelope had been diagnosed as websocket-blocked, but run-level events deliberately never travel the server push leg (`job-events.md` names `sm record --json`'s synthetic envelope as the record surface), so the contract was a stdout ndjson one all along and the missing piece was only the line-oriented `ndjson-line` assertion (every non-empty line must parse, first line deep-equal on top-level keys selects, the `json-path` comparators then apply to the matched line).

The two rows that needed primitives the format lacked (concurrent invocation for the claim race, a clock for reap) closed on 2026-07-31 with the last two case-format additions. `invoke.parallel` spawns N identical invocations concurrently, all started before any is awaited, because the atomic claim (`job-lifecycle.md` §Concurrency) is only observable as a race; with one queued job and two concurrent claims, the `parallel-exit-codes [0, 1]` set assertion IS the atomicity proof (two zeros would be a double handout, two ones a lost job), and `parallel-json-path-count` pins exactly one `$.nonce` handover. Setting `parallel` makes every per-result assertion an authoring error the runner fails loudly before spawning anything, since "the" result is ambiguous across N. The clock is deliberately not a clock but a staged sleep: `sleepAfterMs` on a `priorInvokes` step exists solely so TTL-expiry contracts become observable (`ttl-reap-abandoned` submits with `--ttl 1`, claims, sleeps 3000ms, and the next claim's ride-along reap fails the expired job with `abandoned`), and the old objection that a sleep would make the suite timing-dependent is answered by proportionality: cases arm at least 3x the TTL they staged, so wall-clock noise cannot flip the outcome.

The recorded-network-transport row closed on 2026-07-31 with `setup.staticServe`: the runner serves a fixture of RECORDED responses (plain files whose relative paths mirror the request paths) read-only over loopback HTTP on an ephemeral port and binds the base URL as `{{staticServeUrl}}`, so `github-enrichment-recorded` redirects `github/enrichment`'s base-URL settings at it and the real fetch chain (API ref resolution, raw fetch at the resolved SHA) runs against deterministic bytes while the scope stays offline. The base-URL settings themselves are new and PROJECT-LOCAL ONLY (`plugins.github.extensions.enrichment.settings.apiBaseUrl` / `.rawBaseUrl` joined `PROJECT_LOCAL_ONLY_KEYS`): the token setting rides the Authorization header to whatever host `apiBaseUrl` names, so a committed override in a cloned repo would exfiltrate it on the first `sm enrich`; the loader strips a committed value with a warning, exactly the `scan.followExternalSymlinks` treatment. `allowNetworkActions` itself joined the same class on 2026-08-01 (audit): it had shipped as a COMMITTED team-shared policy while its own contract promised that "a cloned repo must not be able to make skill-map fetch remote content without the operator's explicit consent", which the committed layer cannot deliver, because that file IS the cloned repo's. A hostile project shipped `allowNetworkActions: true` beside an enabled network action and the victim's first `sm enrich` reached out unasked. The rule extracted from it: a committed policy key is safe exactly when its committed value can only be MORE restrictive than its default, which is why `allowSidecarWriters` (default `true`, so a repo can only turn its own extensions off) deliberately stayed put. The case asserts through the gate the schema governs (row 12's doctrine): `sm enrich` validates the report against the action's schema (which `allOf`-extends the canonical `enrichments/github.schema.json`) before persisting, and the report values (`verified: true`, the recorded SHA) surface via `sm history --json`'s `reportPath`, today the only surface that re-emits the persisted report (as a JSON string, so the values are pinned by regex rather than a second direct schema validation).

The self-referential row closed at the only place it could: the runner now validates every case document against `conformance-case.schema.json` BEFORE executing it (normative in the conformance README, `case-invalid` on failure), and a unit sweep audits every bundled case across all six scopes. A case can never assert this schema, since the documents live outside the provisioned scope and a case invoking the suite would recurse; the load gate is the point every case necessarily passes through, so nothing runs without the schema having held. The gate also promptly caught three of the suite's own synthesized test cases that omitted `description`, which is the mechanism working as intended. One row remains, deliberate rather than blocked: `user-settings.schema.json` lives outside the provisioned scope and the containment guard refuses it correctly, so the row waits rather than the guard weakening.

Two habits proved their worth and are worth keeping. Every new case was checked in BOTH directions, most sharply the enable-gate case, whose fixture module throws at import: with `stability: 'experimental'` the plugin reports `enabled` with zero extensions, and with the field removed it reports `load-error`, so the case detects a broken gate instead of passing by inertia. And a promotion was nearly made on a false premise: `scan-result.schema.json` `$ref`s `node` / `link` / `issue`, so validating a populated ScanResult covers all three transitively, except the chosen fixture emitted zero links and zero issues. An empty array validates against anything. The case was repointed at a fixture that populates all three and now ASSERTS they are non-empty, so the transitive coverage cannot silently evaporate when a fixture changes.

### Repo layout

```
skill-map/
├── spec/                          ← source of truth for the STANDARD (38 schemas + prose contracts + plugin author guide)
│   ├── README.md                  ← human-readable spec
│   ├── CHANGELOG.md               ← spec history (independent from tool)
│   ├── versioning.md              ← evolution policy
│   ├── architecture.md            ← hexagonal ports & adapters
│   ├── cli-contract.md            ← verbs, flags, exit codes, JSON introspection
│   ├── job-events.md              ← canonical event stream schema
│   ├── prompt-preamble.md         ← canonical injection-mitigation preamble
│   ├── db-schema.md               ← table catalog (kernel-owned)
│   ├── plugin-kv-api.md           ← ctx.store KV persistence contract
│   ├── job-lifecycle.md           ← queued → running → completed | failed
│   ├── index.json                 ← machine-readable manifest + per-file sha256
│   ├── package.json               ← published as @skill-map/spec
│   ├── plugin-author-guide.md     ← drop-in plugin authoring contract (manifest, six kinds, storage)
│   ├── schemas/                   ← 25 JSON Schemas, draft 2020-12, camelCase keys
│   │   ├── node.schema.json                 ┐
│   │   ├── link.schema.json                 │
│   │   ├── issue.schema.json                │
│   │   ├── scan-result.schema.json          │
│   │   ├── execution-record.schema.json     │ 11 top-level
│   │   ├── project-config.schema.json       │
│   │   ├── plugins-registry.schema.json     │
│   │   ├── job.schema.json                  │
│   │   ├── report-base.schema.json          │
│   │   ├── conformance-case.schema.json     │
│   │   ├── history-stats.schema.json        ┘
│   │   ├── api/                             ← BFF wire envelopes (Step 14.2)
│   │   │   └── rest-envelope.schema.json    ← 1 envelope schema
│   │   ├── extensions/                      ← one per extension kind (loaded at plugin load)
│   │   │   ├── base.schema.json             ┐
│   │   │   ├── provider.schema.json         │
│   │   │   ├── extractor.schema.json        │ 7 extension schemas
│   │   │   ├── analyzer.schema.json             │ (base + 6 kinds)
│   │   │   ├── action.schema.json           │
│   │   │   ├── formatter.schema.json        │
│   │   │   └── hook.schema.json             ┘
│   │   ├── frontmatter/                     ← universal-only; per-kind schemas live in the Provider that declares them
│   │   │   └── base.schema.json             ← 1 universal frontmatter schema
│   │   └── summaries/                       ← kernel-controlled; additionalProperties: false
│   │       ├── skill.schema.json            ┐
│   │       ├── agent.schema.json            │ 5 summaries (extend
│   │       ├── command.schema.json          │ report-base via allOf)
│   │       ├── hook.schema.json             │
│   │       └── markdown.schema.json         ┘
│   ├── interfaces/
│   │   └── security-scanner.md              ← convention over the Analyzer kind (NOT a 7th kind)
│   └── conformance/
│       ├── README.md                        ← human-readable guide to the suite
│       ├── coverage.md                      ← release-gate matrix (schemas + artifacts ↔ cases)
│       ├── fixtures/                        ← controlled MD corpora + preamble-v1.txt
│       └── cases/                           ← basic-scan, kernel-empty-boot (preamble-bitwise-match deferred to Step 10)
└── src/                           ← reference implementation (published as skill-map)
```

### Properties

- **Machine-readable**: all schemas are JSON Schema; validate from any language.
- **Human-readable**: prose documents with examples.
- **Independently versioned**: spec `v1.0.0` implementable by CLI `v0.3.2`.
- **Platform-neutral**: no Claude Code required in any schema; it's one example adapter.
- **Conformance-tested**: any implementation passes or fails, binary.

### Distribution

- Publish schemas to JSON Schema Store (unblocked by the 1.0.0 release: the stable `v1` URL scheme it waited on is now the shipped one).
- Canonical URLs: `https://skill-map.ai/spec/v1/<path>.schema.json` (Railway-deployed Caddy; DNS at Vercel). The scheme bumped from the pre-stable `v0` at the 1.0.0 release, as `versioning.md` §Canonical URLs prescribed; the `v0` path is no longer emitted by the site build.
- npm package `@skill-map/spec`, schemas + conformance tests.
- Spec semver separate from CLI semver; both tracks stabilized at `v1.0.0` (2026-08) and future versions can diverge. Post-1.0 bumps follow strict SemVer classified critically (user decision 2026-08-02: smallest honest bump, majors are deliberate and user-confirmed); the shifted pre-1.0 convention (minor = breaking, patch = everything compatible) stays documented in [`versioning.md`](./spec/versioning.md) §Pre-1.0 for any future `0.Y.Z` track.

---

## Architecture: Hexagonal (Ports & Adapters)

```
                    Driving adapters (primary)
                         │
   ┌─────────┐       ┌─────────┐       ┌──────┐
   │   CLI   │       │ Server  │       │Skill │
   └────┬────┘       └────┬────┘       └───┬──┘
        │                 │                │
        └─────────────────┼────────────────┘
                          ▼
                   ┌──────────────┐
                   │    Kernel    │  ← domain core (pure use cases)
                   └──────┬───────┘
                          │
      ┌────────┬──────────┴──────────┬────────┐
      ▼        ▼                     ▼        ▼
  ┌────────┐ ┌────┐              ┌─────────┐ ┌────────┐
  │ SQLite │ │ FS │              │ Plugins │ │ Runner │
  └────────┘ └────┘              └─────────┘ └────────┘
                Driven adapters (secondary)
```

(ProgressEmitterPort exists alongside the three shown; its adapters are terminal sinks, `pretty` / `--json` ndjson, and do not participate in the kernel-owning diagram.)

The kernel accepts ports (`StoragePort`, `FilesystemPort`, `PluginLoaderPort`, `ProgressEmitterPort`) and never imports SQLite, fs, or subprocess directly (there is deliberately NO runner port: execution is handed to external agents, `spec/architecture.md` §Execution handover). The normative port contract and IO discipline live in [`spec/architecture.md`](./spec/architecture.md) (§Ports, §Layering). Design consequences worth restating here:

- Each adapter is swappable: in-memory fakes in tests, `SqliteStorageAdapter` in production. The test pyramid collapses cleanly, unit tests inject mocks into the kernel, integration tests wire real adapters.
- CLI and UI are **peers** consuming the same kernel API; neither depends on the other.

### Package layout

pnpm workspaces. Two today (`spec/`, `src/`); `ui/` joins at Step 0c. Changesets manage each package's semver independently (see Decision #5 and the note at the end of this section).

The marker `[Step N]` in the tree below means the folder is part of the target layout and lands at that step, it is NOT yet on disk as of Step 0b. The remaining folders already exist.

```
skill-map/                        ← private root workspace (not published)
├── package.json                  ← { "name": "skill-map-monorepo", "private": true,
│                                     "workspaces": ["spec", "src"],  // "ui" added at Step 0c
│                                     "engines": { "node": ">=24.0" } }
├── .changeset/                   ← changesets config + pending release notes
├── scripts/                      ← build-site.js · build-spec-index.js · check-changeset.js · check-coverage.js
├── web/                          ← editable landing source (HTML/CSS/JS); copied into site/ at build
├── site/                         ← generated public site (Caddy on Railway)
│
├── spec/                         ← workspace #1, published as @skill-map/spec
│   └── (see previous §Repo layout tree)
│
├── src/                          ← workspace #2, published as @skill-map/cli
│   ├── package.json              ← { "name": "@skill-map/cli",
│   │                                  "bin": { "sm": "bin/sm.js", "skill-map": "bin/sm.js" },
│   │                                  "exports": { ".", "./kernel", "./conformance" } }
│   ├── kernel/                   Registry, Orchestrator, domain types, ports, use cases
│   ├── cli/                      Clipanion commands, thin wrappers over kernel
│   ├── conformance/              Contract runner (loads a spec case, asserts against binary)
│   ├── extensions/               Built-in extensions (empty until Step 2; user plugins drop in at `<scope>/.skill-map/plugins/`)
│   ├── test/                     node:test + tsx loader (*.test.ts)
│   ├── bin/sm.js                CLI entry, imports from ../dist/cli
│   ├── index.ts                  Package entry (re-exports)
│   ├── server/         [Step 14] Hono + WebSocket, thin wrapper over kernel
│   ├── migrations/     [Step 1a] Kernel .sql migrations, up-only
│   └── adapters/       [Step 1a+] port implementations
│       ├── sqlite/               node:sqlite + Kysely + CamelCasePlugin
│       ├── filesystem/           real fs
│       ├── plugin-loader/        drop-in discovery
│
└── ui/                 [Step 0c] workspace #3, Angular SPA (standalone) + Foblex Flow + PrimeNG
    └── (scaffolded when Step 0c starts; isolation analyzer: no import from ../src/)
```

Two independently published packages (`@skill-map/spec`, `@skill-map/cli`). Two un-scoped placeholder packages (`skill-map`, `skill-mapper`) were published once to lock the names against squatters and have since been retired locally, they remain on npm with a `npm deprecate` notice pointing at `@skill-map/cli` and the workspaces are gone (see decision #5 history). `ui/` stays private at least through v1.0.0. Plugin authors reach the kernel via `import { registerDetector } from '@skill-map/cli/kernel'` (subpath export). Splitting into more `@skill-map/*` packages is deferred until a concrete external consumer justifies it; the org scope is already protected by ownership of `@skill-map/spec`.

The kernel never imports Angular; `ui/` never imports `src/` internals. The sole cross-workspace contract is `spec/` (JSON Schemas + typed DTOs). At Step 14 the Hono BFF inside `src/server/` exposes kernel operations over HTTP/WS, and `sm serve` serves the built Angular SPA from the same listener (single-port mandate).

---

## Persistence

SQLite at `<cwd>/.skill-map/skill-map.db`, **gitignored by default** because it carries per-developer state (job runs, summaries, plugin KV) most teams do not want to diff in PRs; a team opts into sharing audit history via the experimental `history.share` flag (then re-includes the DB with a `!skill-map.db` line in the scope ignore file). The ignore rules for everything skill-map generates live in a committed `<cwd>/.skill-map/.gitignore` written by the tool itself ([`spec/cli-contract.md`](./spec/cli-contract.md) §Scope ignore file), NOT appended to the project-root `.gitignore`: the rules travel with the directory they describe, they reach a project bootstrapped by an older CLI on its next scan (the previous design shipped the list once at `sm init` time, so every artifact added afterwards, the activity bridge, the operations log, the SQLite `-wal` / `-shm` sidecars, leaked into users' commits), and skill-map stops editing a file it does not own. Top-up is additive and honours a `!` negation as the operator's explicit opt-out. The same doctrine reaches the skill folders the CLI materialises OUTSIDE the scope directory (`<skillDir>/sm-process-jobs/` from `sm agent install`, `<skillDir>/sm-tutorial/` from `sm tutorial`, 2026-08-03): each is a verbatim copy of what the binary ships, compared byte-for-byte to decide staleness, so it is a generated artifact and gets a `.gitignore` of its own (a bare `*`, which hides the file too, so the folder disappears from git rather than leaving a husk). Written when the folder is CREATED, never on a refresh and never over an existing file, and deliberately outside the canonical file set, otherwise deleting it (the supported way to commit the folder anyway) would report the skill as permanently outdated. Both folders are also excluded from the scan by default; `sm-tutorial/` had been missing from that list, so the tutorial's own pages were being mapped as project content. There is no global / user scope: the CLI never reads `$HOME` by default, and the only way to extend the scan beyond `<cwd>` is passing positional roots to `sm scan` (per-invocation, never persisted). The one documented `$HOME` exception is `~/.skill-map/settings.json` (per-machine preferences, read directly, never merged into the project layers). Scope principle: [`spec/cli-contract.md`](./spec/cli-contract.md) §Scope is always project-local.

The full table catalog, column conventions, and migration rules are normative in [`spec/db-schema.md`](./spec/db-schema.md). Design shape worth restating:

- **Three zones**: `scan_*` (last scan result, truncated and repopulated by `sm scan`), `state_*` (persistent operational data: jobs, executions, summaries, enrichments, plugin KV), `config_*` (user-owned config). Backups preserve `state_*` + `config_*`; `scan_*` regenerates on demand.

- **Data access**: Kysely + CamelCasePlugin inside the SQLite adapter; the kernel and adapters consume typed `camelCase` repos and never see SQL (the `snake_case ↔ camelCase` mapping is automatic). Full ORMs (Prisma, Drizzle, TypeORM) were rejected as incompatible with hand-written `.sql` migrations. Table / column / index naming conventions are in [`spec/db-schema.md`](./spec/db-schema.md).
- **File permissions**: every file skill-map creates is owner-only (`0o600`), the DB and its backups included, since they carry scanned content (Decision #149). `DatabaseSync` and `copyFileSync` take no mode argument, so those paths chmod after the fact through the shared best-effort helper in `kernel/util/atomic-write.ts`; a `chmod` failure never fails the operation that produced the file.
- **Migrations**: up-only `.sql` files, auto-applied on startup with auto-backup. **Pre-1.0 schema drift**: while the kernel is in `0.Y.Z` there are no incremental kernel migrations; a write-side open (`sm scan`, `sm watch`, `sm serve` before listening) detects drift on two axes, the recorded `scan_meta.scanned_by_version` against the running CLI (any `major.minor` mismatch is drift) AND the recorded `scan_meta.schema_fingerprint` (sha256 of the bundled migration DDL) against the live fingerprint (any mismatch, or a NULL value from a pre-fingerprint DB, is drift). The fingerprint axis catches an inline `001_initial.sql` column add within the same `major.minor` (greenfield posture, no version bump) that the version axis cannot see. Either axis deletes and rebuilds the DB from `001_initial.sql` (the cache is derived; `.sm` sidecars hold the real data); a TTY operator confirms first (`--yes` / non-TTY auto-rebuild), with the prompt defaulting to Yes since 1.0.x (bare Enter rebuilds, only an explicit no declines; the rebuild is safe and declining dead-ends the verb), declining aborts with a nonzero exit. Read verbs surface a WARN advisory (point at `sm scan` / `sm db reset`) instead of rebuilding, and a read that fails BECAUSE of the drift converts into the same clean advisory (exit 2), never a raw SQL error; non-drift-owning WRITE verbs (the `sm jobs` family, `sm record`, `sm findings prune`, `sm enrich`, `sm plugins trust` / `enable` / `disable`, `sm orphans reconcile` / `undo-rename`) refuse with the advisory on either axis before loading the plugin runtime or touching any table. Real up-only migrations land at `1.0.0`. See [`spec/db-schema.md`](./spec/db-schema.md) §Schema drift (pre-1.0).
- **`sm db` verbs** (`reset` / `reset --state` / `reset --hard` / `backup` / `restore` / `shell` / `dump` / `migrate`) are specified in [`spec/cli-contract.md`](./spec/cli-contract.md).

---

## Job system

### Core model

- **Job** = runtime instance of an Action applied to one or more Nodes, in `state_jobs`. The rendered content (preamble + action template + interpolated node content) is stored DB-only in `state_job_contents` keyed by `content_hash` (no on-disk artifact; jobs that render identically share one content row). State lives in `state_jobs.status`.
- **ID formats**: base shape `<prefix>-YYYYMMDD-HHMMSS-XXXX` (UTC + 4 hex); prefixes `d-` jobs, `e-` execution records, `r-[<mode>-]` runs (modes `ext` / `scan` / `check`), carried in `runId` so parallel runner streams stay demuxable. Full analyzer in Decision #88.

### Lifecycle

```
             submit
                │
                ▼
        ┌──────────┐   atomic claim   ┌──────────┐
        │  queued  │ ───────────────▶ │ running  │
        └────┬─────┘                  └─────┬────┘
             │                              │
   cancel →  │  ← fail          cancel →   │   ← fail
             │                              │ callback success / failure
             │                              │ TTL expires (auto-reap)
             │                              │ runner-error / report-invalid
             ▼                              ▼
  ┌────────────────────┐      ┌─────────────────────────────────┐
  │ cancelled · failed │      │ completed · failed · cancelled  │
  └────────────────────┘      └─────────────────────────────────┘
```

Terminal states `completed` / `failed` / `cancelled`. `cancelled` is a distinct operator-driven state (`sm jobs cancel`), NOT a `failed` sub-reason and with no `failureReason`; `sm jobs fail` is its symmetric counterpart (terminal `failed`, reason `user-failed`). Two mechanisms make multiple runners safe to race, both normative in [`spec/job-lifecycle.md`](./spec/job-lifecycle.md): the **atomic claim** (`UPDATE ... WHERE status='queued' ... RETURNING id`, single-row, priority then FIFO) and the start-of-run **auto-reap** (TTL-expired `running` rows flipped to `failed`, reason `abandoned`).

**TTL** is resolved at submit time and frozen on `state_jobs.ttlSeconds`: `action.probExpectedDurationSeconds` (else `config.jobs.ttlSeconds`, default 3600) times `graceMultiplier` (3), floored at `minimumTtlSeconds` (60), with `config.jobs.perActionTtl.<id>` or `--ttl` overriding. **Duplicate prevention**: a submit matching an active `(actionId, actionVersion, nodeId, contentHash)` in `queued|running` is refused with exit 3 unless `--force`.

### Runners

Three execution paths, matching the `runner` field in `job.schema.json` (`cli` / `skill` / `in-process`):

- **External agent** (`runner: agent`), THE probabilistic path: any LLM runtime (a Claude Code session, Codex, opencode, a cron-driven agent, an MCP client) loops `sm jobs claim` → executes the rendered content with its own model → `sm record`. The agent IS the execution; skill-map only hands over data and validates the callback. Whether the process protocol ships as a skill, an MCP surface, or both is an open evaluation (2026-07-13).
- **In-process** (`mode: local`, `runner: in-process`): the action's own code produces the report synchronously, no rendered content row, no subprocess; the kernel validates it against the action's report schema and transitions straight to terminal. For deterministic enrichment and cheap aggregations.

> **Removed (decision 2026-07-13)**: the third path, a CLI-runner loop (`sm jobs run` spawning `claude -p` through a `RunnerPort`), shipped in Step 10 Phase E and was removed the same week: skill-map must never invoke the agent, whoever the agent is; the queue is pull-only. `spec/architecture.md` §Execution handover carries the normative wording.

Skill agent flow:
```
loop:
  1. bash: sm jobs claim         → <id> or exit 1 (queue empty)
  2. [rendered content from the sm jobs claim --json response, with the nonce]
  3. [agent reasons in-session]
  4. [agent produces the report payload]
  5. bash: sm record --id <id> --nonce <n> --status completed --report <path|-> ...
```

### Nonce + callback auth

Each job carries a unique `nonce` (on `state_jobs.nonce`, handed back by `sm jobs claim --json`); `sm record` requires a matching `--id` + `--nonce`, so a forged callback cannot close someone else's pending dispatch.

### Prompt injection mitigation

Two kernel-enforced layers, normative in [`spec/prompt-preamble.md`](./spec/prompt-preamble.md): (1) every interpolated node body is wrapped in `<user-content id="...">...</user-content>` delimiters (the kernel escapes any literal closing tag with a zero-width space, reversed only for display, never for hashing; nesting is forbidden, one block per node; a template interpolating user text outside a block is rejected at registration); (2) the canonical preamble is auto-prepended before every action template (templates cannot modify, omit, or precede it) and instructs the model to treat user-content as data and record detected injections in the report's `safety` field.

### Atomicity edge cases

Missing-content / mid-run-crash handling (a `state_jobs` row whose `content_hash` no longer resolves in `state_job_contents` → `failed: job-file-missing`, a DB-corruption-only state since submit and prune keep the two tables consistent; orphaned `state_job_contents` rows referenced by no job → collected by `sm jobs prune`; crash between claim and read → covered by auto-reap) is specified in [`spec/job-lifecycle.md`](./spec/job-lifecycle.md).

### Concurrency

The job subsystem runs jobs **sequentially within a single runner**, one claim / spawn / record cycle at a time. There is no pool or scheduler through `v1.0`.

Multiple agents MAY process in parallel (e.g. a cron-driven agent alongside an interactive session). The atomic-claim semantics exist precisely for this case: the `UPDATE ... WHERE status='queued' RETURNING id` guarantees that no two agents ever claim the same row, even when they race.

The event schema carries `runId` + `jobId`; each `sm record` emits one self-contained synthetic envelope, so parallel agents never interleave within an envelope.

### Progress events

Emitted via `ProgressEmitterPort` (adapters: `pretty` default TTY, `--json` ndjson) with `sm record --json` as the canonical emitter (the synthetic `r-ext-` envelope wrapping exactly one job), re-emitted over **WebSocket** by the server when live. The canonical event catalog, the `{ type, timestamp, runId, jobId, data }` envelope, and the synthetic-run pattern (`r-ext-…` / `r-scan-…` / `r-check-…`) are in [`spec/job-events.md`](./spec/job-events.md): the **job family** is stable, the **`scan.*` / `issue.*`** families are experimental until promoted. Task-UI integration (Claude Code's `TaskCreate`, future host primitives) lives as a host-specific skill (`sm-cli-run-queue`), not a CLI output mode; Cursor is out of scope (see §Discarded).

### `sm jobs` CLI surface

The verb surface (`submit` / `list` / `show` / `preview` / `claim` / `run` / `status` / `cancel` / `prune`, with `--run` / `--all` / `--force` / `--ttl` / `--priority`) is specified in [`spec/cli-contract.md`](./spec/cli-contract.md).

---

## Plugin system

### Six plugin kinds

| Kind | Role | Modes | Reads | Writes |
|---|---|---|---|---|
| **Provider** | Knows a platform: declares its kinds + their schemas + globs, classifies paths to kinds. | det only | filesystem | none directly |
| **Extractor** | Extracts data from a parsed node body, emits links, enriches the node, or persists custom data. | det / prob | one node | `links`, enrichment layer, or plugin's own table |
| **Analyzer** | Cross-node reasoning over the merged graph; emits issues. | det / prob | full graph | `issues` |
| **Action** | Operates on one or more nodes; the only kind that mutates source files. | det / prob | one or more nodes | filesystem (det) or rendered prompt to runner (prob) |
| **Formatter** | Serializes the graph to a string output (ASCII / Mermaid / DOT / JSON / custom). | det only | full graph | stdout (string) |
| **Hook** | Reacts to a curated set of kernel lifecycle events; declarative subscriber. | det only | event payload | side effects (notifications, integrations, cascades) |

The six extension kinds are Provider, Extractor, Analyzer, Action, Formatter, Hook. The kernel ships `validate-all` as a Analyzer (post-scan AJV revalidation against the spec schemas); there is no Suite, Enricher, or composer kind, composition is explicit at the verb / Hook level.

### Drop-in installation

No `add` / `remove` verbs: the user drops a plugin folder into `<cwd>/.skill-map/plugins/<plugin-id>/` (the only default discovery root), or points `--plugin-dir <path>` at a custom root per invocation. The directory name MUST equal the plugin id (mismatch → `invalid-manifest`), which eliminates same-root id collisions by construction; cross-root collisions block **both** plugins with status `id-collision` (no precedence, the user renames one).

**Plugin enable vs import trust (security boundary, two orthogonal axes).** Dropping a folder is not consent to run it. A project-local plugin is discovered (manifest parsed, listed) but its code is imported, by EVERY verb including the `sm plugins` management family, only when it is BOTH **enabled** (operational, shareable, in the config layers, `settings.json` overlaid by `settings.local.json`, written by `sm plugins enable / disable`) AND **trusted** (security, LOCAL per-machine, a per-plugin grant in the scope lock (`.skill-map/scope.lock.json`) written by `sm plugins trust / untrust <id>`, or `sm plugins trust --all` for every discovered drop-in at once). The committed `settings.json` baseline cannot grant import trust, a cloned repo controls its own `settings.json`, and neither does a shipped scope lock, because each grant is derived from the `.skill-map/` directory's filesystem identity and git does not transport it, so cloning-and-scanning never auto-executes the repo's plugins; the runtime emits a one-time "found but not loaded" notice pointing at `sm plugins trust`. Built-ins and explicit `--plugin-dir` are exempt; `--no-plugins` skips discovery. The two not-loaded reasons stay distinct (`disabledByConfig` vs `untrustedNotLoaded`), so an explicit disable never re-reads as untrusted. Normative in [`spec/architecture.md`](./spec/architecture.md) §Locality. **Revised 2026-06-27: enable (operational) split from trust (security), enable moved from the DB to the config layers, `config_plugins` repurposed to the trust store, new `sm plugins trust / untrust` verbs plus a per-plugin UI Trust control. Revised 2026-07-09: the blanket `pluginTrust.projectEnabled` opt-in was removed, trust is per-plugin only (`sm plugins trust --all` covers "trust everything"), so a single config toggle can no longer widen the code-execution surface. Revised 2026-07-28: trust moved out of the DB into the scope lock (audit C1, the DB was force-addable and its "never travels" premise was a default, not a boundary), and the `sm plugins` family stopped being exempt from the gate, it had made `sm plugins list` the shortest clone-and-scan path to a hostile repo's code and `sm plugins trust` run the code it was asking consent for. Revised 2026-07-29: the ENABLE axis became a pre-import gate too, closing the half the trust work left open. Per-extension enable used to be applied AFTER the import, at registration, so an extension the operator had disabled, and one shipping `stability: 'experimental'` that nobody opted into, still had its module body evaluated on every scan: the interface said off while the code ran. It could not be fixed in place, because the decision needs `stability` / `defaultEnabled` and those lived in the module, so answering "may this run?" required running it. The four declarative fields (`version`, `description`, `stability`, `defaultEnabled`) moved out of the module into a per-extension `extension.json` on disk ([`spec/schemas/extensions/extension-manifest.schema.json`](./spec/schemas/extensions/extension-manifest.schema.json)); reading JSON is not execution, so the gate now closes ahead of the import and a module re-declaring any of the four is `invalid-manifest`. Built-ins stay exempt and keep declaring them inline, they never reach the disk loader. Three consequences worth remembering: membership in `IDiscoveredPlugin.extensions` is now the PROOF that an extension was allowed to execute (everything discovered-but-unimported rides a separate `unloadedExtensions`, so nothing unimported is type-compatible with anything runnable); an untrusted plugin's full inventory became listable without executing anything, which is what makes "review before you trust" real and retired the `0 ext` wart; and every layer that resolves an extension id had to learn the enable/disable asymmetry (`pluginCatalogue` on the CLI, `pluginExtensionIds` / `hasExtension` on the BFF), because looking only at the imported set 404s the very request that turns a disabled extension back on. `sm serve` rebuilds its plugin runtime on a `false → true` enable (a disabled extension has no instance to un-filter); disabling needs no rebuild. `sm plugins upgrade` migrates an existing plugin by reading its module as TEXT, never importing it, since importing there would execute exactly the code the gate exists to hold back, on a freshly cloned repo before anything is trusted. Revised 2026-08-01 (audit): the `--plugin-dir` exemption stands but is no longer SILENT. It disables the trust gate, so a hostile project's README saying "inspect our plugins with `sm plugins list --plugin-dir ./tools/sm-plugins`" restored the execute-on-inspect path verbatim, with nothing on screen to say code had run; every `--plugin-dir` load now warns on stderr that module bodies under that directory run. Typing the flag is consent to load the directory, not evidence the operator knows a LISTING evaluates code. The audit's companion suggestion, moving manifest checks ahead of the import, turned out to be already satisfied: everything decidable from `extension.json` alone (path-derived kind / id, the AJV manifest validation, the enable gate) already precedes the `import()`, and the checks that remain after it read the MODULE's own exports (`pluginId`, the re-declared derived keys), which cannot be answered without loading it. The defence there is the trust gate, which is exactly why its bypass was the finding that mattered. Revised 2026-08-03: the enable toggle stopped restating the defaults. Every surface wrote `plugins.<p>.extensions.<e>.enabled` unconditionally, so flipping a default-on extension off and back on left `enabled: true` behind: a committed line saying what the manifest already says, which also PINNED the extension against any future change to its installed default. The writer now asks "is this key redundant?" rather than "does the value equal the default?", the two differ whenever the other layer has an opinion (with `settings.json` on `false`, a `--local` re-enable must persist), and the same pass sweeps the target layer so configs that accumulated redundant keys clean themselves up on the next toggle. An id whose installed default cannot be read (a drop-in discovered but not loaded) is always written, never pruned, and the plugin-level `plugins.<p>.enabled` is left alone: no toggle surface writes it. `core/plugins/enable-persist.ts`, shared by the CLI verb and the three BFF PATCH shapes.**

The folder layout (`<kind>s/<name>/index.js` per extension, `kinds/` for Providers) and the manifest shape (`version` / `specCompat` / `catalogCompat` / `description`, no `id`, no `extensions[]` array) are documented in [`spec/plugin-author-guide.md`](./spec/plugin-author-guide.md). Now that the spec ships `1.x`, manifests pin `specCompat: "^1.0.0"` under standard SemVer. The pre-1.0 regime (minor RESERVED for incompatibility, so manifests pinned a minor range) is retained in [`versioning.md`](./spec/versioning.md) §Pre-1.0 for any track that returns to `0.y.z`.

### Loading and qualified ids

On boot (or `sm plugins list`) the kernel walks the plugin root, validates each manifest (directory name == id, id unique across roots, `specCompat` satisfied), dynamic-imports and schema-validates each extension, and registers it under the **qualified id** `<plugin-id>/<extension-id>` per kind. Qualified ids namespace every cross-extension reference, so two plugins can safely ship the same short id. The seven load statuses (`loaded` / `disabled` / `incompatible-spec` / `incompatible-catalog` / `invalid-manifest` / `load-error` / `id-collision`) and the full load contract are in [`spec/plugin-author-guide.md`](./spec/plugin-author-guide.md) §Discovery and §Diagnostics.

### Provider declares its own kind catalog

Each Provider owns the catalog of kinds it emits and their frontmatter schemas; the spec keeps only the universal `frontmatter/base.schema.json`. Since the structure-as-truth refactor the catalog lives **on disk** at `<plugin>/kinds/<kindName>/{schema.json, kind.json}` (not an inline manifest map, and there is no `defaultRefreshAction`), so a future Cursor Provider would just ship `kinds/mcp-server/`, `kinds/mode/`, etc. See [`spec/plugin-author-guide.md`](./spec/plugin-author-guide.md) §Providers and [`spec/architecture.md`](./spec/architecture.md) §Provider.

**External Provider parity (drop-in lenses).** Building a full clone of a built-in vendor lens as an on-disk drop-in surfaced (and closed) five gaps where the external plugin path lagged the built-in one, since no external Provider had ever exercised the richer surface: (1) `kinds/<name>/kind.json` now accepts the optional `identifiers` / `identifierMismatch` keys, so a drop-in kind is name-resolvable (an `invokes` / reference link resolves to it) like a built-in; (2) the provider manifest schema now accepts the declarative `resolution` and `reservedNames` fields; (3) the loader strips the `activity` capability's runtime-only fields (`pluginHooksSource` / `mapEvent`) from the AJV view, so a drop-in can ship a live-activity adapter; (4) the walk partitions gated lens Providers ahead of the ungated `core/markdown` base so a drop-in lens (appended after the built-ins in registry order) claims its own territory instead of losing every `.md` to the fallback; (5) `sm activity install/status/uninstall` resolve providers off the full composed runtime (built-ins + trusted drop-ins), not the built-in registry only. The spec was already correct on the runtime-only `activity` fields; the other four are spec + kernel additions. A drop-in vendor lens now scans, classifies, links, is selectable as a lens, and installs live activity on equal footing with a built-in.

### Multi-provider rollout (Step 9.7)

Three conventions land together when more than one Provider is active in the same scope:

1. **Declarative `read` instead of hand-rolled `walk()`**. Provider manifests declare `read: { extensions, parser }` (e.g. `{ extensions: ['.md'], parser: 'frontmatter-yaml' }`). The kernel walker owns symlink handling (an in-tree link is always followed with cycle detection, refined on 2026-08-07: the guard is a per-branch ancestor chain plus a hard cap on symlinked-directory entries, replacing the walk-global visited set that silently dropped every sibling link after the first one to reach a shared target, when the spec promised in-tree links are ALWAYS followed; a link whose real target escapes the scan roots is refused by default and followed only when the project-local `scan.followExternalSymlinks` key is set, the realpath-containment gate reinstated on 2026-07-05 for the clone-and-scan threat model, superseding the 2026-07-02 "dereference wherever it points" decision, and extended on 2026-08-01 to the live WATCHER, which had been left dereferencing by chokidar's default so a committed `docs/x -> ~/` armed inotify watches across the operator's home; expressed as the same containment gate rather than chokidar's blunt `followSymlinks: false`, because live updates behind an INTERNAL symlinked directory are the whole reason that backend is selectable; and closed from the other side on 2026-08-13: the two scoped single-file reads, submit-time drift verification and the incremental pass's reread of unchanged nodes, had been running on the gate's DEFAULT instead of the project's `scan.followExternalSymlinks`, so a node scanned through an authorised external link was indexed but not operable, misreported as "file missing", and could even have its content silently blanked by a live re-scan), TOCTOU re-stat, ignore-filter consumption, prototype-pollution strip, and the `js-yaml` JSON_SCHEMA pin so every Provider inherits them by construction. Built-in parsers ship as a closed set inside the kernel (`frontmatter-yaml`, `plain`); user plugins cannot register their own. A Provider that needs non-standard discovery still implements `walk()` directly, it wins over `read` and accepts the duplication of audit defences.

2. **`classify(): string | null`**. With multiple Providers active, every Provider walks every file matching its `read.extensions`. Each Provider claims its own conventions and disclaims the rest by returning `null`. The orchestrator skips disclaimed paths, so the same path is never persisted twice. Concretely (current catalog): Claude claims `.claude/`, `notes/`, `CLAUDE.md`; OpenAI Codex claims `.codex/agents/*.toml` (and routes their TOML envelope through the kernel walker); the neutral `agent-skills` Provider claims `.agents/skills/<n>/SKILL.md` (the same on-disk home Google adopted for Antigravity skills after retiring the vendor-specific `.gemini/` layout); the `core` fallback owns generic `.md`. The `antigravity` Provider claims `.agent/workflows/<n>.md` (singular `.agent`) as its own `workflow` kind, AND reuses the `agent-skills` classifier by manifest composition for skills (so under its lens `.agents/skills/<n>/SKILL.md` classifies as `antigravity`/`skill`); it also contributes lens identity plus its own reserved-name verbs layered on the open-standard base catalog that `agent-skills` owns and every standard adopter inherits. Files outside every Provider's territory are silently ignored. The spec's `provider-ambiguous` issue still fires when two Providers DO claim the same file (e.g. a misconfigured plugin); the disclaim contract prevents the legacy "Claude as catch-all for any markdown" footgun that otherwise produces the conflict by default.

3. **Format-named kinds = fallback only**. Each Provider has one fallback kind named after the file's *format* (`markdown` today; future `toml` for Codex's slash-commands, future `json` for Gemini's extension manifests). The convention: format-named kinds apply only when no specific role matches, a `.toml` file that IS a Codex agent classifies as `agent`, never `toml`. Specific roles (agent / command / skill) prevail over format naming. The Claude fallback was renamed `note` → `markdown` to land this convention.

### Per-Provider node painting (kindRegistry)

When two Providers declare the same kind name (e.g. Claude `agent` and Codex `agent`), the BFF's `kindRegistry` keeps every contribution under `entry.providers[<providerId>]` and points `primaryProviderId` at the first Provider in iteration order. The primary drives the kind's shared CSS var (`--sm-kind-<kind>`) so static stylesheets stay valid; per-node painting picks `entry.providers[node.provider]` to override the accent inline. Result: a Claude-sourced `agent` paints blue, a Codex-sourced `agent` paints with its own palette, on the same graph, without forcing different kind names. The UI exposes `KindRegistryService.providersOf(kind)` for surfaces that need the full per-Provider drill-down (inspector audit panel, future plugin-contributions panel).

The sibling **`providerRegistry`** carries the Provider's OWN identity (the manifest `presentation` block: label, color, optional `colorDark` / `icon` / `emoji` / `hideChip` / `invocationSigil`), distinct from its kinds' visuals. The BFF assembles it at boot the same way (`buildProviderRegistry`) and embeds it on every payload-bearing envelope; the SPA's `ProviderRegistryService` feeds the active-lens dropdown, the topbar lens chip, and the per-node provider chip from it, so adding a Provider never requires a UI edit. `hideChip` (set by the universal `markdown` fallback) suppresses only the per-card badge. `invocationSigil` is the single glyph the lens invokes skills with (`/` for Claude / Antigravity, `$` for Codex); the link-kind palette joins it against the active lens to paint the `invokes` edge-kind glyph (and tooltip) instead of a hardcoded `/`, so the toggle mirrors the lens's real source syntax (lenses with no `/`/`$` channel, `agent-skills` / `markdown`, omit it and never paint the glyph). Auto-detect markers ride on the same manifest (`detect.markers`), so the detectable lens set also derives from the registered Providers rather than a hardcoded table. The lens dropdown additionally greys out Providers the operator has currently disabled (the `selectable` set on `GET /api/active-provider`, resolved live from the per-extension enabled state): a disabled Provider stays listed but cannot be chosen as the lens, matching the scan-time rule that a lens pointing at a disabled Provider runs none of its extractors.

### Extractor persistence channels and scan cache

An Extractor emits through three context channels, in any combination per `extract()` call: `ctx.emitLink(link)` (the `links` table), `ctx.enrichNode(partial)` (the separate enrichment layer, see §Enrichment), and `ctx.store.set(key, value)` (the plugin's own KV namespace). Extractors are **deterministic-only** (they run inside `sm scan`; LLM-driven enrichment is an Action concern, so Extractor manifests carry no `mode`), and a manifest may narrow which kinds it runs on via `precondition.kind` and validate its own writes via an opt-in storage schema. The per-`(node, extractor)` cache (`scan_extractor_runs`) turns the incremental default (`sm scan`; `--changed` is the explicit alias, `--full` opts out) into a one-row reuse on unchanged bodies: a newly registered Extractor runs against cached nodes, an uninstalled one has its links / enrichments cleaned without invalidating the rest. The cache key is three-legged (body hash + sidecar-annotations hash + resolved-settings hash, 2026-08-07): a settings change, including a secret's `envVar`, re-runs the pair on the next scan so the default never serves outputs computed under superseded settings. Full contracts: [`spec/plugin-author-guide.md`](./spec/plugin-author-guide.md) §Extractors and [`spec/architecture.md`](./spec/architecture.md) §Extractor.

### Hook trigger set

Hooks subscribe to a curated set of **nine** lifecycle events: seven pipeline-driven (`scan.started`, `scan.completed`, `extractor.completed`, `analyzer.completed`, `action.completed`, `job.completed`, `job.failed`) plus two CLI-process-driven, `boot` (before the verb routes, the dispatcher awaits hooks so their output lands above the verb, `core/update-check` relies on this) and `shutdown` (after the exit code resolves, never altering it). Everything else (`scan.progress`, `model.delta`, `job.claimed`, ...) is intentionally not hookable. Hooks are **deterministic-only** and may narrow with a declarative `filter`; the shared dispatcher (`src/kernel/extensions/hook-dispatcher.ts`) serves both the orchestrator and the CLI entry, with `core/update-check` (on `boot`) as the first built-in consumer. Full catalog and payloads: [`spec/architecture.md`](./spec/architecture.md) §Hook.

### Plugin storage

Plugins persist state by declaring `storage: { mode: 'kv' }` in the manifest, which gives `ctx.store.{get,set,list,delete}` over the kernel-owned `state_plugin_kvs` table and can opt into write-side schema validation via `storage.schema`. Scoping is structural: the plugin id is captured when the accessor is built and is never an argument, so a plugin has no way to name another plugin's rows. Per-value and per-plugin-per-scan byte ceilings keep one plugin from growing the project database without bound. A plugin never owns tables in that database; data needing relational shape lives outside it, under the author's own control. **Honest note**: drop-in plugins are user-placed code, so row scoping guards accidents, not hostile plugins; signing is a post-v1.0 question. Full contracts: [`spec/plugin-kv-api.md`](./spec/plugin-kv-api.md) and [`spec/db-schema.md`](./spec/db-schema.md).

### Plugin commands

The `sm plugins` family (`list` / `show` / `enable` / `disable` / `doctor` / `create` / `upgrade`), plus `sm conformance run` and `sm check --include-prob`, is specified in [`spec/cli-contract.md`](./spec/cli-contract.md).

### Default plugin pack

The reference implementation bundles built-ins for every kind. The **live
roster is not restated here** (it drifted: this section long claimed a single
`claude` Provider while six ship). Read it from the tool instead, `sm plugins
list`, or from `src/plugins/` in the repo.

Two design points are worth keeping at roadmap altitude, both normative in
[`spec/architecture.md`](./spec/architecture.md):

- **Code regions are mined, not just stripped.** `backtick-path` is the
  deliberate inverse of the code-strip policy, extracting relative `.md` paths
  FROM code spans and fences (the Agent Skills standard's "load referenced
  files on demand" contract) as `points` edges (Decision #127). The trigger
  siblings (`backtick-mention` / `backtick-slash` / `backtick-dollar`,
  Decisions #134 / #135) are the narrower inversion, one per trigger grammar,
  emitting links ONLY when the token resolves to a real entity; the rest is
  dropped by the `prune-unresolved-code-triggers` gate. See
  §Extractor · code-region file references and §Extractor · code-region triggers.
- **Reference resolution is dual-base.** `points` links resolve file-relative
  first and retry against the scan root when the authored token carries no
  explicit `./` / `../` prefix; `references` links stay file-relative for bare
  destinations but honour the GitHub/GitLab root-relative `/docs/foo.md` form.
  See §Provider · resolution rules and §Extractor · markdown links.

Practical guidance for anyone touching a body extractor, including the
hard-won rule that token grammars are widened as a SET, lives in
[`context/runtime-quirks.md`](./context/runtime-quirks.md).

`github-enrichment` remains the firm commitment for the Action lineup (it is
what the hash-verify property needs). Third-party enrichment plugins (Snyk,
Socket) install post-1.0 against the same stable contract.

---

## UI contribution system

### Why this exists

Out of the six plugin kinds, the only first-class UI surface today is the Provider's `kinds[*].ui` block (label, color, icon). Extractors that emit links and analyzers that emit issues ride the canonical kernel-built UI (`linked-nodes-panel`, issues panels). The moment a plugin author wants to surface anything else, a counter, a tag, a per-node breakdown, a tree, a key-value record from parsed frontmatter, there is no path. They cannot ship Angular components from a plugin (correctly, by design); the kernel has no extension surface for "render this data per node".

The annotation-contributions system (Step 9.6.6) covers sidecar root keys but only that. Everything else has been deferred to Decision #293 ("Third-party UI + BFF extensions") as post-v1.0.

This system fills the gap with a deterministic, scoped, built-in-driven model that lands pre-v1.0.

### Two layers (post-2026-05-10 collapse)

| Term | Owner | Definition |
|---|---|---|
| **Slot** | Spec + kernel + UI | A named visual surface in the UI that fixes both the renderer and the payload shape. Closed catalog of 19 slots in `spec/schemas/view-slots.schema.json`. The plugin author picks ONE slot per contribution; that pick is the entire mental model. |
| **Contribution** | Plugin | Per-node typed data emission via `ctx.emitContribution(ref, payload)`, payload conforms to the slot's payload schema. |

Plugin authors pick slots. The kernel + spec publish the catalog and the per-slot AJV payload schemas. There is no separate "contract" abstraction, the slot IS the contract.

**Earlier model (superseded)**: a separate "Contract" layer (11 contract names like `node-counter`, `node-tag`) sat between Slot and Contribution. The plugin author picked a contract and the UI broadcast each contribution to ALL slots compatible with that contract. The 2026-05-10 redesign eliminated the contract layer because it doubled the mental model with no real win for plugin authors and produced surprise duplication when the same data appeared in 4 places automatically. See decision #9 below.

### Catalogs and manifest

Plugin authors pick a `slot` from the closed catalog (19 slots, [`spec/view-slots.md`](./spec/view-slots.md)) and, for settings, an input-type from the closed catalog (11 types, [`spec/input-types.md`](./spec/input-types.md)), writing zero JSON Schema and zero renderer code. View contributions are declared in the extension manifest's `ui` map (alongside `settings` and `annotation`) and emitted per node at scan time via `ctx.emitContribution(ref, payload)`; the manifest carries `catalogCompat` (parallel to `specCompat`), and a catalog mismatch surfaces as `incompatible-catalog`. The per-slot renderers and payload shapes, the prefix-discriminated icon strings, and the UI-side slot configuration (cardinality, deterministic ordering, replacement strategy) live in [`spec/plugin-author-guide.md`](./spec/plugin-author-guide.md) §View contributions, [`spec/view-slots.md`](./spec/view-slots.md), and [`context/view-slots.md`](./context/view-slots.md).

### Author API (emit by reference + dev-time payload types, 2026-06-07)

The author surface was tightened to remove the string-id duplication and to push payload validation up to author time. Three changes, all breaking (minor, pre-1.0):

- **Emit by reference, not by id.** A contribution is declared as a module-level const and listed in `ui` by shorthand (`const facts = { slot: '...' } satisfies IViewContribution; ui: { facts }`); `evaluate` / `extract` emit by passing that const **by reference** (`ctx.emitContribution([nodePath,] facts, payload)`), never a string id. The kernel recovers the contribution id (the `ui` key) by **object identity** against the `ui` map. A ref that is not a declared `ui` value (a spread copy, an inline literal) drops with a loud `extension.error` (`reason: undeclared-contribution-ref`), never silently. This kills the old failure mode where a typo'd string id dropped at runtime with no compile-time signal. (The id is still the `ui` key, kebab-cased per the manifest schema, not the const's variable name.)
- **Dev-time payload types.** `emitContribution` is generic: `emitContribution<C extends IViewContribution>([nodePath,] ref: C, payload: SlotPayload<C['slot']>)`. `SlotPayload<S>` + the per-slot payload interfaces are **generated from `view-slots.schema.json#/$defs/payloads`** by `json-schema-to-typescript` (wired into `scripts/generate-view-catalog.js`, drift-guarded by `view-catalog:check`). A TS author who writes `satisfies IViewContribution` gets a compile-time error on a wrong-shape payload. The generics are a dev-time layer **on top of**, not a replacement for, the runtime AJV check, which stays the authority across the plugin trust boundary (JS plugins, `as any`, and hostile code all bypass the types). The repo's fixture plugins moved to `.ts` (typecheck wired into `validate:compile`) to dogfood the net.
- **Self-documenting list-payload fields.** The three array payloads were renamed off the overloaded `entries`: breakdown `bars`, key-values `pairs`, link-list `links` (records `columns`/`rows` and tree `children` unchanged).

The runtime off-shape is now **visible** post-scan (shipped as the follow-up). Rejected contributions (undeclared ref, or a payload that fails the slot's AJV schema) are persisted per scan to a replace-all `scan_contribution_errors` table and surfaced two ways: `sm plugins doctor` reads the last scan's errors, prints a "Runtime contribution errors (last scan)" section grouped by plugin, and promotes its exit code to 1 when any exist; the Settings plugin panel shows a per-plugin warning badge + a collapsible diagnostics list, fed by an embedded `runtimeContributionErrors[]` field on `GET /api/plugins`. The `extension.error` event still fires on the scan stream; the persisted table is the post-scan surface for the load-static doctor and the no-live-scan UI.

### Persistence, BFF, isolation

Emissions persist to a `scan_contributions` table in the `scan_*` family. The persist is **not** a pure replace-all: because the watcher's cached pass skips `extract()` (no `emitContribution` fires) for unchanged nodes, a naive wipe-all would drop valid rows, so the kernel runs orphan-sweep + catalog-sweep + upsert passes that leave cached rows untouched (full sweep contract in [`spec/db-schema.md`](./spec/db-schema.md) §`scan_contributions`). The BFF serves the runtime catalog and per-node contributions (embedded on `/api/nodes`, `/api/nodes/:pathB64`, `/api/scan`, capped by `bff.maxBulkContributions`), enforcing `pluginId` ↔ namespace at the route level (see [`spec/architecture.md`](./spec/architecture.md) §View contribution system). Isolation rests on six rules (typed data only, no plugin CSS/DOM, BFF-namespaced reads, typed verb dispatches, AJV at load/emit/response, no `[innerHTML]` / dangerous-attr binding); **honest note**: isolated against accidents, not hostile code, until a worker-thread / iframe sandbox post-v1.0.

### Scaffolder, built-ins, soft-warnings

`sm plugins create <kind> <plugin-id>` is the canonical entry point (it scaffolds a loader-clean stub for any of the six extension kinds, provider / extractor / analyzer / action / formatter / hook, the kind is the required first positional; the closed slot / input-type catalog is browsable via `sm plugins slots list`, not walked interactively, and the kernel + CLI mirrors of that catalog are generated from the spec by `scripts/generate-view-catalog.js` with a `view-catalog:check` drift guard wired into `validate:compile`); `sm plugins upgrade <id>` runs catalog migrations (console error + UI dialog + non-zero exit when auto-migration is impossible) and `sm plugins doctor` reports `incompatible-catalog` + deprecated-slot usage. Of the two built-in soft-warning analyzers that shipped with the system, neither survives as a scan rule: `core/unknown-slot` (a loaded plugin references a slot outside the current catalog) moved into `sm plugins doctor`, and `core/contribution-orphan` (a `scan_contributions` row pointing at a vanished node) was DELETED on 2026-07-22 at the analyzer-review-pass closure: it had always been a `return []` stub waiting for a `contributionsRows` context field that never landed, and a listed rule that cannot fire is noise; if incremental-scan rename misses surface post-release, rebuild it together with the field it needs (`IAnalyzerContext.viewContributions` stays as the generic catalog surface). At landing there are a handful of built-in adopters, notably `core/link-counts` (the `linksIn` / `linksOut` footer pair) and `core/stability` (the experimental / deprecated chips); the rest have no clear UI surface and migrate in a later coverage sprint.

### Inspection verbs (`list` / `show`)

The two read-only inspection verbs split by altitude so each datum lives in exactly one place. `sm plugins list` is the index, one row per plugin (glyph, extension count, source) and nothing else; `sm plugins list <id>` drills into one plugin (manifest fields plus a per-extension block with kind / version / per-extension toggle glyph); `sm plugins show <plugin>/<ext>` renders a single extension's detail (Kind / Version / Stability / Description / Preconditions / Entry). Because the index carries no extension names and `show` is extension-only, the two off-altitude shapes are rejected with a directed redirect to the sibling verb: a bare `show <plugin>` points at `list <id>`, a qualified `list <plugin>/<ext>` points at `show`. The qualified-id parse + validation is shared with `enable` / `disable` (one helper in `cli/commands/plugins/shared.ts`). Pre-1.0 breaking change shipped as a minor bump in `@skill-map/spec` and `@skill-map/cli`.

### Decisions

| # | Decision | Resolution |
|---|---|---|
| 1 | Slot config ownership | ~~UI-only. Kernel/BFF stays slot-blind.~~ **Superseded 2026-05-10 (decision #9)**: slots are now spec-level. The UI may rearrange visual surfaces beneath a slot id without renaming, but the slot id itself is normative across kernel + BFF + UI. |
| 2 | Per-node payload shape | Always object envelope (`{ value, ... }`). |
| 3 | Multi-instance per extension | `Record<string, IViewContribution>` (parallel to annotations). |
| 4 | Settings change propagation | Rescan-required. UI surfaces "rescan needed" badge. |
| 5 | Catalog version compat | Semver `catalogCompat` field, parallel to `specCompat`. |
| 6 | Bulk endpoint cap | 200 nodes hard, override via `bff.maxBulkContributions`. |
| 7 | Migration UX on incompatibility | Console + dialog + exit ≠ 0. |
| 8 | Built-in adopter list at landing | Two at landing (`core/annotations`, `core/external-url-counter`); `core/annotations` later dropped as misadopter (kernel data, not plugin-derived). Post-2026-05-10: `core/at-directive` and `core/link-counts` joined as adopters of the new slot model. |
| 9 | **Contract layer eliminated (2026-05-10)** | The intermediate "contract" abstraction (11 named contracts the plugin author picked, with the UI broadcasting to all compatible slots) was removed. Plugin authors now pick `slot` directly from a closed catalog of 14 slots; each slot fixes a single renderer + a single payload shape. The polymorphic slots `inspector.body.panel`, `card.footer.left`, `inspector.header.badge` were split into per-shape sub-slots via dotted suffix (e.g. `inspector.body.panel.records`, `card.footer.left.tag`). Trade-off: lost automatic multi-slot broadcast (an author who wants the same data in two surfaces declares two contributions, one per slot); gained a smaller mental model (one catalog instead of two), no surprise duplication, and slot ids that map 1:1 to a payload shape. Slot vocabulary is now part of the public contract, a UI rename is a catalog-major bump. Pre-1.0 breaking change shipped as a minor bump in `@skill-map/spec` and `@skill-map/cli`. |

### Known limitations carried forward

- Catalog evolution treadmill, every new slot adds spec doc + AJV schema + UI renderer wiring + scaffolder support + tests + conformance fixtures.
- Cross-slot orchestration undefined, two contributions sharing underlying state can drift; no kernel arbitration today.
- Probabilistic plugins not modeled, deferred until deterministic model has bedded in.
- Multi-surface broadcast now requires N declarations, by design (decision #9). If a plugin author keeps the values in sync across declarations, they cannot accidentally desync; if they don't, the UI shows the same `mentions` chip with different counts in different places. Post-v1.0 we may revisit a "broadcast group" concept if real-world plugins hit this often.

### Replaces Decision #293

Decision #293 ("Third-party UI + BFF extensions", post-v1.0) is **superseded** by this section for the deterministic case. The probabilistic / sandboxed-iframe case for fully arbitrary third-party UI remains deferred to post-v1.0 per the original decision.

### Slot debug overlay (kept dev tool)

Seeing every slot lit up on the page, even when empty, is how the slot map stays
debuggable. The overlay (`?debug=1`, or `localStorage` `sm-debug-slots`) paints a
ring + label around every slot host and lists the contributions dispatched
there. It is a **deliberate, gated dev tool, not a hack to clean up**: it never
renders for production users, and removing it is not a valid cleanup. Mechanics,
the sibling perf HUD, and the `DebugSurface` directive for the dedicated
`inspector.surface.*` slots live in [`context/ui.md`](./context/ui.md) §Debug
overlays.

---

## Summarizer pattern

ONE universal summarizer Action (`core/ai-summarizer-action`, shipped at Step 10) covers every node kind: `markdown` names the body format it reads (all node bodies are markdown prose, including frontmatter-field bodies like codex TOML `developer_instructions`), not a kind gate, so it carries no `precondition` and `--all` fans out to every non-virtual node. The per-kind summarizer roster (`skill` / `agent` / `command` / `hook`) and their `summaries/<kind>.schema.json` shapes were dropped on 2026-07-13: the per-kind shapes were speculative, and the summary the map needs (subject matter, topics, key facts) is kind-agnostic. [`spec/schemas/summaries/markdown.schema.json`](./spec/schemas/summaries/markdown.schema.json) is the single canonical node-summary shape; it extends `report-base.schema.json`, the universal probabilistic-report envelope: a `confidence` (0.0-1.0, the model's metacognition) plus a `safety` block (`injectionDetected`, `injectionType` enum, `contentQuality` enum). A summarizer opts in through its own `report.schema.json`, which extends the canonical schema via `$ref` (the `summaries/` namespace is the signal `sm record` detects; there is no manifest flag), so a plugin MAY ship its own summarizer, specializing on the PROMPT side rather than the schema side. Summaries persist in the kernel-owned `state_summaries` table keyed by `(node_id, summarizer_action_id)` (the `kind` column mirrors the node's own kind) with the body hash at generation, so `sm show <node>` renders the summary and marks it `(stale)` when the body changed. Between 2026-07-18 and 2026-07-21 the summarizer shipped `stability: 'experimental'` / disabled (its only reader was `sm show`); it GRADUATED back to stable / enabled on 2026-07-21 when its UI surface landed, not as the originally sketched summary card but as the inspector HEADER's semantic-analysis affordance (user shape): a sparkles button beside the node title (excluded from the AI-actions launcher row and the ALL button) that queues the summarizer, shows the usual queue states, and once the judgment lands auto-expands an analysis block under the identity strip (sparkles icon + whatItCovers subject, key facts, quality notes, confidence, per-row stale mark + re-run; the report's `topics` and `relatedNodes` lists are deliberately NOT rendered, user call). Backed by `GET /api/nodes/:pathB64/summary` (direct shape, envelope-exempt), silently re-fetched on job frames / scan completions. Follow-ups same day: the block gained a delete X (`DELETE /api/nodes/:pathB64/summary[?summarizer=]`, hard row delete, no consent, logs `summaries.delete`; the header falls back to its idle invitation), the prompt now mandates matching the FILE's content language (Spanish body -> Spanish summary), and the where-does-it-live question sealed the project-wide **storage rule** (spec/architecture.md §Storage rule, AGENTS.md invariant): machine output -> DB, human curation -> `.sm` companion; summaries are machine output, so they stay in `state_summaries`. A node is refreshed either deterministically (`sm scan -n <id>`, recomputes bytes / hashes / links) or probabilistically (queue the summarizer as a job). **Direction (2026-07-13, user decision)**: the current multi-field report (subject + `topics` + `keyFacts` + `relatedNodes` + `qualityNotes`) bundles too many responsibilities for ONE extension and is transitional; it will split into focused extensions (summary, taxonomy, fact extraction, relation inference, quality review) in a later step. The architecture already accommodates the split with zero schema work (`state_summaries` is keyed per action, the `summaries/` detection convention is open); the real cost lands on the consumers (Step 11 finders and readers, Step 16 cards pick which rows to read), so the cut should be planned alongside those steps.

The TAXONOMY leg of that split shipped on 2026-07-21 as **`core/ai-tagger-action`** (stable, enabled; user request "auto-tag"): a universal probabilistic Action (no `precondition`, `--all` fans out like the summarizer) that infers 2-6 lowercase kebab-case topical tags in the FILE's content language and returns them in its report only, the agent never touches the `.sm`. Detection mirrors the summarizer convention exactly: a report schema `$ref`ing the canonical [`spec/schemas/tags/markdown.schema.json`](./spec/schemas/tags/markdown.schema.json) (the `tags/` namespace, `isTagsReportSchema` in `kernel/jobs/tags-schema.ts`) marks the Action as a tagger, and `sm record` applies the report's `tags[]` to the node's sidecar `annotations.tags` AFTER the transaction (spec/job-lifecycle.md §Tags write-through): union merge (existing entries keep order and casing, case-insensitive dedup), identity backfill from the live scan node for brand-new sidecars, annotations-mirror refresh, and the STANDING `.sm` consent only (`allowEditSmFiles`; without it the record still succeeds, a human advisory says the tags stayed in the report). Tags land in the CURATION store as the storage rule's documented delegated-curation carve-out (spec/architecture.md §Storage rule): the operator launched the run, and afterwards the tags are ordinary human-editable annotations. UI: a sparkles button on the inspector's TAG ROW (idle / queued / running; no `ready` state, the merged chips ARE the result), excluded from the AI-actions launcher row and the ALL button like the summarizer.

---

## Frontmatter standard

Skill-map AGGREGATES vendor specs, it does not curate them. The base schema declares only what every node, on every Provider, MUST carry to participate in the graph. Vendor-specific fields (Anthropic Claude Code, Cursor, Continue, …) live in the Provider that emits the kind. A Provider's per-kind schema is a verbatim mirror of the vendor's documented frontmatter, skill-map does not pick a subset, does not rename fields, does not re-shape values. When the vendor evolves their schema, the Provider's mirror evolves with it; drift detection vs upstream docs is a deferred follow-up.

Cross-vendor research (Cursor, Continue, Aider, Copilot, Windsurf, Cline, Roo, Anthropic Claude Code, 2026-05) confirmed `description` is the only field universal across the indexable ecosystems; `name` is universal among formats with explicit identifiers (some vendors use the filename as identity, not a frontmatter field). All other fields, `tools`, `model`, `globs`, etc., are vendor idiosyncrasy.

Spec artifact: `spec/schemas/frontmatter/base.schema.json`. Per-kind schemas ship with the Provider that declares each kind, the Claude Provider declares `skill` / `agent` / `command` / `markdown`, ships the corresponding `*.schema.json` files under its own `schemas/` folder, and references them via the `kinds` map in its manifest. The OpenAI Codex Provider declares `agent` (consuming the TOML envelope under `.codex/agents/*.toml`, with the `developer_instructions` field fed through the body extractors via the declarative `read.bodyField` knob) and inherits the open-standard `skill` kind from `agent-skills` by manifest composition (classifying `.agents/skills/<n>/SKILL.md`, the open layout Codex actually reads), expressing the mixed TOML+Markdown surface via a multi-rule `read` array; the neutral `agent-skills` Provider declares `skill` only, claiming the open-standard `.agents/skills/<n>/SKILL.md` path that Antigravity also adopted after replacing Gemini CLI; the Antigravity Provider declares its own `workflow` kind (`.agent/workflows/<n>.md`, singular `.agent`, shipping a `workflow.schema.json` under its own `schemas/` folder) AND reuses the open-standard `skill` kind + classifier by manifest composition. The retired Gemini Provider used to declare `agent` / `skill` / `markdown`; its plugin was removed in 2026-05 and its on-disk paths route through `agent-skills` (skills) and the `core/markdown` fallback (`AGENTS.md`). A different Provider (Cursor, Cline, custom runner) brings its own kind catalog and its own schemas; the kernel does not opine on the kind list.

### Base (universal, lives in spec)

**Two fields, both defined, neither required at base level**:

- `name`, short human-readable identifier (`string`, `minLength: 1`).
- `description`, one-to-three-sentence description (`string`, `minLength: 1`).

The base DEFINES these two cross-vendor fields (so a present value is validated as non-empty) but deliberately does NOT mark them `required`: whether either is mandatory is a per-kind decision, not a universal one. A kind whose vendor mandates the fields adds `required` on its own per-kind extension (Claude `agent`, OpenAI Codex `agent`, the Agent Skills `skill`); the generic `markdown` fallback and Claude `skill` / `command` leave them optional, no normative Markdown standard mandates frontmatter fields, and Anthropic's merged skill/command contract defaults `name` to the directory/file name and `description` to the first paragraph. This refines Decision #124 (which originally placed `required: [name, description]` on the universal base); the requirement now lives per-kind, so the per-kind schema is the single source of truth and the kernel enforces it at scan time via `frontmatter-invalid`.

The base declares `additionalProperties: true` so vendor-specific fields and skill-map annotation fields flow through validation silently, formal validation of those happens in the per-kind extension (vendor fields) or in a future skill-map annotation schema (annotation fields, see §Skill-map annotation fields below).

This is intentionally minimal. Earlier versions of the base carried a richer field set (`type`, `author`, `authors`, `license`, `tools`, `allowedTools`, `metadata.{version, stability, supersedes, …}`); Step 9.5 (2026-05) trimmed it after the cross-vendor research showed those fields were either Claude-specific (`tools`, `allowedTools`) or skill-map-invented (`metadata.*`), neither is universal, neither belongs in the universal base. Decision #55 (which justified `tools`/`allowedTools` at base "to mirror Claude Code's frontmatter shape") is superseded by the absorb-verbatim principle.

### Kind-specific schemas

Each Provider mirrors its vendor's documented frontmatter **verbatim** (no subsetting, renaming, or reshaping) in per-kind schema files that extend `base.schema.json` via `allOf` + `$ref`, all `additionalProperties: true` so vendor additions never break consumers. The Claude Provider's `agent` / `skill` / `command` / `markdown` schemas (and the shared auxiliary `skill-base`, referenced by `$ref`) live under its own `schemas/` folder and track Anthropic's docs; the `hook` kind was dropped in Step 9.5 (Anthropic hooks live in `settings.json` or as frontmatter sub-objects, never standalone markdown, so `.claude/hooks/*.md` now classify as the `markdown` fallback). Format-named kinds (`markdown`, future `toml`) apply only as the generic fallback, a `.toml` that IS a Codex agent classifies as `agent`. A future Cursor / Cline / custom Provider ships its own kinds and schemas; the kernel does not opine on the list. The three-tier validation model (permissive `additionalProperties` → an always-on `unknown-field` warning → `--strict` / `scan.strict` promoting warnings to CI-failing errors, no "schema-extender" kind) and the `scan_nodes` denormalization (`stability` / `version` / `author` columns, sourced from the sidecar's `annotations` block) are documented in [`spec/plugin-author-guide.md`](./spec/plugin-author-guide.md) and [`spec/db-schema.md`](./spec/db-schema.md).

**Scan-time frontmatter diagnostic lanes (2026-07)**: the kernel stamps a closed three-id vocabulary during the walk (`frontmatter-parse-error`; `frontmatter-malformed` with a five-hint set `paste-with-indent` / `byte-order-mark` / `leading-blank-line` / `missing-close` / `early-close`; `frontmatter-invalid`), routing each node through exactly ONE lane so a single authoring defect never stacks diagnostics: a parse error short-circuits everything (validating the `{}` fallback would report present fields as missing; the content-free declared block is the deliberate exception, it is YAML-nothing, not an error); a declared block runs the early-close detector (a stray `---` inside the block, verdict names the leaked schema keys and suppresses the misleading AJV report) then per-kind AJV, with a declared-but-empty block validating like any other via the parser's `frontmatterDeclared` flag; no block runs the malformed-fence heuristics, each gated on a YAML-looking key line to spare horizontal rules; and a fully ABSENT block still validates against the kind's schema, so kinds with required fields flag missing frontmatter instead of staying silent while all-optional kinds stay quiet. Landed 2026-07 across three CLI changesets (silent-loss gaps, absent-block validation, early-close + BOM-blank combo); normative in [`spec/architecture.md`](./spec/architecture.md) §Kernel check · frontmatter diagnostics.

### Skill-map annotation fields, co-located sidecars

Skill-map's own annotation layer (lifecycle, supersession, provenance, taxonomy, docs) lives in **co-located YAML `.sm` sidecars** next to each node, leaving the vendor file untouched (Decision #125; full rationale in `memory/project_annotation_architecture.md`). The sidecar shape (reserved `for` / `annotations` / `settings` / `audit` blocks plus opt-in `<plugin-id>:` namespaces), the curated 10-field annotations catalog (versioning / supersession / provenance / taxonomy / docs, all optional), identity + drift detection (a `for.bodyHash` / `for.frontmatterHash` mismatch emits the advisory `annotation-stale` signal (info severity, never blocking), drift derived never stored), and the deterministic bump model (`sm bump <node>` / `--pending [--staged]`, the optional `pre-commit-bump` hook, the `SidecarStore` write port; watch never auto-bumps) are normative in [`spec/architecture.md`](./spec/architecture.md) §Annotation system and the `sidecar` / `annotations` schemas. Tags are a skill-map concept (no vendor carries them), so they live here, not in vendor frontmatter; `references` edges come from `core/markdown-link` and `points` edges from `core/backtick-path` (relative `.md` paths inside code spans / fences, the dominant reference shape in agent-authored skills, Decision #127), not sidecar keys. Migration is greenfield (no port of pre-9.6 `metadata: {}` blocks).

---

## Enrichment

Two enrichment models coexist. **Model A, provenance enrichment** (GitHub today, more registries post-v1.0): a remote-fetch Action backed by `state_enrichments`, concerned with verification and idempotency, not interpretation. **Execution decided 2026-07-13**: NOT via the queue (post pull-only, the queue carries prompts for agents; a deterministic fetch has nothing an agent could claim) but via `sm enrich`, in-process (`runner: in-process`), under the new declared-IO carve-out (`io: ['network']` on the Action manifest injects `ctx.fetch`; the committed `allowNetworkActions` policy, default false, gates execution so a cloned repo cannot make skill-map fetch anything without operator opt-in). The write-through mirrors the summaries convention: a report schema extending `enrichments/<x>.schema.json` lands in `state_enrichments` keyed by the action's qualified id. Ships as the built-in plugin `github` (extension `github/enrichment`), `stability: experimental` so it starts disabled; optional `token` secret setting, SHA-pinned fetches need no API at all. It reconciles the local `body_hash` against the canonical source via a SHA pin (an immutable raw URL when `sourceVersion` is a full commit SHA) or tag/branch resolution (query the API, store `resolvedSha`, re-fetch only on change); `verified: true` means local matches remote. **Model B, plugin-driven node enrichment**: any Extractor calls `ctx.enrichNode(partial)` and the kernel persists it in `node_enrichments` (one row per `(node, extractor)`), **never** overwriting the author's immutable frontmatter, consumers read a merged view. Extractors are deterministic-only, so their rows regenerate via the per-Extractor scan cache and never need stale flags; the `stale` / `is_probabilistic` / `body_hash_at_enrichment` columns are reserved inert for a future Action-issued probabilistic enrichment revision (LLM jobs writing back through `ctx.enrichNode`, where a stale row is flagged not deleted so paid output survives a body change). Data that does not fit the canonical Node shape uses `ctx.store.write` (the plugin's own table) instead. Refresh via `sm enrich <node>` / `sm enrich --stale`; there is deliberately no `sm scan --refresh-stale` (a probabilistic refresh never runs inside a deterministic scan). Table and column contracts: [`spec/db-schema.md`](./spec/db-schema.md) and [`spec/architecture.md`](./spec/architecture.md) §Extractor · enrichment layer.

---

## Reference counts

`scan_nodes` carries three denormalized integer columns computed at scan time, `links_out_count`, `links_in_count`, and `external_refs_count` (distinct http/https URLs in the body, normalized). They surface in `sm show` ("N in · M out · K external") and `sm list --sort-by external-refs`. There is no URL-list table (the user cares about the count, not identity) and no liveness check pre-v1.0. Column contract: [`spec/db-schema.md`](./spec/db-schema.md).

Alongside the counts, `scan_nodes.modified_at_ms` records each file's on-disk modification time (`mtime`, Unix ms), captured for free from the walker's existing TOCTOU `lstat` (no extra syscall) and exposed on the node wire shape as `modifiedAtMs` (nullable, virtual / derived nodes have no backing file). It backs the files-view "Modified" column (ISO short date in the cell, full date+time on hover) which sorts by the raw timestamp and sinks fileless nodes to the bottom. It is pure file metadata: it never participates in `body_hash` / `frontmatter_hash`, so touching a file without changing content does not flag drift.

---

## Trigger normalization

Extractors that emit invocation-style links populate a `link.trigger` block with `originalTrigger` (exact source text, for display) and `normalizedTrigger` (the equality / resolution key the post-walk resolver reads; the same normalization applied to `frontmatter.name` backs the `name-collision` analyzer); both are always present together, never mutated independently. The normalization pipeline (NFD, strip diacritics, lowercase, unify `-` / `_` / whitespace to a single space, collapse, trim, while **preserving** syntax characters like `/` `@` `:` so `/skill-map:explore` stays comparable), its worked examples, and its stability contract are normative in [`spec/architecture.md`](./spec/architecture.md) §Extractor · trigger normalization and [`spec/schemas/link.schema.json`](./spec/schemas/link.schema.json) (Decision #21). Stripping a sigil such as the leading `/` is the Extractor's job, applied before emitting, not the normalizer's.

---

## Configuration

`.skill-map/settings.json` is the canonical config for both the CLI and the bundled UI (the filename + `.local.json` partner mirror Claude Code). The loader deep-merges four layers, low → high precedence: **defaults** (compiled into the bundle) → **project** (`<cwd>/.skill-map/settings.json`, committed) → **project-local** (`settings.local.json`, gitignored, the only home for `PROJECT_LOCAL_ONLY_KEYS` like `allowEditSmFiles` / `scan.referencePaths` / `tutorialReminderStep`) → **env / flags**. There is **no user / global layer** (skill-map never reads `~/.skill-map/settings*.json`; per-machine preferences live in project-local). A malformed key warns and is skipped (the app never crashes on bad config); `--strict` makes it fatal. The layered loader is normative in [`spec/architecture.md`](./spec/architecture.md) §Config layering; the `sm ui --config <path>` escape hatch (Step 15) replaces the project + project-local layers with a single file.

### Runtime delivery to the UI

The bundled UI is a static artifact, it does not read files from disk. The CLI sub-command `sm ui` (Step 15) loads + merges + validates the hierarchy and serves the resulting object as `GET /config.json` over the same HTTP server that hosts the UI bundle. The UI fetches that URL once on boot (via `APP_INITIALIZER`), then reads the data through a signal-backed `RuntimeConfigService`. When the bundle is served by a third party (nginx, S3, Caddy), the operator places a `config.json` next to `index.html`; same contract from the UI's side.

This is the only path by which UI-side keys reach the browser. There is no build-time UI config and no `fileReplacements`. Changing UI settings means editing one of the four files in the hierarchy (or the `--config` override) and restarting the server, see §Step 15 for why hot reload is deferred.

### Commands and keys

The `sm config` verbs (`list` / `get` / `set` / `reset` / `show --source`, where `set` targets project-local for `PROJECT_LOCAL_ONLY_KEYS`) are specified in [`spec/cli-contract.md`](./spec/cli-contract.md), and every key with its default is normative in [`spec/schemas/project-config.schema.json`](./spec/schemas/project-config.schema.json): `schemaVersion`, `tokenizer`, `roots` / `ignore`, `plugins.<id>.enabled`, the `scan.*` block (`tokenize` / `strict` / `maxFileSizeBytes` / `respectGitignore`), and the `jobs.*` block (the TTL formula `max(base × graceMultiplier, minimumTtlSeconds)` plus `perActionTtl` / `perActionPriority` / `retention`). The generated `.skillmapignore` is editable directly and through the Settings → Project CRUD (BFF route `/api/project-ignore`, comments preserved). The scan and live watcher build their ignore filter by layering bundled defaults → the project `.gitignore` → `config.ignore` → `.skillmapignore` (later layers `!`-re-include); the `.gitignore` layer is opt-in through the committed `scan.respectGitignore` key (default `false`, so a fresh project does not read `.gitignore` and a git-ignored note still shows on the map unless another layer excludes it), a team-shared toggle surfaced at the end of Settings → Project. The live watcher additionally holds OS watches only on the file types a scan opens (the registered providers' `read.extensions`, e.g. `.md` / `.toml`, plus `.sm` sidecars; a provider with a custom `walk()` disables that gate since its file set is not statically known), while `.skillmapignore` / `.gitignore` / `settings.json` edits are picked up by a dedicated meta-watcher that rebuilds the filter live. Normative in [`spec/cli-contract.md`](./spec/cli-contract.md) and [`spec/architecture.md`](./spec/architecture.md).

### UI-side keys

UI-only keys (declared in `ui/src/models/settings.ts`) cohabit the same file and reach the browser through the runtime-delivery path above: `graph.perf.cache` (Foblex `[fCache]` geometry caching) and `graph.perf.virtualization` (`*fVirtualFor`, worth enabling above ~300 visible nodes). Each side ignores keys it does not recognise (graceful forward-compat). The planned `spec/runtime-settings.schema.json` never landed and is not scheduled: Step 15 closed without it, and the debug overlays are driven by their own URL / localStorage gates rather than a settings key (see §Slot debug overlay).

---

## CLI surface

Shared flags (inherited by every verb): `--json`, `-v`/`-q`, `--no-color`, `-h`/`--help`, `--db <path>`, with `SKILL_MAP_*` env equivalents and `flag > env > config > default` precedence. There is no `-g/--global` flag (scope is always project-local). `--all` is documented only on verbs with meaningful fan-out (`sm jobs submit/run/cancel`, `sm plugins enable/disable`). The normative **exit codes** (`0` success, `1` success-with-issues, `2` operational error, `3` duplicate job, `4` nonce mismatch, `5` not found, `6–15` reserved, `≥16` per-verb) and the normative **elapsed-time** reporting grammar (`done in <N>ms | <N.N>s | <M>m <S>s` on stderr plus an `elapsedMs` field in object `--json` payloads) are specified in [`spec/cli-contract.md`](./spec/cli-contract.md).

**Output sanitization** (Decision #147): every plugin-, DB- or filesystem-sourced string is stripped of control and ANSI bytes before it reaches a human render path, so a hostile clone cannot repaint the operator's terminal through a stored row. The exemption is narrow and deliberate: a stdout stream that IS the artifact keeps byte fidelity, which today means `--json` bodies and `sm graph` formatter output. Surfaces that print stored content to a human still sanitize (`sm jobs preview`, the `sm plugins show` prompt render); their byte-exact counterparts are the `--json` / `sm jobs claim --json` paths. Full doctrine in [`context/cli-output-style.md`](./context/cli-output-style.md) §Sanitisation.

### Verb families

The verb surface is specified in [`spec/cli-contract.md`](./spec/cli-contract.md); each verb's flags and JSON shape live there and in `sm help <verb>`. The families:

- **Setup & state**: `init` (bootstrap `.skill-map/` + first scan), `tutorial` (materialize the single tester walkthrough skill, a "book" of parts whose advanced parts, plugins/settings/view-slots, the CLI in depth, are reached from its in-skill menu), `example` (drop a ready-to-explore example project into an empty cwd, the same wired harness the public demo renders, sourced from the single canonical `fixtures/demo/` fixture, so a new user can `sm scan` then `sm serve` against a real graph without authoring files first), `version`, `doctor`, `help`. Bare `sm` in an empty folder surfaces `tutorial` and `example` as an interactive getting-started menu; in a non-empty folder with no project it offers to run `init` and, on accept, continues into `serve`; the no-project hint still shows when the offer is declined or stdin is non-interactive. See [`spec/cli-contract.md`](./spec/cli-contract.md) §Binary.
- **Scan**: `scan` (incremental by default since 2026-08-07: a persisted prior reuses unchanged nodes; `--full` forces a complete re-extraction, `--changed` stays as the explicit alias, `-n`/`--dry-run` previews in memory). An unchanged node keeps its prior `(provider, kind)` pairing (2026-08-09): the mtime fast path has no parsed frontmatter to classify with, so binding it to the first iterating provider relabelled every unchanged node with the active lens and, on the next re-extraction, invented a `frontmatter-invalid: no-schema`. Same date, the lens itself became a cache input: each scan records its lens in `scan_meta.active_provider` and the next one rebuilds every node when it differs, so a lens changed OUT OF BAND (hand-edited or pulled `settings.json`) can no longer leave stale kinds behind. The `scan_*` zone drop that `sm config set activeProvider` and the BFF route already perform stays as the mutation-site defence; the recorded lens is the consumer-side one, sibling of the `tokenizer` check. Also `scan compare-with <dump>` (delta), `watch` / `scan --watch` (live loop). There is deliberately no single-node scan (graph-wide analyzers need the whole corpus; see `spec/cli-contract.md` §Scan).
- **Browse**: `list`, `show`, `check`, `findings`, `graph`, `export`, and `orphans` (with `orphans reconcile` / `orphans undo-rename` for rename recovery).
- **Actions**: `actions list` / `actions show`.
- **Record**: `record --id <id> --nonce <n> --status completed|failed ...`, the job callback.
- **History**: `history`, `history stats`.
- **Config**, **Jobs**, **Plugins**, **Database**, **Server** (`serve`): see [Configuration](#configuration), [Job system](#job-system), [Plugin system](#plugin-system), [Persistence](#persistence), and below.

### Semantic capabilities are extensions, not verbs (Step 11, Decision #137)

The Decision #49 verb set (`sm what`, `sm dedupe`, `sm cluster-triggers`, `sm impact-of`, `sm recommend-optimization`) is DROPPED. Those verbs presupposed a runner ("submit one probabilistic job and render the result"); the pull-only decision removed the runner, so a verb can neither wait for nor render a probabilistic result, it can only read cache or enqueue, and both halves already exist generically (`sm show` renders summaries with the stale marker; `sm jobs submit` enqueues any probabilistic extension). Each capability ships as an extension instead: describe = `core/ai-summarizer-action` (shipped); consistency / redundancy / quality judgments = per-node finder Analyzers (`mode: 'probabilistic'`, findings via the canonical `findings/report.schema.json` envelope); refactor suggestions = the `skill-optimizer` Action queued generically; dedupe / trigger-clustering were finally DROPPED with the whole graph-scoped second wave (user call 2026-07-23, see the Discarded list in context/roadmap-history.md). The single surviving verb is **`sm findings`**, a generic reader of the kernel-owned `state_findings` table (the probabilistic sibling of `sm check`), plus the kernel-derived safety findings (`injection-detected` / `content-suspicious` / `content-malformed`) synthesized from any probabilistic report's `safety` block. Fixers are Actions joined via Modelo B (`precondition.analyzerIds`); a prose fixer is a queued job whose rendered prompt instructs the processing agent to edit the file with its own tools, skill-map never writes node bodies.

### Server and introspection

`sm serve [--port N] [--host ...] [--no-open]` runs the Hono + WebSocket server for the Web UI. `sm help --format json|md` is the self-describing surface dump consumed by the docs generator, shell completion, UI form generation, IDE extensions, and the `sm-cli` skill (the `md` form is generated on demand, not committed).

---

## Skills catalog

Single source of truth for every skill-shaped artifact shipped alongside `skill-map`. All use the `/skill-map:` namespace inside host agents (Claude Code today; future hosts register under the same namespace).

| Id | Type | Host | Ships at | Purpose |
|---|---|---|---|---|
| ~~`/skill-map:explore`~~ | Meta-skill (conversational) | Claude Code | DISCARDED 2026-07-23 | Was to wrap every `sm … --json` verb into a single slash-command with conversational follow-ups. Dropped with the graph-scoped second wave (the queue stays per-node); rationale in the Discarded list (`context/roadmap-history.md`). Row kept so citations resolve. |
| `sm-process-jobs` (skill, materialised by `sm agent install`) | Agent process deliverable | Any agent runtime | Step 10 Phase G | Teaches an agent the process protocol: loop `sm jobs claim` → [agent reasons over the rendered content] → `sm record`. Decided 2026-07-13: ships as a runtime-agnostic skill written into the lens's `scaffold.skillDir` (no npm package, no host glue); the queue-aware `/mcp` surface followed as the typed second wrapper over the same protocol. Split into three progressive-disclosure files 2026-07-23 (`SKILL.md` probe + routing + shared CLI processing loop + Rules; `mcp.md` for MCP-tool management; `cli.md` for the `sm`-verb equivalent), so a run loads only its mode's surface; `sm agent install/status` treat the set atomically (any missing/drifted file → update/stale). Default is resident/watch (`once` processes a single pass); the skill's first action PROBES for the MCP tools, recommends hybrid mode, and when absent tips how to mount AND register the server (project-local `claude mcp add`). Endpoint authority moved to `.skill-map/serve.json` (2026-07-28, live report: `sm --port 5252` left agents probing and registering the hardcoded 4242 default): the fallback checklist composes `http://<host>:<port>/mcp` from the live file (wildcard hosts read as loopback) and every per-runtime register snippet carries that composed `<mcp-url>`; the default port survives only as the file-absent fallback, and a registration left on a stale port routes to the re-register step. |
| `sm-cli` | Agent integration package | Claude Code (installable) | Step 15 | Feeds `sm help --format json` to the agent so it can compose CLI invocations without hand-maintained knowledge. Mentioned in Decision #65; ships at distribution polish. |
| `skill-optimizer` | Dual-surface action + skill | Claude Code (skill) + any runner (action) | Skill exists before `v0.5.0`; generalized as skill actions 2026-08-09 | Canonical dual-mode example: exists as a Claude Code skill AND runs as per-node skill-map work. The one-off "Action wrapper in invocation-template mode" plan was superseded by the generic **skill actions** catalog ([`spec/skill-actions.md`](./spec/skill-actions.md)): ANY `SKILL.md` skill installed under `.skill-map/.agents/skills/` (e.g. via `cd .skill-map && npx skills add <repo> --skill <name>`, skills.sh) is discovered at `sm serve` boot and offered on every node as a `skill:<name>` probabilistic job (offering gated by the project-local `skillActions.enabled` toggle, default off (opt-in), surfaced in Settings > Project and read fresh per request so it applies live), its body inlined under a kernel-authored section behind the spec-pinned wrapper template, reporting against the canonical `schemas/skill-actions/report.schema.json`. The reference pattern for "same capability, two surfaces" is now a shipped generic mechanism, not a per-skill wrapper. |

Naming analyzers:

- **Slash-command ids** (`/skill-map:<verb>`) are what the user types inside the host.
- **Package ids** (`sm-cli`, `sm-cli-run-queue`) are what the user installs. One package MAY register multiple slash-commands; one slash-command is registered by exactly one package.
- **Host-specific** skills live under `sm-cli-*` namespace. When a second host (Codex, Antigravity) lands as a full skill catalog, its packages get their own prefix (`sm-codex-*`, `sm-antigravity-*`), the namespace is owned by the host, not by the skill. The retired `sm-gemini-*` slot is preserved for historical references; current Antigravity skills route through the vendor-neutral `agent-skills` standard.

Non-skills shipped for context (listed here to prevent confusion, do NOT register as skills):

- **The queue itself**, submit/claim/record are plain CLI verbs; no runner component exists (the `sm jobs run` CLI-runner loop was removed by the pull-only decision, 2026-07-13). Not a skill.
- **Default plugin pack**, `github-enrichment`, plus TBD Extractors/Analyzers. Not skills, but installable via drop-in.

---

## UI (Step 14 full)

### Step 14, Full UI (Flavor B)

Vertical slice with real kernel. Same prototype upgraded to consume the actual Hono server.

**Single-port mandate (non-negotiable)**: `sm serve` exposes the SPA, the BFF and the WebSocket under **one listener**. Consumers never need to know two ports exist.

```
sm serve --port 7777
│
├── GET  /api/*     → BFF endpoints (thin wrappers over kernel)
├── WS   /ws        → canonical job / scan / issue events
├── GET  /assets/*  → Angular bundles (JS/CSS/fonts)
└── GET  /*         → fallback to ui/dist/index.html (SPA routing)
```

- **Production**: Hono serves the Angular build via `serveStatic` alongside the API and WS. One process, one port, one command.
- **Development**: Angular dev server with HMR (its own port) proxies `/api` and `/ws` to Hono via `proxy.conf.json`. The SPA still sees a single origin.
- BFF role: **thin proxy** over the kernel. No domain logic. No second DI. Keep it minimal, that is why Hono was chosen over NestJS / Express.

WebSocket `/ws` endpoint:
- Server pushes the canonical event stream from `spec/job-events.md`: job family (stable) + `scan.*` + `issue.*` families (experimental in v0.x).
- UI sends commands (rescan, submit, cancel) on the same channel.
- REST HTTP reserved for discrete CRUD (config, exports).
- Transport keep-alive: the server pings every connected client on a fixed interval (30s) so idle connections survive intermediary proxies (the dev-server proxy, hosted load balancers) and half-open peers get reaped; the browser auto-pongs, no client code or envelope involved. The client resets its reconnect backoff only after a connection proves stable, so a flapping endpoint escalates to a non-fatal "connection lost" banner instead of re-seeding `/api/scan` in a tight loop. See `spec/cli-contract.md` § WebSocket protocol.

Inspector panel renders:
```
External (github-enrichment, if applicable):
  stars, last commit, verified ✓/✗

Summary (ai-summarizer-action, if run):
  kind-specific summary fields
  (stale) flag if bodyHash diverged

Links:
  incoming (N) and outgoing (M) with kinds

Issues: N     Findings: M
```

### Workspace (fused view)

The SPA's former two destinations (a standalone files list and a standalone graph) were fused into a single **workspace** at route `/`: a drag-resizable files tree rail on the left, the graph canvas in the center, and the inspector as a floating right-side slide-over, all linked through the shared `?path` selection. The rail doubles as a **map-visibility curation** surface: per-file and per-folder (tri-state) checkboxes pick which nodes the graph shows; an "isolate" gesture (focus a node and its direct, 1-hop neighbors) is a one-click shortcut; the curated state persists to localStorage. The graph's layout reset re-arranges only the currently visible nodes when the view is curated. The old `/files` and `/map` routes and their topbar tabs were retired. **Deviation model (landed 2026-07-27, user decision):** the original INCLUDE-set semantics (empty set = whole corpus; checking narrows) was replaced by rsync-filter-style overrides, because the include set made the checkboxes lie (everything visible, nothing checked) and hid newly created files under an active selection. Now every checkbox starts CHECKED, unchecking EXCLUDES the subtree, and overrides inherit with nearest-ancestor-wins, so both workflows stay one gesture ("hide this noise" = uncheck it; "show only app/" = master-uncheck in the tree header + check `app/`); a mixed folder clicks to fully visible (native tri-state convention). Evaluation is server-side pre-cap: `/api/branch` (and MCP `get_branch`) grew `exclude=` / `excludeRoot=` params compiled into the SQL scope, with an inference rule that keeps bare `?path=app` meaning "only app/" so historical callers never broke; the old localStorage include-set migrates to root-exclude + includes. Normative: `spec/cli-contract.md` §Map scope overrides + the route row, `spec/mcp-server.md` get_branch row. The covered-by-ancestor disabled-checkbox state died with the include set (every row is now toggleable; a toggle under a selected ancestor just writes a deeper override).

**Map views (wave 1, 2026-08-10, Decision #152).** The curated topology stopped being browser-local: a named **map view** persists the override map plus the manually pinned node positions as one committed file (`.skill-map/views/<slug>.json`, canonical serialization, schema `spec/schemas/map-view.schema.json`, contract `spec/map-views.md`), so a curated map travels to the team through git, the fourth storage home of `spec/architecture.md` §Storage rule. The graph toolbar grows a switcher (apply / save / save as / revert / exit views, dirty dot, broken-refs badge, `?view=` deep link, confirm-on-dirty-switch behind the project-local `ui.confirmViewSwitch`, drag-to-reorder rows whose SHARED order persists in each file's optional `order` field with the first nine positions mapped to the digit shortcuts 1-9, spec §Ordering and shortcuts); the BFF exposes `GET/PUT/DELETE /api/map-views` (fresh reads, atomic canonical writes, last-write-wins, operations-logged); dead node references in a view are tolerated on apply and surfaced as a count, never machine-pruned (a reserved `view-ref-broken` analyzer + fixer may later propose the cleanup). Views deliberately capture NEITHER filters (attribute axis, already URL-synced) NOR the viewport; only `manual: true` pins travel, so auto-layout absorbs corpus drift between checkouts. **Wave 2 (deferred): visual grouping** on Foblex's native `fGroup` (drag-to-group, auto-size, connections-to-groups); the schema reserves the `groups` array (id, label, color, members) from day one so wave-1 files never migrate.

**The rail table is virtualised (2026-07-26).** Measured against a generated 1000-node fixture (`pnpm perf:gen`, `fixtures/perf/`) in real Chrome via CDP, an expanded tree mounted every row: 19,663 DOM elements in the rail, 1,019 `<tr>`, 2,022 tooltip directives and 32,167 JS listeners app-wide, with `expandAll` at 746ms and a single small-folder collapse costing 95ms open versus 24.8ms closed. Cost tracked rows MOUNTED, not rows changed. PrimeNG's `[virtualScroll]` now renders only the window: 945 rail elements, 48 rows, 94 tooltips, listeners back at the collapsed baseline, and the folder toggle down to ~53ms.

Three decisions worth keeping on the record. **Unconditional**, with no setting and no row-count threshold: a threshold would flip the DOM contract mid-session and would leave CI (a 9-node demo) exercising only the non-virtual branch, which is the `fnode-branch-parity` failure mode the graph already paid for; the graph's `perf.virtualization` flag exists because Foblex forced a duplicated template, and PrimeNG needs none. **Rows are a uniform 36px**, owned by `FILES_ROW_HEIGHT_PX` in TypeScript and bound into CSS as `--files-row-h`, because the scroller is a fixed-item-size virtualizer and a drifting row height desynchronises the spacer silently. **Keyboard navigation replaced Tab-walking**: only the window is in the DOM, so the rail took a roving tabindex with a delegated key handler (arrows, Home / End, Page Up / Down, Arrow Right / Left to expand / collapse / climb, Enter to activate, Space for the map checkbox) plus a focus rescue for rows recycled out from under the focus. The table deliberately stays `role="table"` instead of claiming `treegrid`, whose contract (cell roles, two-dimensional navigation, a mode switch for in-cell widgets) would be worse half-implemented than an honest table. Row animations are gone for good; the standing prohibition and its history live above `.files__row` in `files-view.css`.

---

## Testing strategy

From commit 1. Same rigor as kernel-first.

| Layer | What it tests | When |
|---|---|---|
| Contract | Every registered extension conforms to its kind's schema | Each startup + CI |
| Unit | Each Extractor / Analyzer / Provider / etc. in isolation | CI + dev |
| Integration | Scanner end-to-end over fixtures | CI |
| Self-scan | `sm scan` on skill-map's own repo | CI (mandatory) |
| CLI | Spawn binary, assert stdout / stderr / exit codes | CI |
| Snapshot | Renderers produce byte-exact output | CI |

Framework: **`node:test`** (built-in, zero deps, Node 24+).

Every extension in `src/extensions/` ships a sibling `*.test.ts`. Missing test → contract check fails → tool does not boot.

**Performance budget**: `sm scan` on 500 MDs completes in ≤ 2s on a modern laptop, enforced by a CI benchmark (lands with Step 4 when the scanner goes end-to-end). **Startup posture (2026-08-07)**: fixed per-invocation costs are engineered out rather than tolerated. Scans are incremental by default (`--full` opts out); the serve verb dynamic-imports the whole BFF subgraph so no other verb parses or executes it; spec schema validators compile per schema on first use (files still read eagerly so a corrupt install fails fast); the BPE tokenizer is constructed once per process (the watcher used to rebuild it every rescan); the BFF watcher consumes the boot-cached plugin runtime instead of running a second discovery; and tsup code-splits the bundle so shared kernel code dedupes across entries.

**Conformance cases deferred**: `preamble-bitwise-match` ✅ landed at Step 10 (Phase B closure): a `ai-summarizer-action` job submitted via the new `setup.priorInvokes` staging phase, read back with `sm jobs preview --last`, asserted byte-exact against `spec/conformance/fixtures/preamble-v1.txt` via the new `stdout-contains-verbatim` assertion.

---

## Stack conventions

- **Naming**: two analyzers, both normative and enforced spec-wide (see `spec/README.md` §Naming conventions).
  - **Filesystem artefacts in kebab-case**: every file, directory, enum value, and `issue.analyzerId` value, `scan-result.schema.json`, `job-lifecycle.md`, `auto-rename-medium`, `direct-override`. So a value can be echoed into a URL, a filename, or a log key without escaping.
  - **JSON content in camelCase**: every key in a schema, frontmatter block, config file, plugin/action manifest, job record, report, event payload, or API response, `whatItDoes`, `injectionDetected`, `expectedTools`, `sourceVersion`, `docsUrl`, `ttlSeconds`, `runId`. The SQL layer is the sole exception (`snake_case` tables/columns, bridged by Kysely's `CamelCasePlugin`); nothing crosses the kernel boundary as `snake_case`.
- **Runtime**: Node 24+ (required, active LTS since Oct 2025; `node:sqlite` stable; WebSocket built-in; modern ESM loader).
- **Language**: TypeScript strict + ESM.
- **Build**: `tsup` / `esbuild`.
- **CLI framework**: **Clipanion** (pragmatic pick, introspection built-in, used by Yarn Berry).
- **HTTP server**: **Hono** (lightweight, ESM-native). Acts as the BFF for the Angular UI and any future client.
- **WebSocket**: server side uses the official `upgradeWebSocket` re-exported from `@hono/node-server@2.x` paired with the canonical `ws` Node WebSocket library (`ws@8.20.0`); both share the single Hono listener, single-port mandate. Client side uses the browser-native `WebSocket` (browser) or the Node 24 global `WebSocket` (Node-side tests and consumers, no extra dep needed beyond the server-side `ws`).
- **Single-port mandate**: `sm serve` exposes SPA + BFF + WS under one listener. Dev uses Angular dev server + proxy; prod uses Hono + `serveStatic`.
- **UI framework**: **Angular ≥ 21** (standalone components). Scaffolded at `^21.0.0`, later pinned to an exact version per the dependency-pinning policy, see §Operating rules in `AGENTS.md`.
- **Dependency versioning policy**: every dependency in `package.json` at root, `ui/`, and `src/` is pinned to an exact version (no `^` / `~`). `spec/` has no dependencies. Reproducibility takes priority over automatic patch drift; upgrades are explicit edits. Revisit if `src/` ever flips to public, published libs may want caret ranges so consumers can dedupe transitive deps.
- **Node-based UI library**: **Foblex Flow**.
- **Component library**: **PrimeNG** + `@primeuix/themes` for theming. The legacy `@primeng/themes` package is deprecated upstream (the registry marks it as `Deprecated. Please migrate to @primeuix/themes`) and is intentionally NOT used. **Version ceiling: PrimeNG 21.x MIT (decision 2026-07-20).** PrimeNG 22+ moved to the commercial "PrimeUI" license (free Community tier keys expire at runtime after 12 months + 30-day grace, so every shipped bundle older than ~13 months would show an "Invalid PrimeUI License" banner to end users, and the key is a personally-identified credential embedded in a public npm package). A full Angular 22 + PrimeNG 22 migration was executed, validated green, and deliberately reverted on these grounds; only the version-independent fixes were kept. Consequences: Angular stays on 21.x while PrimeNG is in the stack (primeng 21 peers cap at `@angular/* ^21`), 21.1.9 is likely the last free release of the 21 line (future 21 fixes ship via the paid `-lts` channel), and a PrimeNG exit (23 primeng modules to CDK/custom, sized at weeks) is the enabler for any future Angular major. Revisit if PrimeTek changes the model or when the exit is scheduled.
- **UI styling**: **SCSS scoped per component**. No utility CSS (no Tailwind, no PrimeFlex).
- **UI workspace**: `ui/` as pnpm workspace peer of `spec/` and `src/`. Kernel is Angular-agnostic; UI imports only typed contracts from `spec/` once those exist, see the DTO gap note below.
- **UI YAML parser**: **`js-yaml`**, locked at Step 0c when the prototype's mock-collection loader first needs to parse frontmatter in the browser. The second candidate (`yaml`) was dropped at pick time; revisit only if the impl-side pick diverges.

### UI-only deps (Step 0c onwards)

These deps live in `ui/package.json` only. The kernel does NOT import them and MUST never gain a transitive path to them, they stay on the UI side of the workspace boundary.

- **`js-yaml`** (+ `@types/js-yaml`), frontmatter parsing in the browser. Locked above; duplicated here so a reader of §UI-only deps has the full picture.
- **`@dagrejs/dagre`**, hierarchical graph auto-layout. Consumes `{ nodes, edges }`, returns `{ x, y }` per node; rendering stays with Foblex. Picked over the inactive `dagre` package (the `@dagrejs/*` scope is the maintained fork). No viable Angular-native alternative at Step 0c pick time; revisit only if Foblex ships its own layout primitive that covers the same cases.
- **`primeng`** + **`@primeuix/themes`**, already captured in §UI framework.
- **`@foblex/flow`** + peers, already captured in §Node-based UI library.
- **DB**: SQLite via `node:sqlite` (zero native deps).
- **Data-access**: **Kysely + CamelCasePlugin** (typed query builder, not an ORM).
- **Logger**: `pino` (JSON lines).
- **Tokenizer**: `gpt-tokenizer` (pure JS; replaced `js-tiktoken` in the 2026-08-17 perf sprint: ~4x faster init, ~7x faster encode, identical counts on the perf corpus), selected by the `tokenizer` project-config key from a closed allow-list of two encodings, `cl100k_base` (default) and `o200k_base`. Construction is LAZY behind `ITokenCounterHandle` (`kernel/orchestrator/token-counter.ts`): a fully-warm scan never loads an encoding at all, and the chosen subpath is imported via bundler-safe literal dynamic imports. The resolved encoder is persisted in `scan_meta.tokenizer` / `ScanResult.tokenizer`, and changing it forces a token recompute on the next incremental scan (the cache cannot serve counts from the other encoder). Special-token policy: token counting never throws; a literal `<|endoftext|>` in prose is counted as plain text (the js-tiktoken engine aborted the whole scan on such a file, a latent bug fixed by the swap).
- **Semver**: `semver` npm package.
- **File watcher** (Step 7): the primary scan watcher is selectable via `scan.watch.backend` (`chokidar` default) and the per-invocation `--watch-backend <chokidar|parcel>` flag (overrides the setting on `sm serve` / `sm watch` / `sm scan --watch`). `chokidar` (one `fs.watch` per directory) observes changes behind followed symlinks, so a live edit inside a symlinked directory refreshes the map; `parcel` uses `@parcel/watcher` (a single native inotify instance, scales to huge trees without chokidar's per-directory `EMFILE` exhaustion) but does not live-watch behind a symlinked directory (the initial walk still follows the link). The meta-watcher (config files at `depth: 0`, which parcel cannot express) is always `chokidar`. Both sit behind the `IFsWatcher` interface (`kernel/scan/watcher.ts`); selection is `resolveWatcherBackend` in `core/watcher/runtime.ts`.
- **Package layout**: pnpm workspaces, `spec/` (`@skill-map/spec`), `src/` (`@skill-map/cli`, with subpath `exports` for `./kernel` and `./conformance`), `ui/` (private, joins at Step 0c). The `alias/*` glob held un-scoped placeholder packages (`skill-map`, `skill-mapper`) for one publish round; once the names were locked on npm and a `npm deprecate` notice routed users to `@skill-map/cli`, the workspaces were dropped. Further `@skill-map/*` splits deferred until a concrete external consumer justifies them.

### Tech picks deferred (resolve at the step that first needs them)

~~YAML parser (`yaml` vs `js-yaml`)~~, **resolved at Step 0c: `js-yaml`.** · MD parsing strategy (regex vs `remark`/`unified`) · template engine for job MDs (template literals vs `mustache` vs `handlebars`) · pretty CLI output (`chalk` + `cli-table3` + `ora`) · path globbing (`glob` vs `fast-glob` vs `picomatch`) · diff lib (hand-written vs `deep-diff` vs `microdiff`).

Lock-in-abstract rejected during Step 0b: each pick lands with the step that first requires it, so the decision is made against a concrete use case rather than in the void.

### DTO gap (standing accepted trade-off)

The §Architecture section promises "typed DTOs" as part of the cross-workspace contract, but `@skill-map/spec` still ships only JSON Schemas and `index.json`, no `.d.ts` and no `spec/types/`. Both `ui/src/models/` and `src/kernel/types/` hand-curate local mirrors of the shapes they consume. This was originally framed as pending Step 2; Step 2 shipped long ago and the gap was never closed, so it is recorded here as what it actually is: an accepted trade-off, not scheduled work. It holds because AJV enforces the real shapes at runtime against the authoritative schemas, so a divergent TS mirror surfaces as a validation error rather than a silent bug. Codegen IS wired for the one surface where drift was expensive enough to justify it: the view-slot payload types are generated from `view-slots.schema.json` by `json-schema-to-typescript` in `scripts/generate-view-catalog.js`, drift-guarded by `view-catalog:check`. Extending that pattern to the rest of the DTO surface stays available if a third consumer makes it pay.

---

## Execution plan

Sequential build path. Each step ships green tests before the next begins.

### Step inventory at a glance

Closed Steps, green checkmark = "ships green tests, lives in the released code path". Per-step landing prose lives in the git history and the per-package changelogs.

Phase A, `v0.5.0` (deterministic kernel + CLI):

- ✅ **0a / 0b / 0c**, Spec bootstrap, implementation bootstrap, UI prototype (Flavor A).
- ✅ **1a / 1b / 1c**, Storage + migrations / Plugin loader / Orchestrator + CLI dispatcher.
- ✅ **2 / 3 / 4 / 5 / 6 / 7 / 8 / 9**, First extensions, UI design refinement, scan end-to-end, history + orphans, config + onboarding, robustness, diff + export, plugin author UX (9.1–9.4).

Phase A → v0.6.0 (Web UI):

- ✅ **9.5**, Spec base cleanup: absorb provider verbatim. Pre-wave-2 prerequisite.
- ✅ **9.6**, Annotation system (sidecar `.sm` files). Sub-steps 9.6.1–9.6.7, review queue R1–R15 closed.
- ✅ **14.1–14.7**, Full Web UI (Hono BFF, REST, WS broadcaster, inspector polish, Foblex strict types + dark-mode tri-state, bundle hard cut + responsive scope + demo smoke).
Phase B, Stabilization (deterministic core hardened into **Beta**, `v0.6.0` → `v0.52.0`):

- ✅ **Active-lens migration, Phases 1–6** (2026-05-19 → 2026-05-23), post-v0.6.0 deterministic polish that lands the multi-runtime story end-to-end:
  - **Phase 1**, lens model + Signal IR scaffold + numeric `Confidence` + MCP virtual nodes + OpenAI Codex provider (`.codex/agents/*.toml`) + extractor mudanza (`core/{markdown-link, slash, at-directive}` move to vendor-neutral `core`). Settings → Project → "Active provider" dropdown switches the runtime lens; switching drops `scan_*` and rebuilds the graph under the new lens. Single coherent migration in commit `29fb353`.
  - **Phase 2**, lens-only extractor gating (per-provider extractors run when the **active lens** matches, regardless of which provider classified the host file). Closes the cross-lens isolation contract.
  - **Phase 3**, provider-aware confidence bump for resolved invocation links + new `IProviderKind.identifiers` + `IProvider.resolution: Record<linkKind, targetKind[]>`. `@reviewer` mentions and `/explore` invokes that resolve render at confidence `1.0`.
  - **Phase 4**, Antigravity Provider onboarding (metadata-only, lens identity + reserved-names) + Gemini Provider retired (Google sunset Gemini CLI 2026-06-18; Antigravity ships under the open `.agents/skills/` standard, so the legacy `.gemini/` classifier had nothing to claim).
  - **Phase 5**, reserved-name catalog (`IProvider.reservedNames`) + `core/reserved-name` analyzer, and the **confidence-penalty model** that replaced the old per-extractor emit floor: the kernel seeds a `1.0` baseline on every link and score-phase detectors subtract a fixed delta (`RESERVED_PENALTY` 0.9, `BROKEN_PENALTY` 0.5), each co-located with the finding it reports, so disabling a detector drops both its report and its score effect. Two later consolidations fold in here: the `agent-skills` Provider became the universal DEFAULT lens (`core/markdown` demoted to the invisible base that classifies orphan `.md` under whatever lens is active), and the cross-vendor `@<file>` / `/<command>` grammars moved into vendor-neutral `core`, gated by `precondition.provider`.
  - **Phase 6**, observable link analysis: `core/link-counts` analyzer emits two `card.footer.left` chips per node (`linksIn` / `linksOut`) with per-`Link.kind` breakdown tooltips. Self-loops excluded from card chips. Link confidence renders as edge opacity in the graph view. Inspector linked-nodes panel groups by direction × kind.
  - **Phase 2/3 closure (Step 11.5, landed 2026-05-23)**, Signal IR resolver wired end-to-end: orchestrator calls `resolveSignals` after extract; all six link-emitter extractors emit Signals with byte ranges; cross-extractor range-overlap collisions detected via union-find clusters; new `core/extractor-collision` analyzer surfaces losers as `warn` issues naming the winner extractor and tiebreak reason. Two conformance cases close coverage row 37. Phase 4+ stubs (per-extension enable filter, confidence floor) documented but not wired.
  - **Safety nets shipped alongside the migration**: lens-drift warning when the persisted `activeProvider` disagrees with the markers on disk, db-version skew detection, and auto-detect on first scan (ambiguous marker sets always prompt rather than silently inheriting).
  - **Phase 5b (MCP config-side discovery)**: SHIPPED across the 2026-07 MCP iteration (codex `.codex/config.toml` + opencode `opencode.json` via the declarative `mcpConfig` capability; Antigravity stays frontmatter-only, its config is home-global), alongside live MCP invocation on all four runtimes and the opt-in read-only MCP server. See the dated log (2026-07-10/11) and [`spec/architecture.md` §Provider · MCP config discovery](./spec/architecture.md).
  - **Liveness = the answer, not the receipt (2026-08-06)**: the Check probe and `GET /api/agent/presence` both counted `job.claimed` as proof. A claim is a receipt: an agent parked on `sm jobs claim --wait` picks a job up within one poll cycle (2s), so the row went green while the model had not read a line of the prompt, and the boot ping made it go green before the operator asked anything at all. Positive evidence is now an ANSWER (`job.completed` / `job.failed`), which is what `sm record` emits at the end of the full circle. The claim keeps a job: it moves the check into a second phase ("picked it up, waiting for the answer") with its own longer window (45s vs 15s), and a claim that never comes back reports `no-answer`, a genuinely different diagnosis from "nobody is there". `lastClaimAt` stays claim-based (it answers "when was work last picked up"), it just no longer decides `attending`. A same-day review pass closed the two receipt-based signals the first cut missed: the MCP `claim_job` attempt hook (which flipped `attending` the moment an agent asked for work, before reading anything; removed, an MCP-parked agent on an empty queue now stays unknown until it answers something, symmetric with a CLI-parked one) and the UI submit-gate heal (which reopened AI affordances on `job.claimed`; it heals on answers now).
  - **Nodeless jobs (2026-08-06)**: `IAction.probNodeless` + `POST /api/jobs`. The `core/ai-ping-action` liveness probe used to aim at "the first real node" of the corpus and then tell the model to ignore what it sent, which bound a question about the AGENT to the state of an arbitrary file: a node deleted since the last scan surfaced the raw `node-drifted` refusal as "Something went wrong" in Quick Start and the inspector, and a project with nothing scanned yet could not be probed at all. A nodeless submit skips target resolution and the on-disk read + drift verification and enqueues against the synthetic `sm://<qualified-extension-id>`, whose constant hashes also make "one active probe at a time" fall out of the ordinary duplicate check. The claim → execute → record circle is deliberately untouched: the agent still answers the ping itself. Normative shape in [`spec/job-lifecycle.md` §Submit · Nodeless submit](./spec/job-lifecycle.md).
  - **Phase 5c (MCP registration recipe, 2026-08-06)**: the write-side mirror, `IProvider.mcpRegister`. How an operator declares skill-map's OWN server to a runtime (a shell command for the lenses with an `mcp` CLI verb, a complete config document for the rest) used to be a closed `Record` keyed by provider id inside the UI, so every lens outside that list silently copied the bare endpoint URL: exactly the hardcoded-provider-list the `providerRegistry` exists to abolish, and it made a correct recipe unreachable for any project-local drop-in Provider. The recipe is Provider data now, declared in the manifest, projected verbatim into the registry, rendered by the UI with `{{url}}` bound to the live endpoint. The four built-ins carry their former catalog entries verbatim; a Provider that declares nothing keeps the bare-URL fallback. Normative shape in [`spec/architecture.md` §Provider · MCP registration](./spec/architecture.md).
  - **Fail-closed AI gate at boot (2026-08-09, supersedes the 2026-07-26 fail-open call)**: an unknown skill reading (probe pending, probe failed, no lens resolved yet) used to leave every submitting affordance ENABLED so a transport hiccup never locked the AI surface, which in practice let the operator submit before anything was confirmed. The gate now holds the AI surface DISABLED under the dedicated `probe-pending` reason (tooltip: "Checking your agent setup...") until the automatic boot probe confirms the setup; drainage evidence outranks the pending probe, so a green manual-check verdict or any observed answer still opens the gate without waiting. Companion UX in the same pass: the inspector's AI actions re-read their launcher catalog when Settings closes (`SettingsVisibilityService.closed$` joins the live-refresh sources), since Settings is where the catalog's inputs (plugin extension toggles, the skill-actions offering) get mutated.
  - **Deferred (post-v1.0)**: Phase 6b's Codex AGENTS.md hierarchical walker (its skill half shipped 2026-06-25, the open `.agents/skills/` standard via composition; the AGENTS.md cascade remains). See §Deferred beyond v1.0.
  - **Codex body extractor (TOML `developer_instructions` field)**: ✅ landed via `read.bodyField: 'developer_instructions'` (Step 13); the codex provider is now beta (enabled by default, auto-detects `.codex/`).

- ✅ **Hardening + telemetry** (Phase B). Loopback-only `sm serve`, node render caps, and opt-in, off-by-default usage / error telemetry: three independent toggles (`usageCliEnabled` / `usageUiEnabled` / `errorsEnabled`), anonymous PostHog usage + Sentry errors behind one shared first-run consent prompt, a shared anonymous install id as the `distinct_id`. Normative contract in [`spec/telemetry.md`](./spec/telemetry.md). The errors surface later moved to per-incident consent (a scrubbed-preview dialog per crash, nothing persisted, spec §Per-incident crash-report consent); since 2026-08-09 UI module-load failures (a lazy chunk failing to fetch because the serving process is gone or a cached shell went stale, e.g. opening the app from cache with `sm serve` down) never open the consent dialog: the crash is environmental, carries nothing actionable, and usually has no server behind the page anyway.

Phase C, real-time exploration (shipped):

- ✅ **Live node activity v1** (design locked 2026-06-30, normative contract in [`spec/provider-activity.md`](./spec/provider-activity.md); merged 2026-07-03): the map lights each node the moment the operator's AI runtime invokes it, driven by the PROVIDER's own hook system. Architecture: an optional `activity` capability on the `provider` manifest (NOT a new extension kind), a zero-dep bridge installed by `sm activity install <provider>`, discovery via the `serve.json` runtime file, and a token+loopback-gated `POST /api/activity` ingest that broadcasts `node.activity` over `/ws`. All four runtimes (claude / codex / antigravity / opencode) were live-probed and shipped adapters.
- ✅ **Live activity v1.1: execution stats, spawn relations, session anchors, consent-gated conversations** (2026-07-05 onward, contract extended in [`spec/provider-activity.md`](./spec/provider-activity.md)): everything ephemeral and in-memory in the BFF, dies with `sm serve`, zero persistence. Per-node execution stats accumulate server-side (the client receives overwrites only), spawns resolve into stateless `agent.spawn` frames drawn as dashed ephemeral edges, client-side session capsules anchor spawns from the main context, agent capsules materialize for vendor built-ins that resolve to no scanned node, and inter-agent conversations are retained ONLY behind the opt-in `activity.captureConversations` gate (project-local, off by default, content never rides the WS).
- ✅ **Follow the Activity (camera follow toggle)** (2026-07-05): a map-toolbar toggle that auto-frames every executing node plus the live session capsules in one animated fit, re-framing whenever the membership changes. Reuses the existing signal-tween camera path. The disarm rule settled at gesture-interrupt (a free-form gesture switches follow off only when it interrupts an animated move in flight; explicit camera intents still disable at their call sites), and the stored default flipped OFF → ON in 2026-07-26.
- ✅ **Tutorial coverage of live activity** (2026-07-05): sm-tutorial part "Real time: watch your agent run", three chapters shared across the four runtimes with per-provider trigger deltas (wire the hook from Settings → Project, restart the session and watch the portfolio glow, opt-in conversation capture). The pure `agent-skills` lens has no runtime to hook, so the part exits with a short note.
- ✅ **Tutorial coverage of the AI layer** (2026-07-26): sm-tutorial part "The AI layer: your agent works the map", seven UI-only chapters over the `flawed-portfolio` seed (the deterministic/probabilistic split, the agent circuit and its second-session model, a first AI action, finders over planted flaws, fixers with the human-decision path, the tagger's propose-then-save flow, and the security lane teaching why suspicion ships fixer-less).
- ✅ **Live activity abstraction hardening (fifth-provider readiness)** (2026-07-05): with all four adapters shipped, the seams a NEW provider would hit got closed in four cuts with no behavior change: the in-process plugin template keeps only the envelope (the provider supplies its own hook registrations via `pluginHooksSource`), uninstall honors the shared bridge across coexisting providers, the duplicated adapter idioms moved to the shared kit `kernel/util/activity-adapter.ts`, and `IActivityInstall` became a discriminated union gated by AJV.
- ✅ **Activity clear-all** (2026-08-08): the inspector's Activity section gained a per-node "Clear all" (`DELETE /api/activity/node/<pathB64>`, [`spec/provider-activity.md`](./spec/provider-activity.md) §DELETE): one call drops the persistent `state_executions` run history (the port's single targeted history delete, same JSON1 containment as the GET), the in-memory runtime stats + pair counters, and the retained spawn conversations touching the node. Regenerable machine data per the storage rule, so no consent dialog (summaries-delete posture); logs `activity.clear` to the operations log; the UI re-hydrates the stats mirror afterwards so the section gate, node-card pill and edge labels converge without a reload.

- ✅ **Executing-tool badge on the lit card** (2026-08-08): while a node glows, a small monospace badge names the LITERAL tool that lit it, riding the existing `detail` field end to end with no wire change ([`spec/provider-activity.md`](./spec/provider-activity.md) §detail now blesses unit-frame detail: Claude `Skill` / `Read` / `Agent`, Codex `spawn_agent`, Antigravity `view_file`, opencode `skill` / `read`; MCP tool names already rode it). The client invocation-edge gate moved from detail-presence to the `mcp://` path prefix (unit details must never draw an edge), `NodeActivityService` publishes a per-path `executionDetails` map swept with the glow (same OnPush set-equality discipline as `activePaths`), and the inspector's recent ring now names the tool on unit entries too. Scope is deliberately the tools that already reach the bridge; the write half of that widening landed next (see the markdown-writes entry below), Bash / Grep badges remain out.
- ✅ **Markdown writes light the map** (2026-08-09): the claude adapter's `PreToolUse` matcher widened to `^(Skill|Agent|Read|Write|Edit|mcp__.+)$` and the opencode adapter now consumes the `write` / `edit` tools alongside `read`, so an agent MODIFYING an in-scope `.md` lights the node at the moment of the write (previously only the watcher's later rescan recorded it, with no attribution glow). Same filter-first PATH-signal path as reads, the literal tool name rides the existing `detail` badge (`Write` / `Edit` / `write` / `edit`), zero new contract surface. Deliberate exclusions, documented per-row in [`spec/provider-activity.md`](./spec/provider-activity.md): codex (hooks fire for `apply_patch` only, a patch body with no path arg; parsing deferred), antigravity (write tools exist in the binary but their hook arg shapes are not live-verified; waits for a probe run), and both `apply_patch` flavours.
- ✅ **Ignore buttons (append to `.skillmapignore` from the map surfaces)** (2026-08-09): until now the only way to ignore a path was the Settings > Project pattern editor; the gesture moved to where the operator already is: a hover-reveal ban glyph on the files rail's file AND folder rows plus a header affordance in the inspector, appending a root-anchored pattern (`/docs/notes.md`, `/docs/guides/`) through the existing `PATCH /api/project-ignore` (replace-list; the route's watcher restart re-scan removes the node / subtree from map + list on its own). A confirmation dialog (structural mirror of the sidecar-consent dialog, single shell mount driven by the new `ui/src/services/project-ignore.ts` owner, which also serializes the read-modify-write PATCHes) fronts the write and carries a don't-ask-again checkbox persisting the new project-local `ui.confirmIgnore` key (default `true` = ask; positive naming so the shared ui.* default fits). Duplicates resolve silently, demo mode hides the buttons, failures surface as a closable message in the files rail, and telemetry rides `ui.feature.ignore-path` (`once` / `always` / `declined` / `auto`, source-stamped `files` / `inspector`, never the path). Accepted v1: ignoring the inspected node leaves the inspector on its "Node not found" state, no auto-close.
- ✅ **Change spark (external file changes flash the node)** (2026-08-09): the complement of agent activity for HUMAN edits: when the live watcher detects a node's file changed on disk, the map flashes it once (~1s) in the theme's primary color (`--sm-spark-c: var(--p-primary-color)`), so an editor save or a git operation is noticed without ever reading as agent activity. Discriminator: the kernel now emits `mode: 'changed' | 'full'` on `scan.started` (closing the spec drift, [`spec/job-events.md`](./spec/job-events.md) documented `mode` while the impl emitted only `roots`; the never-implemented `single` / `target` / `rootsCount` were removed per the documented-surface-must-RUN rule), and `scan.progress`'s real per-node shape (`{ index, path, kind, cached, partialCache? }`) became the documented one. The UI's `NodeSparkService` sparks on `cached: false` frames (skipping `partialCache`, an extractor backfill is not a content change) only inside a `mode: 'changed'` scan, which excludes UI rescans (`POST /api/scan` pins full), boot and meta batches for free; new nodes spark, deletions emit no frame. Agent activity wins: lit nodes, and nodes lit within the last 2s (`NodeActivityService.wasActiveWithin`, new recency memory), never spark, so the markdown-writes glow above cannot double-flash. Toggle `ui.changeSpark` (project-local, default ON, subordinate to `liveUpdates` only) below the Real-time node activity row.
- ✅ **Spine comets (particles travel the executing spine source-to-target)** (2026-08-29, user request for more wow on the live map): while a static edge is the executing spine, bright round-capped particles march along it from the caller to the callee, so the DIRECTION of the live call reads on the map instead of a static glow. Built as a sibling Foblex connection layered over the spine with the same geometry inputs (`ui/src/app/views/graph-view/comet-overlay.ts`, a pure projection like the spawn and invocation overlays, never a read of the library's rendered DOM), dressed as a marching capsule dash with its own glow, selection and pointer-events off, hidden from AT; spawn-active pairs are excluded (their marching dash is already the flow signal) and several link kinds on one pair collapse into one track. PRM-gated like every live animation, and under reduced motion the track paints nothing rather than freezing into a dotted line. Spec: [`spec/provider-activity.md`](./spec/provider-activity.md) §Roles and boundary names the particle flow as part of the UI-owned spine treatment. First cut of the 2026-08-29 "wow" batch the user queued in this order: load-time draw-on, director-mode replay with a session trail, cinematic focus dimming, shareable replay, extra themes; 3D nodes were explored the same day and dropped (no third axis pays for losing the HTML cards, keyboard a11y and map-view pins that Foblex gives the 2D canvas).
- ✅ **Boot intro: the map draws itself** (2026-08-29, second cut of the wow batch): once per mount, the cards fade and rise in staggered along the layout's diagonal (a top-down layout unfolds rank by rank, a left-right one sweeps across) and the edges then draw themselves from source to target, markers popping in as the lines finish. Owned by `ui/src/app/views/graph-view/intro.controller.ts`, a three-phase `setupIntro` handle (`pending` -> `running` -> `done`) the host binds to two short-lived classes: `intro-pending` hides the unpositioned pile from mount until the first dagre pass is RECONCILED into positions (the same `layoutReconciledAt` stamp the camera's deferred boot fit keys on, never the raw layout tick nor node arrival, both of which fire while cards still sit at the origin), `intro-running` keys the CSS timeline for `INTRO_ANIMATION_MS` and then everything drops out of the DOM. Rule-3 split as in the view-switch fade: the `[fNode]` host animates opacity only, the rise lands on the card child, the draw-on animates `stroke-dashoffset` with the dasharray carried by the keyframes. Fully behind the reduced-motion gate (the phases toggle over an unchanged canvas, nothing is ever hidden), never replayed by WS refreshes, lens toggles or view switches.
- ✅ **Director camera + numbered trail in the replay** (2026-08-29, third cut of the wow batch): the replay stopped being a static overview. With the director on (transport-bar toggle, `sm.live.director`, default ON, browser-local like follow), the lens camera frames ONLY the node the cursor frame is about, so the tape plays as close-ups gliding from caller to callee (the existing fit math zooms a single card to `TAG_FIT_MAX_ZOOM`, which is the dolly-in), holds on frames about nothing on the map (ends, boundaries, nameless spawns, via the follow's empty-fingerprint sentinel), and frames the whole membership before the first frame and at the END of the tape (pull back to reveal the route). Pure helper `ui/src/app/views/graph-view/director.ts`, wired as the lens follow's `targetPaths` computed. The fold gained `trail` (every lit node once, in first-touch order; spawn frames touch `members` but never the trail); a step badge on each card of the route shipped with it and was REMOVED the same day (user call 2026-08-29, after seeing it next to the rail's current-step highlight: redundant chrome), the fold field stays as the route's data for future finders. Spec: [`spec/provider-activity.md`](./spec/provider-activity.md) §Roles and boundary names the replay narration chrome as UI-owned presentation derived from the same fold that lights the replay. Deliberately NOT drawn: ephemeral trail edges between consecutive steps that share no declared link, that gap is exactly what the observed-link-missing finder already reports. Transport bar colours (user call 2026-08-29, later the same day): the transport verbs (play / pause, step, copy link, director on) paint in the theme primary through a re-keyed secondary-text chain at the host (`.pbar__transport`), exit / trash / director off stay muted (`.pbar__quiet`), and the amber stays the bar's replay chrome (border, scrubber, badge). Same call, the lit EDGES: the exec palette (`--sm-exec-c1..c5`, the comet tint) is re-keyed by every extra theme to its own ramp (green on matrix / neon G, cyan on neon B, rose on neon R, sky-to-white on blueprint, rust ink on paper, the comet a dark ink particle there) and `--sm-exec-hue-sweep` freezes the spine's hue-rotate on those ramps; the base light / dark palettes keep the rainbow.
- ✅ **Cinematic focus (graded depth-of-field dim)** (2026-08-29, fourth cut of the wow batch): the selection dim stopped being a flat two-tone. `ISelectionView` / `IEdgeSelectionView` gained `far`, and `selection-state.ts` grades every node by hop distance from the focus through a multi-source BFS (`computeFocusDepths`, capped at `FOCUS_FAR_DEPTH` = 3): hop 1 is the highlighted ring, hop 2 the near ring (`dimmed`, opacity 0.55), hop 3 and beyond or unreachable is `far` (opacity 0.22 plus `saturate(0.25)`, never blur: a saturate on a thousand cards is cheap, a blur is a compositing bill); edges follow their FARTHER endpoint (0.3 near, 0.12 far) so the lit cluster reads as one and the links into dim territory dim with it, while the tag-selection suspension and the executing-wins-over-dim rules are unchanged. The same grading now has a second origin: with Follow the Activity armed on the curated map (Real Time on, not under the lens or the replay, which narrow the canvas themselves), the EXECUTING nodes become the focus and the map falls off around them without any selection ring on the neighbours, so attention follows the action the way the camera does; a selection stays the operator's own focus and wins. The fade eases over 400ms (rack focus, not a cut), PRM-gated; the follow toggle's label now says "camera and focus" since it owns both.
- ✅ **Addressable replay (deep link + Copy link)** (2026-08-29, fifth cut of the wow batch, the "shareable replay" item scoped down from a video export, which is not honestly buildable in-browser over HTML cards): a session replay is now a URL, `?replay=<rootOwner>[&agent=<spawnId>][&at=<frame>]` ([`spec/provider-activity.md`](./spec/provider-activity.md) §Session journal, Addressable replay). `ui/src/app/views/graph-view/replay-url-sync.ts` reads it ONCE at boot (the `MapViewUrlSyncService` shape: resolve, then write back), the graph view resolves the target through the new shared `ui/src/services/session-catalog.ts` (the Sessions rail's journal fold and title logic extracted so the rail and the link agree on identity and label; tape first, then one best-effort journal GET) and enters the replay paused on `at`; while a session-scoped replay is on screen the transport mirrors itself into the URL, `at` only while paused, and the whole-tape replay stays unlinkable (no session identity). The transport bar gained Copy link, built from the router tree plus the app base href (never `location.href`, the write-back navigation lags the transport by a frame). `TReplaySource` now carries `rootOwner` / `agentSpawnId` on both session-scoped kinds. Same-project, per-machine by nature: the journal is not committed, so a link travels with the recording file; a journal export / import is the natural next step if cross-machine sharing turns out to matter.
- ✅ **Activity stats survive restarts + sighted-only nodes show their log** (2026-08-29, user bug report "nodes light up but declare no activity"): two causes were by contract and one was a UI defect. By contract, custody `keepAlive` starts, `shell` sightings (Bash) and sticky re-emissions light a node without counting; and the accumulator was boot-scoped, so a replay of a journal recorded under an earlier `sm serve` lit nodes the current process had never counted. User decision: the map must show everything that ran, so the accumulator now CHECKPOINTS into the project DB (`state_activity_stats` / `state_activity_pairs`, [`spec/db-schema.md`](./spec/db-schema.md); per the storage rule, machine output goes to the DB): every mutation marks its node / pair dirty, a 250ms debounce upserts the dirty rows through the new `StoragePort.activity` namespace (best-effort, memory-only without a DB), `createServer` hydrates from the rows at boot, `close()` flushes, the rename heuristic migrates both tables, and the Activity clear-all drops the rows with the history it already deleted. The summary's `since` became the earliest persisted first-sighting. The UI defect: a shell sighting's frame rode without `stats`, so the client mirror never learned the node existed (no section, no pill), and after a reload the section's empty gate looked only at `count`, hiding the recent log the spec promised ("so the inspector can show who named the file"); the sighting frame now carries the node's unchanged count-0 stats and the empty gate honours `recent`. Custody starts deliberately keep riding bare (nothing to show). Schema change under the greenfield posture: `001_initial.sql` edited inline, so an existing project DB rebuilds on its next scan.
- ✅ **Blueprint + Paper themes** (2026-08-29, last cut of the wow batch): two extra themes join the Settings picker through the existing registry shape (one CSS file under `ui/src/themes/`, one registry entry, one `angular.json` line, per-theme favicon + brand mark). Blueprint is a cyanotype drafting sheet: white and sky-blue ink, a technical grid, crisp ruled cards and flat strokes instead of glow, on the dark base like the neons. Paper is warm parchment with sepia ink and a dotted journal grid, the FIRST extra theme on the LIGHT base: the registry gained `forcesLight` (the mirror of `forcesDark`, the service drops `.app-dark` + `.dark` even under a dark tri-state), so a light-native look no longer has to fight the dark palette underneath. Both keep the exec rainbow, the replay amber and the spawn magenta as the theme-independent live signals, and clear the same WCAG floors the neon files document (3:1 field borders, 4.5:1 accent text). With seven entries the Settings picker outgrew its selectbutton strip and became a `p-select` dropdown (the active-lens select's idiom, `ariaLabelledBy` on the row label, options stacking the label over the registry description).
- ✅ **Replay steps in the Sessions rail + draggable lens cards** (2026-08-29, two user asks queued during the wow batch). Rail: while a session-scoped replay is on the map, the row under the cursor is `aria-current="step"` with an amber rail, and the panel follows the replay: the session (and the agent chain when the step belongs to a subagent) auto-expands and the current row scrolls into view, reduced-motion aware (a step-number chip mirroring the card badge shipped and was removed the same day with the badge, user call). Lens: cards are draggable while the lens or a replay is on (the `fNodeDraggingDisabled` gate is gone); a second `setupNodeDrag` instance writes the lens controller's session-local `pins`, which the pipeline projection overlays and the next force relayout seeds as fixed nodes, so a dragged card stays put while newcomers settle around it; the layer never touches `localStorage` or the curated pins and is emptied on every lens exit ([`spec/map-views.md`](./spec/map-views.md) persistence table gains the row).
- ✅ **Topbar theme menu** (2026-08-29, user ask): the topbar theme button stopped cycling and now opens a popover menu (`p-popover` appended to body, the map-view switcher's idiom): the tri-state base (auto / light / dark, exactly the three the toggle used to cycle) plus every extra theme from the registry, as `menuitemradio` rows with label + description and the current look checked. The trigger glyph is the mode icon, or the palette while an extra theme is on, and its accessible name carries the current theme. Picking a base mode clears the extra theme and emits `ui.feature.theme-toggle` as before; picking an extra emits `ui.feature.theme-extra` stamped `source: topbar` (Settings now stamps `settings`, [`spec/telemetry.md`](./spec/telemetry.md) adds `topbar` to the source set). The Settings dropdown stays as the second door.
- ✅ **Every Record is a new session** (2026-08-29, user bug report "new recordings concatenate with the previous one"): the server journal was already writing one file per Record gesture; the rail merged them because a session's identity was the runtime owner alone (`main:<session_id>`), so the browser tape folded every recording of the same Claude session into one row and hid the separate journal files behind it (and, on a tape-less browser, showed only the first file of that owner). Identity is now root owner + recording (`sessionKeyOf`): the recorder stamps every captured frame with its Record gesture (`TRecordedEvent.recordedAt`), `computeSessionIndex` partitions by it, the catalog folds each journal file into its own row keyed by the file's `startedAt` and hides only the file whose window the tape already narrates, the replay selection carries `recordedAt` (identity) and `tapeWindow` (the tape filter), the playback source and the deep link carry `rec=<recording>`, and the Sessions view keys expansion, tracking and the active-step match on the composite key. [`spec/provider-activity.md`](./spec/provider-activity.md) §Session journal documents the rule.
- ✅ **Shell sightings count on the card** (2026-08-30, user bug report "se ven las actividades del tipo bash en el inspector, pero no en el card", after several earlier passes that fixed the inspector and the persistence but never the card): root cause was a contract, not a defect. The 2026-08-18 field-audit rule made `access: 'shell'` starts land in the typed recent log with NO count mutation, and the card's execution pill hides at `count` 0 while the wire stats carry no sighting field at all, so a node named only by Bash commands was structurally invisible on the map and visible in the inspector. Three options were laid out (a separate sightings counter on the wire + a second pill, counting shell as executions, or a has-log flag); user picked counting, aware it reverses the rung-5 "sighting, not evidence" stance for the COUNTER only. Spec §Execution stats now says shell starts count like any other resource access; a follow-up decision in the same session admitted the class as EVIDENCE too, so §Capture level rung 5 and §Session journal Consumption say a sighting folds as `reads` (`foldAccessFrame` maps `shell` next to `read`, pinned by the session-journal test; the observed-* finders lean on their repetition + points-coverage gates to absorb the heuristic noise; `write` stays unfolded, a separate decision) and `db-schema.md` notes that rows checkpointed before the change may still carry `count` 0 next to a non-empty log (the inspector's empty gate keeps honouring `recent` for exactly those). `ActivityStatsService.record` lost its `sight()` short-circuit; `count()` admits the `shell` access class and keeps tagging both ends `kind: 'shell'`. Patch changeset with a user-facing note.

- ✅ **Activity wiring self-test (`sm activity status --verify`)** (2026-08-10, contract in [`spec/provider-activity.md`](./spec/provider-activity.md) §Wiring self-test): every failure in the live-activity chain was SILENT by construction (the bridge must exit 0 and stay quiet, the ingest answers 202 regardless, and the install state is pure disk state), so a bridge that crashed on every invocation, a dead server, or a stale `serve.json` all rendered as a green `installed` next to a dark map. The flag is the surface that disagrees: it pushes one synthetic probe event through the REAL installed bridge and asks the server whether it arrived (`__skillMapProbe` short-circuits `POST /api/activity` before `mapEvent`, so the probe can never light a node, count as an execution or broadcast a frame; the boot-scoped nonce ring reads back over the new `GET /api/activity/probe`). Verdicts `ok` / `not-installed` / `unsupported` / `incomplete` / `server-down` / `bridge-failed` / `not-received`, the last four exit 1. Security posture (normative): the bridge is spawned at the path skill-map composes, the command string wired into the operator's hook config is compared as text and NEVER executed, since under clone-and-scan that file is authored by whoever wrote the repo. Deliberate honesty limit: it proves everything DOWNSTREAM of the hook spawn and explicitly does not claim the runtime actually spawns the hook, which no local test can establish. Found by probing why markdown nodes stayed dark (an `@file` mention and a `Bash grep` both consume a file with zero tool events), and validated end to end against the archetypal silent failure: an ESM host `package.json` breaking the bare-`.js` bridge, which `installed` still reported green.

- ✅ **File-absolute line numbers, full-file Raw view, middle-click pan keeps the selection** (2026-08-10, user request): every persisted line (`link.location.line`, the `L<n>` prefix findings render, external-ref lines, the unclosed-backtick warn) now counts the frontmatter block, matching what the author's editor shows. The offset is parser-owned (`IParsedFile.bodyLineOffset`; `frontmatter-yaml` counts the real fence prefix, `plain` / `toml` omit it), rides `IRawNode`, and is applied at ONE point (the orchestrator shifts body-scoped Signals at `emitSignal`), so the eleven line-tracking extractors stay untouched; a `bodyField` provider (codex TOML prompts) drops the offset and stays body-relative because no file-absolute mapping exists there. To keep manual counting honest, the inspector's Raw toggle now shows the on-disk file VERBATIM through the new `GET /api/nodes/:pathB64?include=raw` (`item.raw`, same single disk read as `body`), so its line gutter aligns 1:1 with the reported `L<n>` while the rendered view stays body-only. Companion UI fix: the middle-mouse pan directive consumes its press in the capture phase, so Foblex's any-button selection claimants (`FDraggableDirective.onPointerDown` finalizes non-draggable buttons with an `EmitSelectionChangeEvent`) can no longer clear a live single or marquee-built multi selection when the operator pans the background. Normative: `signal.schema.json` / `link.schema.json` line semantics + `spec/cli-contract.md` single-node route row.

- ✅ **Numeric-token guard on the `/` and `@` grammars** (2026-08-10, field report): "Run the Quick Self-Test (5 dimensions, 0-2 each, total /10)." emitted an `invokes` for `/10` that surfaced as a red `reference-broken`. The command / handle identifier now requires AT LEAST ONE LETTER (fixed-position lookaheads in `SLASH_TOKEN_RE` / `AT_TOKEN_RE`), so prose fractions, scores, and dates (`/10`, `/10:30`, `@10`, `@10/20`) never match, while digit-leading names (`/2fa-setup`) and numeric filenames (`@10.md`, letters live in the extension) keep matching. The `$` grammar already carried the equivalent guard (lowercase-first-letter, rejecting `$5` currency); the `/` and `@` grammars were swept together per the runtime-quirks §8 grammars-move-as-a-set rule, which this divergence validated in the narrowing direction. Normative: `spec/architecture.md` §Extractor · code-region triggers (both sibling rows name the guard).

- ✅ **Live lens (ephemeral "camera on what executes" map mode)** (2026-08-13, user request): a map-toolbar toggle that narrows the canvas to the nodes the AI runtime is executing RIGHT NOW plus the recently-executed ones inside a linger window (5 min default, no-limit option, always-available reset), corpus-wide and curation-independent ("what is executing", not "what my map shows"). Membership is a pure client-side watermark, `activePaths ∪ { lastSeen > max(now - window, resetAt) }` over the already-reactive execution stats (`lastStartAt` survives refresh) plus a local departure stamp for long-running sticky claims; reset just moves the watermark, no server mutation. Rendering is a SECOND `setupGraphPipeline` instance (pass-through filter stub, permanently-empty pin layer, seeded incremental d3-force that PINS the survivors (`fx`/`fy`, whole-cloud centring forces dropped on incremental runs) so only the newcomers move, no drift reads as flicker), display-switched at the component so the main pipeline, reconciler, map views and position storage never notice: exit restores the curated map BY CONSTRUCTION, and the camera restores its enter snapshot only while the main paths fingerprint still matches (else the standard fit wins). Camera reuses `setupFollowActivity` via two new additive seams (`targetPaths`, `followState`), armed session-locally so a lens disarm never clobbers the persisted follow preference; node drag is disabled (`fNodeDraggingDisabled` + handler guard), layout / map-view / reset-layout / facet chrome hides while on, and out-of-branch live nodes fetch through the existing include-scoped `GET /api/branch` wire (cost proportional to the live set). Gating: hidden in demo and with Real Time off (forced exit on the flip), empty state branches on the confirmed-missing activity hook. Zero new HTTP / CLI / schema surface; window preference persists per browser (`sm.live.lens-window`), the mode itself is session-only. **Observed links persist** (2026-08-14, user call: "los links que realmente sucedieron no se evaporan"): the lens keeps an always-on memory of relations that actually happened, MCP invocations (caller -> target, tool label), node-to-node agent spawns (last spawnId keeps the conversation click, session/capsule chrome excluded) and executing-spine pairs over the cached links, rendered with the exact live treatments but exempt from the live TTLs (60s invocation sweep, spawn end-frame): a link that happened stays while both endpoints stay members, ages out with the same watermark, resurfaces on a 5min -> no-limit flip (pruned only by reset and a 2000-relation cap), and reset hard-clears it. On-air chrome shipped alongside: the theme-invariant broadcast red token (`--sm-live-c`, styles.css; the severity token is pale rose / brick / GREEN across themes), the 6px breathing frame, the LIVE corner badge, and the recording-dot toolbar toggle.

- ✅ **Live lens replay (session playback)** (2026-08-14, Fase 1 of the playback evaluation in the 2026-08-13 plan; the durable server-side journal stays deferred until the page-lifetime limit hurts in real use): rewatch everything the map showed since the page opened, time-compressed to ONE event per second, without re-executing anything. Three pieces: `ActivityRecorderService` (eager app-initializer subscriber to the raw `events$` multicast, `node.activity` + `agent.spawn` only so the `scan.progress` fan-out never floods it, 50k-frame oldest-first ring with drop accounting, gated by Real Time, page-lifetime by design); the pure fold `computePlaybackState(events, cursor)` reproducing the live claim semantics in VIRTUAL time (same TTL classes via newly exported defaults, owner heartbeat, session/owner-scoped ends, invocation correlation; accumulative divergences on purpose: members, invocations, spawns and co-lit pairs narrate history instead of decaying), cross-referenced with `NodeActivityService.apply` so neither drifts silently; and `ActivityPlaybackService` (frozen-tape transport: enter snapshots the ring, 1 event/s self-rearming stepper, auto-pause at the end, replay-from-end, clamped scrubbing). The replay rides INSIDE the lens: while active, the lens membership + observed relations switch source to the fold (node cache and include-fetch reused untouched), the executing glow / tool badges / spine read the fold, live spawn chrome stands down, and the on-air chrome flips to the amber `--sm-replay-c` (frame, REPLAY badge, transport pill) so the canvas never claims LIVE while painting the past. Zero new HTTP / CLI / schema surface.

- ✅ **Live lens scopes the whole workspace** (2026-08-14, user call): the lens stopped being a map-only mode. The files rail intersects its listing with the lens membership (one `filteredNodes` gate reading `LiveLensService.membership`, so the replay narrows the rail as the tape advances for free) and grows a lens-specific empty state, checked BEFORE the filters one because an empty list there means "nothing executed yet", not "your facets are too narrow". The Queue tab is disabled for the duration (`aria-disabled` + dimming rather than the native `disabled`, so the control stays focusable and its tooltip can explain why) with the guard in `openSection` covering the tab, the collapsed activity-bar twin and the roving arrow walk in one place; entering the lens with the queue open falls back to files, and leaving does not yank the panel back. Rationale: the queue is a different, unrelated timeline whose live rows would read as part of the execution story the lens narrates.

- ✅ **Replay tape persisted, operator-owned** (2026-08-14, user call: localStorage instead of the deferred server journal): the recorder mirrors its tape to `sm.live.recording` (debounced 2s, never per frame, since localStorage is synchronous) and hydrates it before subscribing, so the replay survives a reload and spans visits. Nothing ages it out; deletion is entirely the operator's, through a Settings > Project readout row (`N events, X KB stored` + Delete) and a shortcut in the replay bar, both calling `ActivityRecorderService.clear()` with no confirm dialog (regenerable machine data, Activity clear-all posture). Because the operator decides when to delete, the mirror is DOUBLE-bounded, most recent wins: 4k events and 1M UTF-16 characters, trimmed oldest-quarter-first, with a halve-once retry and a session stand-down on a quota failure. The bound is the load-bearing part: the ~5 MB origin quota is SHARED with every other `sm.*` key (node positions, viewport, curation, rail state) and they all swallow write errors, so an unbounded tape would surface as unrelated features quietly forgetting state. A corrupt or off-shape stored value is dropped rather than repaired. Deliberately NOT closed by this: activity from before the page opened still never reaches the tape, which remains the deferred server-journal's job.

- ✅ **Sessions rail tab + per-session replay** (2026-08-16, user request; probe 2026-08-15 confirmed the current Claude runtime spawns exclusively async, so `childOwner` closes the parent-child chain with unique ids): the workspace rail grows a third tab, Sessions, listing every runtime session the recorder's tape witnessed, one row per conversation, each expandable into its agent tree with per-node stats (first/last timestamp, event count, touched paths). The index is a NEW pure fold (`computeSessionIndex`, `ui/src/services/session-index.ts`), sibling of the playback fold, because that one deliberately drops spawn owners; roots derive STRUCTURALLY (a spawn's `parentOwner` with `parentNodePath` absent, plus activity owners never claimed as `childOwner`, which covers spawn-less providers like antigravity), activity frames attribute to the LATEST spawn claim at or before their timestamp (re-spawned ids resolve temporally), and everything a trimmed tape cannot place lands in an explicit unattributed drawer instead of being guessed (the ring drops oldest-first, exactly the spawn frames that establish parenthood; a trim-created false root is accepted and surfaced via the trim notice). Play on a row (or an agent sub-node) re-filters the tape at gesture time (`filterTapeForSession`) and enters the lens + a SCOPED replay through a new `SESSION_REPLAY_INTENT` (the `MAP_ISOLATE_INTENT` pattern), riding `ActivityPlaybackService.enter(events?, scopeLabel?)`, whose delete-recording auto-exit keeps watching the RECORDER so a scoped replay stands down with the real tape; the transport bar shows the scope as a chip, and toggling replay off/on afterwards re-enters UNFILTERED by design. The tab stays ENABLED during the lens (it is the lens's front door, the exact opposite of Queue) and is always visible with its own empty state. Zero new HTTP / CLI / schema surface (one telemetry prose addition: `ui.feature.sessions`).

- 🧭 **Principle sealed: design evaluation, not observability** (2026-08-16, user decision; now in `README.md` §Philosophy and AGENTS.md §Project invariants): the live arc answers "is my design working?", never "is my system healthy?". The evaluative artifact it points at is the design-vs-reality diff shaped as FINDERS (a declared link never observed firing; an observed relation with no declared path), so divergence surfaces as a proposed design edit the human curates, not as a metric. Latency / token / cost dashboards are out of scope by decision, not by omission.

- ✅ **Session journal + emergent-use detector** (2026-08-16, the sealed principle's first shipped artifact): `sm serve` now persists each runtime session's RESOLVED activity frames (the same wire payloads the WS broadcasts, minus the boot-scoped `stats` / `pairCount`; content-free by construction) as one JSON file per session under `.skill-map/sessions/` (`spec/schemas/session-recording.schema.json`, the single format: journal today, export/import later; fifth Storage-rule home in `spec/architecture.md` §Storage rule, gitignored, retention 50 files / 20 MiB, debounced ~2s writes, finalize on session release / shutdown with one `activity.session-write` operations line, gate `activity.journal.enabled` default on, a NORMAL committable key because nothing content-shaped can land). At scan, the driving adapters fold the journal into observed relations (`kernel/session-journal`: MCP invocations correlated to their calling unit by owner, spawns with both resolved paths, once per spawnId; READS deliberately deferred as noisy) threaded via `IAnalyzerContext.observedRelations`, and the deterministic `core/observed-link-missing` analyzer emits one `info` issue per observed (source, target) pair no declared `invokes` / `references` link covers (match on `resolvedTarget` first; `mentions` / `points` do not declare execution; both endpoints must be scanned nodes). Rows group under an "Observed in sessions" sub-header in the inspector so they read as reality commenting on the design; fix = declare the link by hand (NO auto-fixer, user decision), dismiss = the standard `(analyzer, target)` sidecar suppression, durable. Recorded follow-ups: the findings-lane + fixer migration is the first customer of the open deterministic-findings-lane question (Phase F list below); the DEAD-DESIGN detector (declared-never-observed) stays deferred until a volume gate exists (only flag when the source executed enough); the reads relation stays deferred; journal finalization upgrades to exact boundaries when the SessionStart/SessionEnd hook surface lands.

- ✅ **Journal hydration + exact finalization + the spine fix** (2026-08-16, the follow-up batch executed in priority order right after the rework below): (1) the executing-spine dressing now keys observed pairs and the lens scan's link filter on `resolvedTarget ?? target`, matching the graph-edge convention, so trigger-style links (`@foo` authored form) dress and render on the lens (the long-standing queue item 9); (2) new `GET /api/activity/sessions` read-back (recordings + `skipped` basenames, map-views dialect, pending journal buffers flushed first) and the Sessions tab MERGES it with the client tape: journal-only sessions fold through the same `computeSessionIndex`, list with their rows / agents / steps, and replay by carrying the recording's own frames through `ISessionReplaySelection.sourceFrames` (the playback's empty-recorder stand-down became transition-based so an external-source replay survives an already-empty tape while the purge still kills every replay); (3) Claude's adapter wires `SessionEnd` to the node-less session-release form (codex main-`Stop` precedent), handing the journal its EXACT finalization boundary; `SessionStart` stays unwired by design (no session-start signal form exists, and the journal derives identity + start from the first frame).

- ✅ **Recording is a gesture: the sessions-arc UX rework** (2026-08-16, user decisions across one working session): capture became MANUAL, a Record session / Stop control atop the Sessions tab (the tape records ONLY between those gestures, never ambient; Real Time keeps the live map glow independently, and flipping it off stops an in-flight recording), and the Live lens lost its entire toolbar cluster: it now exists only as the face of RECORD or REPLAY (record start stamps the reset watermark so each recording narrates from its own moment; exiting a replay outside a recording takes the lens down). The linger-window feature (5 min / no-limit, `sm.live.lens-window`) was REMOVED outright, expiry machinery included. The shared `sm-session-record-control` carries three mirrored faces (Record red / Stop recording red / Stop replay in the saturated amber `--sm-replay-c-bright`, each stop with its dot + status hint gated on frames actually advancing) and mounts stop-only above the Files search. The session rows retitled by USAGE (touched names, time first) with the runtime session id as a 5-char code chip replacing the synthetic "Session N" everywhere user-visible (it correlates with the journal filenames; ordinals survive only in testids); each context lists its INTERNAL STEPS as narrow rows (unit runs, MCP calls, reads; custody / lifecycle claims and ends excluded) that deep-link into the replay landing PAUSED on the exact frame; the transport bar went three-row fixed-width (timeline / controls / caption with wall-clock + elapsed stamps) after the caption-resize scrubber bug, the ticker NEVER goes blank (field find 2026-08-16: node-less custody frames narrate too, `turn ended` for a `turnEnd`, a generic `session signal` otherwise, and a session-context spawn drops the dangling empty parent), and a paused replay freezes every execution animation via `animation-play-state`. Sessions paginate past 100 rows in the Queue dialect. Deleting the recording is ONE confirmed gesture over BOTH memories: the browser tape and the project journal via the new `DELETE /api/activity/sessions` (open buffers discarded, `activity.sessions-clear` op line), with the dialog naming the observed-relations evidence cost.

- ✅ **The gesture rules the disk too: journal capture gated server-side** (2026-08-16, user decision closing the "is SessionEnd enough?" question, option 2 of 2): the server journal no longer captures ambiently. `ActivityJournal` holds a `recordingActive` gate (boot OFF, survives page reloads because it lives in the server process), toggled by the new `POST /api/activity/sessions/recording` (`{recording: bool}` in, EFFECTIVE state out: `activity.journal.enabled` stays the master switch, off means the toggle refuses to engage; stop finalizes every open session, the toggle itself logs nothing). The UI Record / Stop control mirrors the gesture (`setSessionRecording` on the data-source port), the `GET /api/activity/sessions` envelope reports `recording` so a reloaded page RESUMES the local capture when the server is still recording, and a boot probe stops an orphan server recording the client cannot honour (Real Time off). Accepted cost, stated in `spec/architecture.md`: the `core/observed-link-missing` evidence covers only recorded windows, consistent with the design-evaluation posture (the operator points the instrument deliberately; nothing observes them ambiently). Field-found fix from the first real recording (2026-08-16): the parent's and child's hook processes race, so a child's sticky claim can beat the `handoff` frame declaring its `childOwner`, which split one conversation into two journal files (a 1-frame orphan plus the real session); the streaming fold now runs LATE-CLAIM ADOPTION (spec §Session journal · Session grouping): the claiming session absorbs the mis-rooted orphan, merging frames chronologically and unlinking any already-flushed orphan file, mirroring the client index's earliest-claim fallback.

- ✅ **Reopenable finalization: the cross-provider journal review** (2026-08-17, user decision, option 1 of 3): the provider parity sweep after the claude `SessionEnd` wiring found that the other three adapters emit their session release PER TURN (codex's main `Stop` → `sessionScope` end, Antigravity's fully-idle `Stop` and opencode's `session.idle` → root `ownerScope` end), because those wire forms mean "everything is idle NOW" for the live map, only claude's `SessionEnd` is a true terminal; the journal reading them as "the conversation is over" fragmented one multi-turn recording into one file per turn, and the Sessions tab's per-rootOwner dedupe then hid all but one fragment. Fix at the journal (spec §Session journal · Finalization): finalization is REOPENABLE within one recording window; finalized sessions park in a `maxFiles`-bounded map with their owner attributions retained, a frame attributed to a parked root revives it onto its SAME file (`endedAt` re-stamped at the next release), a session-named ownerless frame reopens by id, and the late-claim adoption also absorbs a parked orphan whose terminal end raced the handoff. The park dies with the window (stop / shutdown / clearAll), so a new gesture starts fresh files. Wire spec and adapters untouched by design (option 2, a final-vs-idle wire flag, rejected as more moving parts; option 3, client-side fragment merging, rejected as papering over the storage). Parity otherwise clean: `detail` per adapter + resolver-stamped `access` feed the steps for all four providers, and the spawn/adoption machinery is provider-agnostic.

- ✅ **Dead-design detector: `core/observed-link-dead`** (2026-08-17, closing the follow-up the journal entry above recorded as "deferred until a volume gate exists"): the second direction of the design-vs-reality diff. The journal fold (`foldObservedActivity`, renamed from `foldObservedRelations`) now ALSO produces per-node **observed executions** (unit-run counts: starts with a node and no resource access; `keepAlive` custody excluded, sticky agent spans count once per claim), threaded as `IAnalyzerContext.observedExecutions` beside the relations. The analyzer emits one `info` issue per declared `invokes` / `references` link that clears three honesty gates (normative in `spec/provider-activity.md` §Consumption): OBSERVABILITY, the resolved target is an `mcp://` node or an `agent`-kind node, the fold's two evidence classes (a link whose only honest firing would be a READ cannot be judged while reads stay unfolded, so docs never flag); VOLUME, the source executed ≥ 3 times across recorded sessions (`MIN_SOURCE_RUNS`, a constant by greenfield posture, no config knob until demanded); ABSENCE, the pair appears in no recording. Anchored on the source, `data.target` drives the standard (analyzer, value) dismissal, NO auto-fixer (mirror decision), and rows join the existing "Observed in sessions" inspector group (now a two-analyzer set). Verified by execution against a scratch scope: fires at 3 runs, silent at 2, silent when the pair was observed. Built-ins 70 → 71, analyzers 27 → 28.

- ✅ **The reads relation joins the fold** (2026-08-17, lifting the journal entry's "reads deferred as noisy" with the noise moved into ANALYZER gates instead of dropping the evidence): `foldObservedActivity` folds a third class, `relation: 'reads'` (an `access: 'read'` start correlated to its reading unit by the same owner correlation the invokes class uses; the read path never becomes the owner's current unit). The unit correlation is TURN-BOUNDED (user call in the same review: "why would a read I asked for two turns later count as the skill's?"): a `turnEnd` clears its owner's current-unit claim, the journal's discrete mirror of the live TTL decay, so later-turn accesses attribute to nothing rather than to a stale unit; subagents have no turn boundary, their whole span is their own work. Consumption is asymmetric by design (normative in `spec/provider-activity.md` §Consumption): `observed-link-missing` holds read pairs to a stricter standard on BOTH sides, a REPETITION gate (`MIN_READ_OBSERVATIONS = 3`, reading is routine where executing is deliberate) and wider coverage (`points` also covers a read pair, a backtick path naming the file already declares it matters; `mentions` never covers anything), while `observed-link-dead` widens its observability gate: a `references` link toward ANY scanned target is now judgeable (every scanned node can be read) and an observed read CONFIRMS it, while `invokes` links still require an mcp / agent target (unit-to-unit execution pairs are not folded). Validated on the real fixtures/claude recordings: the 6 observed reads of `docs/STYLE.md` fold, stay silent for missing (declared), confirm the reference as alive for dead, and the issue list stays exactly the one deliberate dead-design demo, zero new noise.

- ✅ **Node-grain dead detector + the evidence is sacred** (2026-08-17, user insight while sizing the volume gate: "la evidencia son los JSONs y los estamos eliminando"): third design-vs-reality analyzer, `core/observed-node-dead`, one `info` per RUNNABLE node (kinds `skill` / `agent` / `command`; docs and virtual nodes never judged, they do not execute; external-provider kinds stay silent until they earn an entry) with ZERO recorded runs once at least 20 ACTIVE sessions accumulated (`MIN_ACTIVE_SESSIONS`, the user's number; `foldObservedActivity` now returns `executions: { byPath, activeSessions }`, the denominator counting only recorded sessions that produced unit runs, since a quiet recording proves nothing). `data.target` carries the node's own path for the standard suppression; same "Observed in sessions" inspector group (three analyzers now). And because the journal files ARE the accumulated evidence those gates count on, the replay transport's trash was SPLIT off the both-memories purge (superseding the 2026-08-16 erase-as-one): it clears the browser tape only, no confirm (regenerable, the Sessions tab re-hydrates from the surviving journal), while the full tape-plus-journal wipe stays in Settings behind the warning that names the cost. Built-ins 71 → 72, analyzers 28 → 29. Follow-up landed the same day (user call: "debería ser configuración del plugin… los otros también"): the three volume / repetition gates became per-extension INTEGER SETTINGS with the shipped values as defaults, `min-active-sessions` (20) on observed-node-dead, `min-source-runs` (3) on observed-link-dead, `min-read-observations` (3) on observed-link-missing, each min 1, declared via the closed input-type catalog so `sm plugins config core/<analyzer> <setting> <n>` and the Settings form come for free; verified end-to-end (gate 25 over 20 recordings silences, gate 5 flags). The retention question CLOSED the same day (user pick, option 1 of the evidence-window discussion): both ceilings became project-config keys beside the master switch, `activity.journal.maxFiles` (default 50) and `activity.journal.maxTotalBytes` (default 20 MiB), read once at serve boot with off-shape values falling back to the shipped defaults; the spec §Retention now states the coupling explicitly (keep `maxFiles` >= the largest `min-active-sessions` in use or that gate can never be met). Deliberately PROJECT config, not extension settings (the user first imagined them inside the analyzers): retention is enforced by the journal WRITER, shared by all three readers, and must hold even with every analyzer disabled. The unreachable-gate warning was scoped in a follow-up discussion (2026-08-17): it will land as an `sm plugins doctor` check row (warn when an enabled observed-* analyzer's `min-active-sessions` exceeds `activity.journal.maxFiles`) WHEN the trio graduates from experimental; a deferral comment marks the spot in `observed-node-dead`.

- ✅ **Capture-level ladder: one live knob for how much skill-map sees** (2026-08-17, the design conversation reshaped twice by measurement and the user's intent): the operator moves ONE cumulative selector, `executions` (1, the mandatory floor: every other class correlates to the units it establishes) < `reads` (2) < `writes` (3) < `mcp` (4, THE DEFAULT, the historical full surface) < `shell` (5, RESERVED: no capture yet, install-side opt-in when it lands), applied LIVE at the ingest seam so Real Time and every recording see the same truth (frames above the level drop BEFORE stats, run history, conversations, journal and broadcast). The original install-time depth knob DIED by measurement: the per-event bridge cost is ~20 ms on the reference machine (Node 26 boot), cheap enough that the hooks always install their full surface, so the perf half of the two-knob model had no case (the user's instinct, "llamar al server directo en vez de spawnear", triggered the measurement; the node bridge stays, portability + invisibility invariants beat the ~15 ms a curl variant would save). Pieces: adapters stamp `access: 'write'` on write-shaped tools (Claude `Write`/`Edit`, opencode `write`/`edit`, codex `apply_patch` and antigravity `write_to_file`/`replace_file_content` since 2026-08-18), the resolver honours the hint, `CaptureLevelState` + the ingest gate, `POST /api/activity/capture-level` persisting the project-LOCAL `activity.captureLevel` key (read at boot), the `GET /api/activity/sessions` envelope reporting it, each recording stamping its MINIMUM level (honest provenance for the observed-* absence gates; per-class gate refinement stays deferred with the trio's graduation), and the UI's `sm-capture-level-selector` beside Record with a Settings > Project mirror (shell rendered but disabled). The Settings mirror's shell-unlock line is LENS-CONDITIONED (user report 2026-08-18: it hardcoded the claude command on every lens): the install-status envelope gained `shellOptIn` (does the descriptor carry the opt-in event), the shared `ActivityReadinessService` probe exposes the active lens id + that capability (re-probed on a confirmed lens switch), and the row renders the command for the CURRENT lens, an "unavailable on this lens" line for providers without the rung, or nothing while the probe is unresolved (never a wrong provider). The Grep "rung 4.5" stays out of v1: the tool_response shape is still unverified (probe blocked, this harness ships no Grep tool) and Bash-only searchers fall to rung 5 regardless. Field follow-up the same day: the level LOCKS while recording (user call after trying a mid-recording move: "no se va a saber si se está cambiando o no"), the POST refuses answering the unchanged level (the recording-toggle refusal dialect), both UI selectors disable with an explanatory tooltip, and the depth is chosen BEFORE Record, keeping each recording's stamp single-valued in practice (the minimum rule stays as defense). And each session row now TAGS the level its recording was made under (user request, same session): a dim mono chip at the title's right edge fed by the journal stamp (looked up by rootOwner, so tape-native rows tag too; no stamp, no tag).

- ✅ **The recording intro note: content-free, said out loud** (2026-08-17, user request: recording must never read as "we are storing your work"): a one-time dismissible callout above the Record control (findings-info dialect: info glyph, muted box, inline "Got it") whose copy LEADS with what is NOT captured (no prompts, no file contents, no results; replay re-animates motion, the work cannot be read back). Dismissal persists MACHINE-WIDE, the new `ui.dismissedNotes` list in `~/.skill-map/settings.json` (the documented `$HOME` exception grows its second consumer): `user-settings.schema.json` + the store's `readDismissedNotes`, carried by the `GET`/`PATCH /api/preferences` envelope (whole-list replace, deduped server-side), and a generic `DismissedNotesService` client-side (notes render only after a POSITIVE "not dismissed", so a slow or failed preferences fetch never flashes a note the operator already closed). Future one-time notes reuse the same list and service.

- ✅ **Shell capture: rung 5 stops being reserved** (2026-08-17, user ask "y como activo el shell?"): the ladder's fifth position is real now, behind a DOUBLE opt-in. Install side: `sm activity install claude --shell` persists the project-LOCAL `activity.shellCapture` key and renders an extra `PreToolUse` hook with matcher `^Bash$` (`--no-shell` retires both; a bare re-install respects the stored choice because the engine reads the key itself, and the status envelope's `events` count reports the RENDERED surface, 7 without / 8 with). Runtime side: the `shell` selector position stays disabled and `POST /api/activity/capture-level` refuses `level: 'shell'` while the key is off (the sessions GET envelope carries `shellCapture` so the UI knows). The mechanism is the new `optIn: 'shell'` field on `IActivityInstallEvent`, filtered at render; wiring it surfaced a latent merge defect fixed at the source: `mergeActivityHooks` deduped per EVENT NAME, so a second spec under `PreToolUse` was silently swallowed, and the wired-already probe now keys on spec identity (event + matcher). A shell frame is a HEURISTIC path sighting, not evidence: the claude adapter's `mapShellUsage` extracts `.md`-shaped tokens out of `tool_input.command` (quotes stripped, relative tokens resolved against the session cwd, home-anchored and URL-shaped tokens dropped, deduped, capped at 5), each survivor becomes `{ path, detail: 'Bash', access: 'shell' }`, and the COMMAND TEXT itself never leaves the parser. Rank 5 at the ingest gate, so any lower level filters the sightings; the journal fold ignores `access: 'shell'` for observed relations (sighting ≠ read evidence), pinned by test (reversed 2026-08-30: the fold now admits `shell` as `reads`). Claude-only v1: codex / antigravity / opencode declared no shell event, documented as such (codex JOINED 2026-08-18, see the codex bullet below: 0.147 reports its shell tool as `Bash` with the claude payload shape, so the mapper was promoted to the shared adapter util and codex's descriptor carries the same opt-in event). Follow-up the same day (user decision after a field surprise: a bare UI re-install silently re-armed the rung from the stored key): `sm activity uninstall` of the provider owning the opt-in event RETIRES `activity.shellCapture` (re-consent is explicit after any uninstall), retirement demotes a persisted `captureLevel: 'shell'` to the default `mcp`, and a running serve self-heals the live cell on its next session-journal read. Verified by execution end-to-end in a `.tmp/` scope: install renders / retires the matcher, the POST refuses then accepts, a real Bash ingest naming a scanned `.md` lands `access: 'shell'` in the recording with the `captureLevel: 'shell'` stamp, and the same ingest at level `mcp` never reaches the journal. Field-audit corrections (2026-08-18, both found by executing the surface): (1) the BFF stats accumulator was still COUNTING shell sightings as executions (a Bash command naming a doc inflated the node's `count`), contradicting the rung's "sighting, not evidence" contract; `ActivityStatsService.record` now short-circuits `access: 'shell'` into a typed recent-log entry only (both ends, `kind: 'shell'`, no count / owners / lastStartAt mutation, no stats enrichment on the WS frame), and spec §Execution stats names the exclusion explicitly alongside the completed recent-`kind` enum (`mcp | read | write | shell`) (REVERSED 2026-08-30: shell sightings count again, see the "Shell sightings count on the card" bullet above; they fold as `reads` as well, only the typed `kind` stays). (2) The opt-in WRITERS were provider-agnostic: `sm activity install codex --shell` happily persisted `activity.shellCapture` (unlocking the ladder's shell selector with zero capture wired) and codex's uninstall never retired it (retirement is keyed on the descriptor's opt-in event); both writers (the CLI flag pair and the HTTP body's `shellCapture`) now refuse a provider whose descriptor carries no `optIn: 'shell'` event, through the shared `providerOwnsShellOptIn` predicate the uninstall retire path already used.
- ✅ **Codex markdown writes: the `apply_patch` hook surface, live-characterised** (2026-08-18, user field report "Codex me menciona que está usando archivos y no los veo en el recording"): the report itself was the DOCUMENTED read gap (codex hooks still never fire for the internal `read_file`, upstream openai/codex#18491 re-verified open; a model-initiated SKILL.md read is invisible on this runtime, the explicit `$skill` grammar remains the way a skill lights), but re-probing the surface (codex 0.147, `codex exec` against a `.tmp/` scratch with a catch-all logging hook) found that upstream HAD shipped `apply_patch` hook events (openai/codex#18391, April 2026) after our 2026-06-30 characterisation: `tool_input.command` carries the patch envelope, whose `*** Add File:` / `*** Update File:` headers NAME the targets, deterministic, no inference. The codex adapter now widens its PreToolUse matcher to `^(spawn_agent|apply_patch|mcp__.+)$` and maps each named `.md` target inside the scope root to a PATH signal with `detail: 'apply_patch'` + `access: 'write'` (deduped, capped at 5; `*** Delete File:` skipped on purpose, a rename lights only the old path), through the new shared `scopeRelativeMarkdownPath` kernel helper (promoted from the claude shell mapping so the two path resolutions cannot drift). Probing also surfaced an operational gate worth knowing: codex 0.147 requires explicit HOOK TRUST (the `/hooks` TUI review flow, per-hash), so a fresh install fires nothing until trusted, which `sm activity status --verify` exposes; and codex now reports its shell tool as `Bash` (claude's convention), which the matcher already excluded. Spec codex row updated with all three facts; verified end-to-end by replaying the real captured payload through serve + ingest (the journal lands `access: 'write'`, `detail: 'apply_patch'`). Same day, follow-up on the `Bash` finding (user ask after the read-gap explanation): the SHELL RUNG joined codex too. `mapShellUsage` was promoted from the claude adapter to the shared util (`mapShellInvocation`, exactly like `scopeRelativeMarkdownPath` before it), codex's descriptor gained the same `optIn: 'shell'` `^Bash$` event, and every provider-neutral gate built that morning lit up on its own: the `--shell` writers accept codex, the lens-aware Settings line shows `sm activity install codex --shell`, the ladder / journal-fold / stats-sighting semantics apply unchanged. Verified END-TO-END against the REAL runtime (`codex exec` 0.147 in a `.tmp/` scope: install renders 7 events, the capture-level POST accepts `shell`, a live `cat docs/notes.md` lands `{ nodePath: docs/notes.md, access: shell, detail: Bash }` in a recording stamped `shell`). Side effect worth remembering: a shell sighting now partially covers codex's `read_file` blindness, a `cat`/`sed` over a `.md` lights the node even though the internal read tool stays hook-less.
- ✅ **Mapper digest: the half the wiring self-test cannot reach** (2026-08-19, external bug report against a drop-in provider plugin, contract in [`spec/provider-activity.md`](./spec/provider-activity.md) §Mapper digest): a third-party adapter cloned from a built-in one inherited the DONOR's payload vocabulary (tool names, `tool_input` key names) as a hidden dependency, and because `mapEvent` is total by contract it disclaimed 100% of the real runtime's events in complete silence. Every checkpoint an operator can reach reported success: `installed` on disk, exit 0 from the bridge, `202` from the ingest, `outcome: ok` in the operations log, and a green `--verify`, because the self-test's probe short-circuits BEFORE `mapEvent` by design and therefore proves transport only. Diagnosis required hand-instrumenting the bridge. The digest closes it for free: the ingest already computed the outcome that drives its observability log (`no-signals`, `unresolved`, `no-nodes`, `no-provider`), so a boot-scoped store now accumulates it per provider keyed by the event's SHAPE (outcome + hook discriminator + tool name + the KEY NAMES reachable to depth two, collapsed with a count, 32 shapes max) and `GET /api/activity/disclaimed` reads it back. `sm activity status --verify` gains `digest: { received, resolved, shapes }` on every `--json` entry, and in human mode prints the block ONLY when a provider received events and resolved none, the one reading with no innocent explanation (disclaiming next to a non-zero `resolved` is filter-first working, and printing it would train the operator to ignore the block). The digest never touches the exit code. Privacy is the load-bearing invariant and is asserted over the whole serialized entry in both specs: SCHEMA only, no payload value survives except the two vendor discriminators the ingest log was already permitted to log, arrays are never descended into, key counts and lengths capped. Verified end to end against a live `sm serve`: six events in a foreign vocabulary render `! mapper: 6 event(s) arrived, none mapped to a node` with `tool_input.path` / `tool_input.skill_name` named, which is the one-command diagnosis the original report spent a bridge-instrumentation session reaching. Deliberate non-goal (§Design evaluation, never observability): the digest answers "does the authored adapter speak this runtime's dialect", not "how is the system performing"; it carries no latency, cost or throughput, and dies with the process. Follow-up the same day: §Per-provider signal notes gained a **payload vocabulary** table (the shape half, split off from the pre-existing signal-by-signal half) naming, per provider, the event discriminator, tool-name key, argument container, containment root, session key, markdown tools, path key, and the skill / spawn / shell triggers. No row matches across all four providers and no field name is shared by all of them, which is exactly the hidden dependency a cloned adapter inherits wholesale; it is also the table the digest's reported key names are meant to be read against, so the two ship as a pair. Two traps the grid cannot express are called out in prose: a case-SENSITIVE install matcher written for another runtime's capitalisation never fires at all (indistinguishable from a disclaiming mapper from outside), and a RELATIVE path key must be absolutized before `relativizeMarkdownPath`, whose containment check is a string prefix, which is what `scopeRelativeMarkdownPath` exists for.
- ✅ **Antigravity writes + shell rung, live-characterised** (2026-08-18, the day's third adapter probe, "luego sigamos con antigravity"): re-probing agy 1.1.14 headless (`--print` + `--dangerously-skip-permissions`, from a scratch scope with its own `.git`, workspace hooks only load for a TRUSTED folder, `trustedWorkspaces` in the CLI settings, the codex-trust analogue, inspectable via the print-mode `/hooks` answer) closed the July deferral: the write tools are `write_to_file` (new files) and `replace_file_content` (edits), BOTH with an ABSOLUTE `toolCall.args.TargetFile` (the old catalog guesses `create_file` / `edit_file` never fire), and `run_command` carries `CommandLine` plus an explicit `Cwd`. The adapter maps writes always-on (rung 3, `access: 'write'`, tool name as `detail`) and the shell rung as the same `optIn: 'shell'` double opt-in as claude / codex (`^run_command$`, `detail: 'run_command'`), through the new shared `shellCommandMarkdownPaths` multi-root core (tokens absolutize against the command's own Cwd, containment against `workspacePaths[*]`; claude / codex collapse both onto the session cwd). With antigravity aboard, opencode (plugin-file, structurally event-less) was briefly the only shell-rung-less provider, and the refused-provider fixtures moved onto it. Verified END-TO-END against the real runtime: `sm activity install antigravity --shell` renders 3 events, a live `agy --print` run lands `docs/notes.md · shell · run_command` and `docs/notes.md · write · replace_file_content` in a recording stamped `shell`, and the conversation Stop finalizes the session.
- ✅ **OpenCode shell rung: the plugin-file opt-in dialect** (2026-08-18, the day's fourth adapter, closing the matrix): opencode's `bash` tool was live-characterised on 1.18.9 (`tool.execute.before`, command in `output.args.command`), and the probe surfaced a POSTURE gap worth fixing on its own: the generated plugin forwarded EVERY tool's before event unfiltered, so bash command lines already left the host process (loopback, then discarded at `mapEvent`), which is exactly what the double opt-in exists to prevent. The fix is the opt-in mechanism itself: the `pluginHooksSource` now parameterizes a wiring-level filter on the new `{{SHELL_ON}}` placeholder (`core/activity/plugin-template.ts`), resolved from the stored key at install render, so bash payloads stay in-process until `sm activity install opencode --shell` re-renders the plugin; once opted in, `mapBashUsage` runs the shared shell grammar against the plugin context's `directory` (`access: 'shell'`, `detail: 'bash'`). `providerOwnsShellOptIn` derives the plugin-file capability from the placeholder's presence (one source of truth, no parallel flag), which also makes uninstall retire the key for opencode automatically. With that, ALL FOUR built-in activity providers own the rung; the refusal path has no built-in left to exercise it, so it is pinned at the engine level with synthetic shapes (json-hooks without the event, plugin-file without the placeholder) and the CLI / BFF specs pin the accept behaviour per dialect instead. The demo tripwire now derives `shellOptIn` through the REAL predicate rather than the events list. Verified END-TO-END against the real runtime both ways: opted in, a live `opencode run` `cat docs/notes.md` lands `docs/notes.md · shell · bash` in a recording stamped `shell` (session.idle finalizes); opted out, the re-rendered filter reads `!false` and the same run leaves zero frames.

- ✅ **The demo replays sessions** (2026-08-17, user ask "que la demo también tenga la sección de Session"): the public demo's Sessions tab ships ONE curated recording of the demo fixture (a `/publish` run: command → check-links → content-editor spawn reading STYLE.md → notion-publish hitting `mcp://notion`; a second shorter brief was cut same day, one strong story beats two) and replays it on the map. Replay is pure client-side theater over recorded frames, so it is the one live-arc feature a static bundle can honestly deliver; the mechanism is `LiveLensService.replayAvailable`, deliberately WIDER than `available` (which stays `mode !== 'demo'` and keeps Record / Real Time honestly disabled): `setActive` admits a demo enter only while a replay is in flight, and `replaySessionFromTape` enters playback BEFORE flipping the lens on. The curated asset lives at `web/scripts/demo-sessions.json`, is copied into the bundle as `sessions.json` by `build-demo-dataset.js`, and is guarded from src (the only workspace with AJV) by `src/__tests__/demo-sessions-asset.spec.ts`: schema-valid against `session-recording.schema.json` AND every referenced path exists in `fixtures/demo/` (hand-authored data rots silently on fixture reorganizations otherwise). Verified by execution: Playwright drive of the freshly baked bundle, the curated session listed, Play enters the lens, spawn edge + reads animate, transport narrates with the session-title chip. The public site's hero frames that same replay chrome-less (2026-09-02, user ask "que sean reales las cards", "no tener nada visible solo las cards"): `/demo/?embed=1&replay=main:demo-publish&theme=neon-blue` under the boot-only `embed` flag (spec §"Embedded replay": canvas alone, looping playback, plain wheel left to the host page, a card click opens the full SPA on that node, an `embed` storage namespace and no persisted theme so the framed visit never touches the visitor's own settings). The hand-drawn SVG simulator with invented token counts and the screen-capture "single screen" section it replaced are gone; what the landing shows IS the map. `/demo/` itself is unchanged.

- ✅ **Browser storage namespaced per project, versioned, no retrocompat** (2026-08-17, field find: sessions recorded in `fixtures/claude` surfaced while serving `fixtures/demo`, because `localStorage` is per-ORIGIN and every locally served project shares `127.0.0.1:<port>`): `sm serve` stamps the RESOLVED scope root (realpath) into the served `index.html` as a `skill-map-scope` meta (`static.ts` injects on `/`, `/index.html` and every SPA deep link; assets stream untouched; spec `cli-contract.md` §Serve), and the UI suffixes its PROJECT-STATE keys (tape, node positions, viewport, node/folder expansions, map overrides, active view) with an 8-hex FNV-1a hash of it, synchronously at module load via `ui/src/services/scoped-storage.ts`. Operator PREFERENCES (rail width, sort modes, filters) deliberately stay bare, a preference follows the operator across projects. Two deliberate simplicities, both user calls: the hash is non-cryptographic (separates a handful of local folders, and `crypto.subtle` is async so it could not mint keys constructors read), and there is NO backward compatibility, with a PER-LAYOUT-CHANGE blast radius (two user refinements, same day): `sm.storage-version` stores the CLI VERSION that last wrote the origin (stamped into the HTML as a second meta, `skill-map-version`, by serve and the demo patch alike), and the gate applies every `VERSION_RESETS` semver threshold CROSSED by the upgrade ('all' or a list of base keys, scoped variants included); a release crossing no threshold wipes nothing, unknown territory (unversioned origin, unreadable version, downgrade) falls back to the full wipe (misreading state is worse than resetting it), and the namespace migration's own threshold ('1.12.0') declares 'all' since every bare-era key is unreadable. The dev harness stamps its own metas rather than sitting outside the gate (same-day follow-up): the raw `ui/src/index.html` carries a static `dev` pseudo-version (inert under a real serve, the BFF's injected meta precedes it in document order), and the `fix:*` dev flow generates a gitignored `index.dev.html` (`ui/scripts/stamp-dev-index.mjs`, composed via the `dev-stamped` build configuration onto `ng serve --configuration=local`) stamping the fixture's realpath as the scope, so the shared 4200 origin gets per-fixture namespaces and the cross-fixture tape/positions leak closes in the dev flow too; the e2e seeds stamp `sm.storage-version` alongside their keys (`e2e/_storage.ts`), because the gate honestly wiped the unversioned `rail-collapsed` seeds and took all 26 smoke tests down until repaired. Same-day UX fix in the selector: the locked `+Shell` position stopped being natively disabled (a disabled button swallows the pointer events its explanatory tooltip needs); it renders muted with `aria-disabled`, `onChange` refuses the selection re-syncing PrimeNG through the public CVA `writeValue`, and the tooltip names the fix, pointing at Settings > Project except when the selector already sits there. The `sm.scopes` registry key maps each hash to its root, a pure debug affordance so devtools readers can tell projects apart. Verified live: stale bare keys wiped, version stamped, state re-written scoped, prefs untouched, demo (no meta) on the `default` namespace.

- ✅ **The rc prerelease channel retired; releases go main-direct** (2026-08-17, user decision while draining the post-promote backlog): the dedicated `release/rc` branch died with the 1.11.0 promote (local branch deleted; remote deletion + the npm `rc` dist-tag cleanup are the operator's manual calls). Swept in one diff per the structural-change protocol: the `release` workflow lost the rc push trigger, the branch-side guard half and the pre.json id-subtraction in its fresh-changeset detection (the pre-mode-on-main guard REMAINS, it protects `latest` regardless), `CONTRIBUTING.md` §Release candidates became a retirement note pointing at git history for the runbook, and the `AGENTS.md` releases bullet now states the main-direct regime. Reviving the channel = recreate the branch from main + `pnpm changeset pre enter rc` + restore the swept pieces from history.

- ⏸ **Findings-lane migration of the observed-* trio, SCOPED and deferred** (2026-08-17, design conversation closed with "postergar hasta que tengamos el escenario", the trigger being the trio's graduation from experimental with real-usage evidence): the trio moves from ISSUES to FINDINGS through a new DETERMINISTIC findings lane, the general seam the open deterministic-findings-lane question asks for, not a trio special-case. Sketch agreed: the scan upserts findings DIRECTLY from analyzer emissions marked for the lane (no job-queue indirection, deterministic logic has nothing to queue; confidence 1.0, no job metadata, which needs a findings-schema evolution), deduped by (analyzer, node, target), superseding on re-scan, and AUTO-RESOLVING to `fixed` when the emission disappears (declare the missing link -> the finding closes itself with history, the lifecycle win issues cannot give). The rows LEAVE the "Observed in sessions" issues sub-header wholesale (one surface, not two). Fixers get REOPENED at that point as per-row human gestures (the earlier "declare by hand, no auto-fixer" call was about ambient auto-fixing): link-declare for observed-link-missing, link-remove for observed-link-dead, none for observed-node-dead (retiring a unit is judgment). Existing issue dismissals do not migrate, cheap while the trio is experimental. Until then the trio stays on the issues surface. Two field bugs from the split, found live and fixed the same day: the Settings recording row read only the browser tape, so an empty tape HID the journal files and disabled the very delete meant to erase them (the row now reads BOTH memories, `N events, X KB in this browser · M session files in the project`, delete enabled while either holds something, and the row gained its first dedicated spec); and a purge with an already-empty tape produced no tape transition for the Sessions tab to react to, so stale journal rows survived until an F5 (`SessionPurgeService` now bumps a `purgedAt` signal once the DELETE settles and the tab refetches from it). The trio then took FAMILY NAMING (user call 2026-08-17, option `observed-*`): `observed-link-missing` (unchanged), `observed-link-dead` (born `declared-link-unobserved`) and `observed-node-dead` (born `node-unobserved`), the prefix reading "verdict from observed sessions" and matching the inspector's "Observed in sessions" header plus the `observedRelations` / `observedExecutions` context fields; ids, directories, suppression keys, spec prose and pins renamed in one diff, no compat shims (nothing published). The trio was then marked `stability: 'experimental'` (user decision 2026-08-17): all three design-vs-reality analyzers ship DISABLED until the gates prove themselves in real projects, opt-in via the Settings toggle / `sm plugins enable core/<analyzer>` (default composition 29 -> 26 analyzers). A third field find closed the loop (the one-session trap: replay it, trash it, the row re-lists from the journal and the trash goes dead): the transport trash became SCOPED to the replay's SOURCE (`ActivityPlaybackService.source`, threaded at `enter()`), a tape-held session's replay removes THAT session's frames alone (`ActivityRecorderService.removeAll`, identity-based over `filterTapeForSession`'s output) and exits explicitly, the whole-tape replay keeps the historical clear, and a journal-sourced replay hides the trash outright, nothing of it lives in the browser; the user chose to KEEP the journal file in all replay-trash paths ("la voy a necesitar luego"), so per-session file deletion stays unbuilt. Same review retired the tab's "Unattributed activity" drawer outright (user call: "se debería omitir directamente"): the fold keeps its unattributed bucket (honesty about what it cannot place), but the UI stopped surfacing it, the trim notice already carries the caveat and the drawer read as noise.

Phase D (LLM optional layer, the `v0.8.0` milestone; shipped stable as `@skill-map/cli` 0.89.0 / `@skill-map/spec` 0.81.0 on npm `latest`, 2026-07-23):

- ✅ **10**, Job subsystem + first probabilistic extension (Extractors are deterministic-only). Phases 0–G shipped across the 0.88 rc cycle and promoted to stable in 0.89.0: the DB-only content-addressed queue, the full `sm jobs` family, `sm record` + nonce auth, the pull-only agent process, `ai-summarizer-action`, `sm doctor`, and `sm actions list / show`. See the Step 10 section below.
- ✅ **11**, Findings pipeline + finder analyzers (the LLM-verb set died with Decision #137; semantic capabilities ship as extensions, `sm findings` is the only surviving verb). COMPLETE and RELEASED: the whole arc (Decisions #137–145: finders + fixers over the pull-only queue, `state_findings`, the fixed / human-decision / dismissed lifecycle, supersede, auto-fix chain, finding-subset fix jobs, pair toggle) landed 2026-07-14 → 07-23 and shipped stable in `@skill-map/cli` 0.89.0. The cross-node second wave was DISCARDED (2026-07-23), see the Step 11 section.
- ✅ **16**, Web UI: the findings workbench (v1). SHIPPED in the same cycle and released in 0.89.0: the inspector findings placeholder became the decision tray (finder / fixer / standalone launchers, per-row fix / dismiss / resolve, hidden buckets, issue-row fixers, unified Findings card), backed by the shared submit machinery and live job events. Details in the Step 16 section.

Phase E (`v1.0.0`, surface + distribution), COMPLETE:

- ✅ **14.1–14.7 / 14a–14c**, Full Web UI (Hono BFF + transport, live mode + demo, polish & budgets). See the Phase A → v0.6.0 entry above; audit-verified DONE 2026-07-23.
- ✅ **17**, Web UI: LLM surfaces v2 (deeper). The semantic capabilities drive interactive UI flows through the finder / summarizer / fixer extensions (Decision #137; there are no per-capability CLI verbs to mirror). Queue inspector + findings management + settings & plugins page + settings-hierarchy viewer + connected agents driving the queue over MCP (SHIPPED 2026-07-23) + the WCAG AA accessibility pass (2026-07-24: app-a11y audit + remediation, `@angular/cdk` added; the flagged contrast + a11y minors closed 2026-07-26 via the automated axe-core + Playwright e2e sweep). The 5 named-flows UI and the cost dashboard were DISCARDED from 1.0 (redundant with the generic AI-actions surface / unverifiable self-reported tokens).
- ✅ **15**, Distribution polish (15a single package with UI bundled · 15b llms.txt + llms-full.txt docs surface, generated from spec/ on every deploy · 15c release infrastructure). The Astro Starlight docs site was DISCARDED from 1.0 (README + spec/ + the marketing site suffice; a formal docs site is post-1.0 if a plugin-author audience grows).

Deferred to Phase F (post-v1.0):

- ✅ **12**, Additional Formatters. Mermaid + DOT ship as built-in `core/` formatters and the filtered subgraph export runs through `sm export <query> --format mermaid`. Verified by execution 2026-08-02: `sm graph --format` answers for all four of `ascii` / `json` / `mermaid` / `dot`. The export format catalog stays deliberately closed at `json` / `md` / `mermaid`.
- 🔮 **13**, Multi-host Providers (Copilot; generic frontmatter-driven Provider). Codex + agent-skills + Antigravity already landed during the active-lens migration, and the Codex body extractor (TOML `developer_instructions` field → markdown / at-directive / slash pipeline) shipped via `read.bodyField`; Copilot + a generic Provider are the remaining post-1.0 pieces. The legacy Gemini Provider shipped at Step 9.7 and was retired in 2026-05.
- ✅ **Prose-to-rules pair** (2026-08-08, user request; the first extension addition after the July queue drained): `core/ai-prose-to-rules-analyzer` finds enumerable rules buried in narrative paragraphs and proposes them as a paste-ready checklist (the finding `detail` IS the extraction), `core/ai-prose-to-rules-action` applies the conversion. GRADUATED stable/enabled the same day after the live playground pass (the throwaway fixture's 8-rule paragraph converted faithfully, rationale preserved as parentheticals, the narrative control section untouched). `ai-structure-analyzer` ceded the prose-should-be-a-list territory in the same revision (its axis narrows to placement / ordering / hierarchy), with both prompts drawing the boundary in positive terms per the no-cross-sibling-deferrals ruling (2026-07-14).

> 🔀 **Execution order**: between v0.5.0 and v0.8.0 the build order diverges from numeric Step order. Steps keep their stable numbers (so commits, changesets, and citations don't churn), but the actual sequence is: Step 14 (Web UI) executes immediately after v0.5.0 and ships v0.6.0, then wave 2 (Steps 10 → 11) resumes and ships the v0.8.0 milestone (realised as `0.89.0` stable, 2026-07-23). Steps 12–13 follow. Rationale: validating the deterministic kernel end-to-end against a real UI before adding LLM cost / probabilistic surfaces. See Decision #118.

### Step 10, Job subsystem + first probabilistic extension (wave 2 begins)

> ✅ **COMPLETE**, released stable in `@skill-map/cli` 0.89.0 (2026-07-23).

Opened wave 2 (probabilistic extensions) over the deterministic half: the
content-addressed queue (`state_jobs` + `state_job_contents`), the
kernel-enforced preamble, the `sm jobs` verb family, `sm record` + nonce auth,
`sm doctor`, `sm actions list / show`, and the first probabilistic Action
(`ai-summarizer-action`, universal). Two decisions from this Step still govern
the design: storage is **DB-only** (no `.skill-map/jobs/*.md` artifacts,
rationale in [`spec/db-schema.md`](./spec/db-schema.md)), and the queue is
**pull-only** (`sm jobs run` / `RunnerPort` shipped and were removed
2026-07-13; skill-map never invokes the agent, external agents claim and
record).

Phase-by-phase landing narrative (Phases 0 through G):
[`context/roadmap-history.md`](./context/roadmap-history.md#step-10-job-subsystem--first-probabilistic-extension-wave-2-begins).

### Step 11, Findings pipeline + finder analyzers

> ✅ **COMPLETE and RELEASED** in `@skill-map/cli` 0.89.0 / `@skill-map/spec`
> 0.81.0 (2026-07-23, promoted from the rc.0 → rc.4 prerelease cycle).

Wave 2 under Decision #137 (semantic capabilities are extensions, not verbs).
Shipped: `state_findings` + the findings envelope, Analyzers joining the queue,
the `sm findings` lifecycle (fixed / human-decision / dismissed, supersede,
prune), three finder+fixer pairs via Modelo B (redundancy / contradiction /
incoherence), the deterministic-analyzer fixers (`ai-reference-action`,
`ai-name-action`), the opt-in `core/auto-fix` chain, the pair toggle, and the
`ai-<subject>-<kind>` naming convention.

Standing rules born here (rationale in the Decision log): dismissal is durable
suppression in the node's `.sm` sidecar at the `(extension, type)` grain
(#144), issue dismissal keys on the verbatim value instead (#146, ENFORCED BY
THE KERNEL since 2026-08-09: the orchestrator's analyzer pass drops every
dismissed value-carrying issue, so any deterministic analyzer is dismissable
without suppression code of its own; `core/reference-broken` keeps its inline
check only to skip the confidence penalty), the
project-level `ignored-references` match-list settles dead-by-design targets
(#148), a submit with no processing skill installed is refused (#145), and TTL
is opt-in (#139). Full arc, including every dated refinement:
[`context/roadmap-history.md`](./context/roadmap-history.md#step-11-findings-pipeline--finder-analyzers).

**Wave 2, cross-node: DISCARDED** (user call 2026-07-23). The queue stays
strictly per-node (`extension + node + contentHash`); no cross-node job model
is planned.

### Step 16, Web UI: the findings workbench (v1)

> ✅ **SHIPPED and RELEASED** in `@skill-map/cli` 0.89.0 (2026-07-23) at the
> workbench scope: the inspector findings card with finder / fixer / standalone
> launchers, the two-state Detect/Fix button + Automatic toggle, per-row fix /
> dismiss / resolve, hidden buckets, issue-row fixers, and the unified Findings
> card.

Findings are a **bandeja de decisiones**, not a report: the UI's job is to let
the operator DECIDE per finding. Delivery was deliberately piecemeal, each piece
usable on its own. Landing narrative for the shipped pieces:
[`context/roadmap-history.md`](./context/roadmap-history.md#step-16-web-ui-the-findings-workbench-v1).

**Later pieces**, re-checked against the running code 2026-08-02. Two of the five landed and one was superseded, so what is actually left is:

- **The `/findings` global page** as a decision inbox (human-decision first, then fresh by severity, fixed/stale collapsed with honest counts, CLI-mirror filters, deep-link to inspector). Not started: the SPA still routes only `''` and `intentional-fail`, and findings live exclusively in the inspector section.
- **Node-card decoration** (fresh-findings counts by severity + a "needs you" badge for pending human-decision; BFF decoration per the `isFavorite` precedent, NOT a catalog slot; no invented aggregate scores). **Half-built**: the BFF already serves `findingCounts` on the node routes, but no UI component consumes it, so the wiring is what remains.

Already shipped, previously listed here: per-finding decisions in the UI (resolve / dismiss / the human-decision answer surface, in `inspector-findings-section`) and the queue surface (`queue-view`, mounted as a workspace section rather than the sketched "minimal chip"). The **summary card** was superseded, not built: the summarizer surfaces through the inspector HEADER's semantic-analysis affordance instead (see §Summarizer pattern), which is the shape the user picked.

### ▶ v0.8.0, LLM optional layer: SHIPPED as `@skill-map/cli` 0.89.0 / `@skill-map/spec` 0.81.0, stable on npm `latest` (2026-07-23)

---

### Step 11.5, Signal IR resolver phase + collision detection (Phase 2/3 of the active-lens migration)

Closed at v0.36.x in a five-commit sequence (Phase 2.A through 2.E). The Signal IR scaffold (types, schema, `ctx.emitSignal` callback, pure `resolveSignals` function) shipped at v0.31.0 but was never wired to the orchestrator; this Step finishes the round-trip and unlocks cross-extractor range-overlap collision detection.

What landed:

- **Phase 2.A**, resolver wiring end-to-end. The orchestrator now calls `resolveSignals` after the extract phase, materialises winning candidates as Links, threads the annotated `Signal[]` through `IAnalyzerContext.signals` to analyzers. Algorithm: filter disabled extractors (Phase 4+ stub) → rank intra-Signal candidates by `IProvider.resolverRules.kindPriority` + confidence + extractor declaration order → build overlap clusters from body-scoped Signals sharing a source (union-find over byte-range intersection) → pick cluster winners by the same tiebreak (with range length inserted between confidence and declaration order) → materialise winners + annotate losers' `resolution.rejectedBy`. External-URL pseudo-link clusters skip cross-cluster ranking. 18 unit tests cover every branch.
- **Phase 2.B**, `claude/at-directive` emits Signals. Each `@<token>` match emits a single-candidate Signal carrying the byte range (start, end, line) and the same kind / target / confidence / trigger shape the prior direct-emit path produced. The resolver materialises identical Links so the migration is transparent at the graph level.
- **Phase 2.C**, remaining link-emitters migrate. `claude/slash`, `core/markdown-link`, `core/annotations`, `core/mcp-tools`, `core/external-url-counter` all route through `emitSignal`. Body-scoped Signals get byte ranges; frontmatter and sidecar Signals get `fieldPath`. Zero behavioural change.
- **Phase 2.D**, `core/extractor-collision` analyzer surfaces resolver rejections as `warn` issues attached to the loser's source node, naming the winner extractor, the loser's matched text + range, and the tiebreak reason (`kind-priority` / `higher-confidence` / `longer-range` / `earlier-declaration`). Two conformance cases land at `spec/conformance/cases/{extractor-emits-signal,extractor-collision-detection}.json`; coverage matrix row 37 flips to ✅. Phase 4+ stubs (`extractorDisabled`, `belowFloor`) are documented and stubbed but not yet wired.
- **Phase 2.E**, ROADMAP catches up.

Deferred to a future Step (the rest of the spec's resolver pipeline, NOT blocking v1.0):

- Phase 4+ per-extension enable filter inside the resolver. The Signal's `resolution.extractorDisabled` field exists for it; the analyzer's message template is in place. The config surface for per-extension state was partially restored when operator-configurable extension settings landed (see the per-extension settings entry below): `plugins.<id>.extensions.<extId>.settings` now exists in `project-config.schema.json`, so the sibling `plugins.<id>.extensions.<extId>.enabled` toggle this filter needs can slot in alongside it when the filter is actually built.
- Phase 4+ confidence floor (drop a Signal whose top candidate falls below a threshold). Same posture: data shape + analyzer message ready, predicate not wired.
- Phase 5+ fragmentation detection (adjacent Signals representing a single authored intent). A different analyzer surface; the IR already supports it via the shared `source` + `range` fields.

### Step 12, Additional Formatters

- **Mermaid, DOT / Graphviz**, ✅ landed. Both ship as built-in `core/` formatters (`src/plugins/core/formatters/{mermaid,dot}/`), closing flags `spec/cli-contract.md` had promised since v0.5.0 (`sm graph --format mermaid|dot` previously exited 2 with "No formatter registered"). `mermaid` emits a `flowchart LR` with synthetic `n<i>` node ids (a path like `docs/a-b.md` is not a legal bare Mermaid id, `-` and `.` are link-token characters) and the real path as a quoted label, escaped with Mermaid's own entity codes (`#quot;`, `#35;`); the link kind rides an edge label rather than an arrow style (Mermaid's arrow vocabulary carries no mapping onto `invokes` / `references` / `mentions` / `points`), and the node kind rides a `classDef` + per-node `class`, emitted only for the kinds actually present. `dot` emits a `digraph` whose node ids ARE the quoted paths (DOT imposes no restriction on quoted-id content), escaping `\` before `"` because a label is an `escString` where a lone `\N` would substitute the node name; the kind rides a second label line and the only graph attribute is `rankdir="LR"`, so `dot -Tsvg` works with no extra flags. Both sort nodes and edges on UTF-16 code units (locale-independent, unlike the `localeCompare` the older renderers use) for the byte-determinism the formatter contract requires. `sm export --format mermaid` stopped being a deferred stub the same day: it resolves `core/mermaid` from the built-ins catalog (never a deep import, per the kernel-agnosticism sweep) and runs it over the FILTERED subset, gated by the same enabled check `sm bump` applies, so the export catalog stays closed while the rendering stays single-sourced.
- Subgraph export with filters.

### Step 13, More adapters

Promotes the long-deferred multi-host scope into Phase E so v1.0 ships supporting more than the Claude ecosystem out of the box. Each adapter recognises its host's on-disk layout, classifies files into the six extension kinds, and feeds the same scan pipeline, no kernel changes, pure composition over the `AdapterPort`.

- **Codex adapter**, file layout, frontmatter conventions, slash invocations. Phase 6 (shipped 2026-05-19) already onboarded codex/Codex as a first-class provider with TOML parsing and the `.codex/agents/*.toml` classifier; this Step finishes the round-trip.
- **Codex body extractor (TOML `developer_instructions` field)**, ✅ landed. The codex provider parses the TOML envelope and classifies `.codex/agents/*.toml` into agent nodes; the `developer_instructions = """..."""` block (the agent's markdown body) now flows through the link extractors via the declarative `read.bodyField: 'developer_instructions'` knob: the kernel walker yields that frontmatter field as the node body so the universal extractors (markdown-link, backtick-path, external-url) plus codex's OWN grammar extractors (`dollar-skill` for `$skill` invocation, `at-file` for `@`-file references) run over it. This is the "body can live on a parsed-frontmatter field" approach rather than a dedicated `codex/body-extractor`, so it generalises to any future provider whose prompt lives in frontmatter (e.g. a Gemini `prompt` field). A Codex agent whose instructions invoke a skill via `$skill`, reference a file via `@file.md` or `[link](path.md)`, or name a backtick path now surfaces in the graph under `activeProvider=codex`. (The body extractor first borrowed Claude's `@`-mention + `/`-slash grammar; once verified against the official docs, Codex's real grammar, `$`-skill invocation and `@`-as-file-picker, was split into codex-owned extractors and the borrowed Claude parsers were ungated from the codex lens.) The inspector renders these TOML nodes the same way it renders a `.md` node, by surfacing the provider's `read.bodyField` on the `providerRegistry` wire entry: the inspector resolves it (no hardcoded provider id), renders `frontmatter.developer_instructions` as the Body section (its parsed value already ships in `node.frontmatter`, so no extra disk read), and excludes that one key from the Definition/metadata card so the prompt is never double-rendered as a chip. The remaining post-v1.0 follow-up for Codex (Phase 6b in §Deferred) is the hierarchical AGENTS.md walker; Codex skills shipped 2026-06-25 (the open `.agents/skills/` standard classified as `codex`/`skill` via manifest composition over a multi-rule `read`). Remaining Step 13 work: Copilot.
- ~~**Gemini adapter**, Google's agent file shape, Gemini-CLI conventions.~~ (Retired 2026-05 when Google sunset Gemini CLI; replaced by the Antigravity onboarding under the open `.agents/skills/` standard during the active-lens migration. The bullet is preserved as historical context; no implementation work is owed.)
- **Copilot adapter**, GitHub Copilot's prompt / instruction surface.
- **Generic adapter**, convention-light fallback driven entirely by frontmatter (`name`, `kind`, `triggers`); the bare-minimum contract for any future host or for users with a custom layout. Doubles as the reference implementation in the adapter author guide that ships at Step 9.
- Each adapter ships its own `sm-<host>-*` skill namespace (host owns its prefix; see §Skills catalog).
- Conformance: each adapter must classify the four worked examples in `spec/conformance/cases/adapters/` (added when this step is scheduled) and round-trip the trigger set through `trigger-normalize` without surprises.

### Step 17, Web UI: LLM surfaces v2 (deeper)

> ✅ **COMPLETE** (audit-verified 2026-07-24), released in `@skill-map/cli`
> 0.89.0.

Promoted the probabilistic layer into interactive UI flows so the operator never
has to drop to a terminal for the high-leverage analyses. Shipped: the queue
inspector, findings management (dismiss / resolve / delete + bulk actions), the
Settings + plugins page with live-applying toggles, the settings-hierarchy
viewer, connected agents driving the queue over `/mcp`, the plugin-extensible
inspector (`inspector.action.button` slot + parametrized actions dispatching
through `POST /api/actions/:id`), the **operations audit log**
(`.skill-map/operations.log`, contract in [`spec/cli-contract.md`](./spec/cli-contract.md)
§Operations log), and the WCAG AA pass (remediation plus the automated axe +
Playwright sweep across six themes).

Two things were **discarded** from 1.0 here rather than deferred: the cost /
token dashboards (self-reported, unverifiable tokens; the raw data stays
captured in `state_executions` and `sm history --stats` already aggregates it)
and the five named verb-panels from the dropped Decision #49 verb set, whose
capabilities ship as extensions instead (Decision #137).

Landing narrative: [`context/roadmap-history.md`](./context/roadmap-history.md#step-17-web-ui-llm-surfaces-v2-deeper).

### Step 18, Quick Start (readiness) panel (onboarding without the tutorial)

> ✅ **landed 2026-07-24**.

A standalone rocket-icon modal in the topbar answering "what do I need to use
X?" without walking a full tutorial: per-capability readiness rows, each probing
its own real state and linking to the one action that fixes it. Born from
testers disliking full tutorials while the tool gates real capability behind
setup steps and permissions. Landing narrative:
[`context/roadmap-history.md`](./context/roadmap-history.md#step-18-quick-start-readiness-panel-onboarding-without-the-tutorial).

### Step 15, Distribution polish

> ✅ **CLOSED** (audit-verified 2026-07-24). v1.0 shipped on npm `latest`.

Three legs, all landed: **15a** the single `@skill-map/cli` package with the UI
bundled in and the `sm` / `skill-map` bin aliases; **15b** the AI-ingestion
docs surface (`llms.txt` + `llms-full.txt`, generated from `PROSE_DOCS` +
`spec/` by `web/scripts/build-site.js` on every deploy, so they cannot drift);
**15c** the release infrastructure (GitHub Actions + changesets, the opt-in
telemetry surfaces, the breaking-changes policy in
[`spec/versioning.md`](./spec/versioning.md), and `sm doctor`).

The release-candidate channel is operational: prereleases cut from `release/rc`
under the `rc` npm tag, never `latest`, with `.changeset/pre.json` barred from
`main` by a workflow guard. Runbook in [`CONTRIBUTING.md`](./CONTRIBUTING.md)
§Release candidates.

Landing narrative, including the end-to-end distribution flow diagram and the
1.0 scope cuts (Astro Starlight docs site, compatibility matrix, Claude Code
plugin wrapper, the `sm ui` verb):
[`context/roadmap-history.md`](./context/roadmap-history.md#step-15-distribution-polish).

### ▶ v1.0.0, full distributable

---

## Performance

**Node perf sprint (landed 2026-08-17).** Follow-up of the Rust/Go rewrite evaluation (2026-08-16, artifact "The Rewrite Question"): the measured slowness was implementation overhead inside Node, not a runtime ceiling, so the rewrite was rejected and the waste recovered in place. Four workstreams, all internal (no new flags, no contract surface beyond two spec description strings):

- **W1, tokenizer swap**: `js-tiktoken` -> `gpt-tokenizer` behind a lazy `ITokenCounterHandle` (see §Stack conventions · Tokenizer). Also fixed the latent scan-abort on literal special tokens in prose.
- **W2, persist fingerprint**: `persistScanResult` computes a sha256 over the canonical persisted content and SKIPS the replace-all rewrite when it matches `scan_meta.result_fingerprint` and no out-of-band inputs (renames, enrichments) ride along; only `scan_meta` refreshes. The freshly-run tuple set participates in the fingerprint as content (analyzers re-run every scan, so an empty-tuples gate would never engage); `emittedAt` provenance stamps are canonicalized out like `ranAt`. Plus: per-connection prepared-statement LRU in the `node:sqlite` dialect, AST-based query dispatch (no regex over 0.5 MB INSERT strings), 250-row insert chunks that share SQL text, tags full-wipe simplification, the stranded-orphan probe collapsed to one UNION query.
- **W3, ordered read-ahead**: `mapOrderedPrefetch` (`kernel/scan/ordered-prefetch.ts`, 16-deep, order-preserving by construction) overlaps file reads + parses in the traversal walk; per-directory lstat prefetch; the orphan-sidecar sweep went async and runs concurrently with the provider walk. Yield order stays byte-identical (claimedPaths / truncation / progress-index surfaces are order-load-bearing).
- **W4, startup Tier A**: V8 compile cache in `bin/sm.js`; the kysely/sqlite adapter subgraph (254 ESM files) deferred behind its two value entry points (`core/sqlite/with-sqlite.ts`, `db/migrate.ts`); conformance runner and both watcher backends (chokidar tree + @parcel/watcher native dlopen) lazy. Eager graph on `sm --version`: ~302 -> 45 ESM modules.

Measured on `fixtures/perf` (1,003 nodes, WSL2, Node 26) before -> after: `sm --version` ~300 -> ~160 ms wall; cold scan kernel phase 2,079 -> ~1,400 ms; fully-warm scan kernel phase ~880 -> ~560-650 ms (the fingerprint skip engages; wall ~0.83 s).

**Post-sprint follow-up round (2026-08-17, same day)**: the warm re-profile showed the fingerprint serialisation itself as a top-3 warm cost (~72 ms building one multi-MB canonical string per scan), so `computeResultFingerprint` moved to a streaming writer (`writeCanonical` + chunked `hash.update`, byte-identical output, ~half the cost). The same profile CLOSED the deferred sidecar-overlay lever: the per-node sync read measures ~15-25 ms per warm scan against an async ripple into ~25 `readSidecarFor` call sites, so it is not worth taking (rationale in `resolveSidecarOverlay`'s doc note). The remaining warm anatomy is structural: walk syscall floor (lstat + readdir ~100 ms for 1k files), always-re-run analyzers (by design, see §Design evaluation), GC + idle tail.

**Tier B closure (same day)**: B-1 (tsup `noExternal` bundling of the pure-JS boot deps: clipanion, ajv, ajv-formats, smol-toml, js-yaml, semver, ignore) EXECUTED: eager ESM modules on boot 45 -> 14, `sm --version` ~160 -> ~135 ms, warm scan wall 0.83 -> 0.75 s (the module loader ran on scans too). The five bundled deps subsequently moved to `devDependencies` (third round, same day): the published package no longer installs what the dist already carries; `ajv` / `ajv-formats` stay in `dependencies` because the lazy seam resolves them from node_modules at runtime. B-2 (built-ins catalog decoupling) MEASURED AND CLOSED without executing: post-compile-cache the 634 KB built-ins chunk costs ~3.4 ms at boot, so the 16-20-file surgery the backlog sketched would recover ~3-5 ms; the plan's 15-40 ms estimate predated A1. What actually remains in the ~135 ms boot: AJV (~25 ms, see next paragraph), clipanion command registration (~30 ms, lazy registration permanently rejected: it would break the spec-normative help introspection), and the Node process floor (~40-50 ms).

**AJV lazification (2026-08-17, third round)**: the "high ripple" that excluded this from Tier A turned out to be obsolete, every `new Ajv2020(...)` in the codebase was already function-local, so the fix is a SYNCHRONOUS `createRequire` seam in `kernel/util/ajv-interop.ts` (`loadAjv()`, no signature changes; ajv/ajv-formats stay external to the bundle). Two structural cleanups rode along: the user-settings store now compiles its OWN single `user-settings.schema.json` validator instead of dragging the full spec catalog onto the boot path, and a built-in analyzer's direct ajv import moved to the seam. The measured boot delta is ~0 though, because the boot-path hook dispatch (`cli/entry.ts` filtering built-in `boot` hooks) legitimately loads the AJV-validated project config on every verb; the residual ~25 ms is the price of the documented config-validation posture, and trading that posture for 25 ms was evaluated and REJECTED. Startup work is CLOSED here: remaining floor is clipanion registration + Node boot, both deliberate.

**Standing perf budget (K1 trigger)**: a fully-warm rescan of a 10k-node corpus must stay under 2 s kernel phase on 8 cores. Crossing it re-opens the perf conversation (more in-place work first, K1-style kernel surgery second).

**Rewrite gate (K2)**: any future kernel-binary / Rust / Go discussion is gated on the OPEN spec question "out-of-process plugin protocol", plugins execute in-process JS today, so a non-Node kernel is not designable before that protocol exists. Record new evidence against these two markers instead of re-litigating the 2026-08-16 evaluation.

---

## Decision log

The full numbered Decision log (every architectural decision, including superseded ones with their reasoning) lives in [`context/roadmap-history.md`](./context/roadmap-history.md#decision-log). All `Decision #N` citations across `spec/`, `AGENTS.md`, commits, PRs, and changesets resolve there.

Numbering is sparse on purpose: sub-items (`74a`…`74e`) land where they belong thematically, gaps reserved for future rows on the same topic. Rows are immutable, a changed decision gets a new row and the old row flips to "superseded by #N" with a date.

---

## Deferred beyond v1.0

- **Step 16+, Write-back**. Edit / create / refactor from UI. Git-based undo. Detectors become bidirectional.
- **Step 17+, Test harness**. Dry-run / real execution / subprocess, scope TBD.
- **Step 18+, Richer workflows**. Node-pipe API, JSON declarative workflows, visual DAG.
- **Step 19+, Additional lenses**. Docs-site, additional providers.
- **Step 20+, URL liveness plugin**. Network HEAD checks, `broken-external-ref` analyzer.
- **Step 21+, Schema v2 + migration tooling**. When breaking changes on the JSON output become necessary.
- **Step 22+, Density / token-economy plugin**. Drop-in plugin that closes the loop between *identifying* token-heavy nodes and *recovering* the value. Ships a deterministic Analyzer `oversized-node` (threshold on `scan_nodes.tokens_total`, per-kind configurable via plugin KV) plus cheap-filter proxies for information density, Shannon entropy over tokens, or a gzip-ratio substitute for a coarser signal. Summarizers emit a probabilistic finding `low-information-density` when they detect repetition without added signal. A Hook on `analyzer.completed` (filtered to the `oversized-node` Analyzer) walks the flagged candidates and pipes them into `skill-optimizer` (Decision #86, canonical dual-surface Action) via `sm jobs submit`. Cheap-filter + expensive-verifier: deterministic proxies pre-filter for free, the LLM summarizer confirms before committing tokens. Exactly the drop-in story the plugin architecture was designed to support, zero kernel changes, pure composition of Analyzer + Finding + Hook + Action.
- **Deterministic / vendor-API scanner findings lane**. A security scanner backed by a vendor HTTP API (Snyk / Socket dependency lookup, advisory feeds) is deterministic in spirit but has no route into `state_findings` today: deterministic Analyzers run on the synchronous scan path (fast, free, offline; they emit issues, not findings) and deterministic network Actions run via `sm enrich` into the enrichment layer, which does not feed findings either. Three candidate resolutions, deliberately undecided: (a) a deterministic findings lane at record/refresh time, (b) an enrichment-to-findings projection, (c) the documented workaround, model the scanner as a probabilistic Analyzer whose PROCESSING AGENT performs the vendor call and reports through the canonical findings envelope. Defer until a real vendor scanner implementation ships and makes the right lane obvious; the open question is recorded in [`spec/interfaces/security-scanner.md`](./spec/interfaces/security-scanner.md) §Deterministic / vendor-API scanners.
- **npm + other registry enrichment plugins**. When registries publish documented APIs.
- **ETag / conditional GET** for GitHub enrichment. Bandwidth optimization.
- **Governance / RFC process**. When external contributors appear.
- **Claude Code hook auto-record**. A PostToolUse hook that auto-calls `sm record` after an action completes. Partial coverage already via the Skill agent; full auto-record hook deferred.
- **Adversarial testing suite** for prompt injection. Fixtures with known payloads.
- **Parallel job execution**. Event schema already supports demuxing by id.
- **Multi-turn conversational jobs in DB**. If a strong case appears.
- **Plugin signing / hash verification**. Post v1.0 distribution hardening.
- **`.ts` migrations** (escape hatch for SQL-impossible data transforms).
- **`sm graph --root <node-path>` (focused subgraph render)**. Today `sm graph` always renders the whole collection through the chosen formatter; on large scopes the user has no way to focus on "what does THIS node connect to". Surface a `--root` flag that scopes the render to the transitive closure (in + out edges) of the named node, with `--depth N` to bound the walk. Useful for inspector-style flows from the CLI without round-tripping through `sm export`.
- **`sm conformance run --format json` (machine-readable conformance output)**. Today the runner prints a human summary; CI pipelines that want to gate on per-case results have to parse the prose. Add `--format json` returning `{ scope, cases: [{ id, status, durationMs, message? }], totals }`, mirroring the JSON shape of `sm version` / `/api/health`.
- **Standalone executable (no Node required on the host)**. Today `@skill-map/cli` ships as an npm package with two `bin` aliases (`sm`, `skill-map`); both require a Node runtime on the user's machine and a `npm install -g` (or `npx`) round-trip. The deferred goal is a self-contained binary per-OS, drop it on the box, run it, no Node, no `node_modules`. Tooling target: **`bun build --compile`** (produces a standalone executable that bundles the Bun runtime + the CLI; cross-compile to linux/macOS/windows targets is supported out of the box). Implications worth flagging: (a) the bundled runtime is Bun, not Node, any kernel code that touches Node-only APIs (e.g. `node:sqlite`, `process.binding`) needs a compat audit before flipping; (b) plugins are user-supplied JS dropped into `.skill-map/plugins/<id>/`, under a Bun standalone they execute through Bun's loader, so the plugin author guide gets a "supported runtime APIs" surface; (c) the npm package still ships in parallel, the standalone is an additional distribution channel for users who don't have or don't want Node, not a replacement. Distribution mechanics (signed releases, GitHub Releases attachments, Homebrew tap, scoop bucket) are part of the same step. Targets post-v1.0 because the v1.0 Phase E distribution polish (Step 15) intentionally locks the npm path first; standalone is a packaging extension once the npm channel is stable and the runtime-API audit is done.
- **Plugin-to-plugin dependencies**. Today the manifest declares compatibility against the spec (`specCompat`) and the contracts catalog (`catalogCompat`), but a plugin cannot declare it requires another plugin. The current escape valve is the data graph: a plugin that consumes Markdown nodes simply finds none if no Markdown extractor is installed, and the user has to discover the missing piece by absence. Use cases that break this pattern: a Markdown-validation Analyzer that is meaningless without a Markdown Extractor; a probabilistic Summarizer that extends a deterministic Extractor's output schema; a Hook chained to another plugin's Action. Add a manifest field, `requires: { "<plugin-id>": "<semver-range>" }`, checked at load time alongside `specCompat`/`catalogCompat`. Resolution order: missing dependency → status `missing-dependency` (load skipped, doctor surfaces it); incompatible version → `incompatible-dependency`; cyclic graph → load aborts with a named cycle. `sm plugins doctor` lists missing/incompatible dependencies; `sm plugins install` (when distribution lands) walks the closure. Out of scope for this entry: runtime imports between plugins (cross-plugin code reuse), that is a packaging problem, not a manifest one, and stays deferred independently. Targets post-v1.0 because the v1.0 plugin set is small enough that the data-graph fallback is acceptable, and the design needs the catalog evolution story to settle first (catalog-major + plugin-dep-major interactions).
- **Third-party UI + BFF extensions**. Today plugins extend the kernel via the six declarative kinds (Provider / Extractor / Analyzer / Action / Formatter / Hook); they cannot ship Angular components, Hono routes, or any code that runs in the browser or in the BFF process. A future plugin kind (or two new kinds, `UIExtension` + `BFFExtension`) lets third parties contribute: (a) Angular lazy modules that mount in declared extension points (extra inspector tabs, list-view columns, graph node decorations, side-nav routes, custom views, driven by the same plugin manifest field surface used today for annotation contributions); (b) Hono route bundles mounted under `/api/plugins/<plugin-id>/*` with their own middleware + Zod validation, sharing the BFF's broadcaster + kernel handle. Use cases: a vendor's plugin adds a "Verify against upstream" tab calling its own BFF endpoint to check the agent against the published version; a team's plugin adds an internal-scoring column in the list view sourced from a private cache; a security plugin adds a heatmap of agents that touch sensitive paths. Risk surface is non-trivial: sandboxing the contributed UI so it can't break the host SPA (CSP, isolated bundles, signed builds), securing plugin BFF endpoints (auth scope, rate limits, no kernel-bypass), versioning the contribution APIs (new sub-spec, plugin-author guide expansion). Distribution model TBD, likely the plugin author ships an extra `ui/` and `bff/` folder under their plugin, the kernel composes them at boot. Targets a deliberate post-v1.0 step because the security + sandboxing design needs masticación before any third-party code runs in the browser or the BFF process.
- **Action discovery surface in the node inspector**. The spec now carries two complementary fields that together describe "what can the user do with this node": (a) `Action.precondition` (already shipped, see `src/kernel/extensions/action.ts:108`) declares which nodes an Action applies to from the Action's own side (`kind`, `provider`, `stability`, `custom`); (b) `Analyzer.recommendedActions` (added in this iteration) declares per-Analyzer which per-node Actions are the canonical resolution for that Analyzer's issues. The UI work pending is the inspector hookup: when the user lands on a node, render two lists, "Applicable Actions" (every Action whose `precondition` matches the current node) and "Recommended for issues" (for each Issue on the node, the `recommendedActions` of the Analyzer that fired it). Actions are per-node by design; project-level operations (e.g. orphan-sidecar cleanup) stay as CLI verbs (e.g. `sm sidecars prune`) and do NOT participate in this surface. A future iteration MAY extend `IActionPrecondition` with scope-level dimensions if a real graph-scoped Action surfaces; until then the surface stays strictly per-node. Built-in pairings shipping today: `core/annotation-stale.recommendedActions = ['core/bump']` (stale sidecar → run `bump`).
- **Graph-level analyzer scope (workflow-wide checks)**. Today every analyzer receives the full graph via `IAnalyzerContext` (`ctx.nodes` + `ctx.links`), but the convention is per-node or per-link iteration (each Issue carries `nodeIds: [path]` with `minItems: 1`). Workflow-wide checks are technically possible already (an analyzer can do a graph traversal in its `evaluate` body), but two pieces are missing for them to be first-class: (a) a `scope: 'node' | 'link' | 'graph'` hint on `IAnalyzer` so the kernel can decide caching / parallelization differently (per-node analyzers are trivially parallel and incremental-friendly; `graph`-scoped ones need a full pass) and the UI can render their findings differently (a "the graph has a cycle between A → B → C" finding is qualitatively distinct from "node X is stale"); (b) an Issue shape for findings that do not attach to a specific node, today the workaround is "emit Issue with all involved nodes in `nodeIds`", which works but couples the graph-finding semantics to the node-finding shape. Concrete future analyzers that warrant this surface: orphan-cluster detection (subgraphs with no path to a designated root), missing-handler detection ("no node has a trigger for `/deploy` despite N callers using it"), agent-skill coverage gaps. Defer until ≥3 concrete graph-level analyzers exist; before then, model each one as a per-node Issue with every involved path in `nodeIds`. The decision is whether the spec needs `IAnalyzer.scope` + an `IGraphIssue` companion shape, or if the per-node Issue with multi-path `nodeIds` is sufficient at scale.
- **Live agent conversation view (real-time LLM transcript in the UI)**. Today LLM jobs (probabilistic Extractors / Analyzers / Summarizers, and any Action that delegates to an agent) surface in the Jobs panel as status + final summary; the user sees that *something* is running and what it produced, but not the back-and-forth, prompt turns, partial assistant deltas, tool calls, tool results, intermediate reasoning. The deferred goal is a streaming transcript view: while a job runs, the UI tails the conversation token-by-token (or turn-by-turn for tool calls), so the operator can watch what the agent is thinking, catch a runaway prompt early, and inspect *why* a finding was emitted without re-running with `--verbose`. Scope sketch: (a) extend `spec/job-events.md` with a `conversation.turn` event family, `turn.start` (role, turn index), `turn.delta` (token chunk, opaque to the kernel), `turn.tool_call` (name + arguments), `turn.tool_result` (truncated payload), `turn.end` (final text + token usage); kernel + runner emit them through the same broadcaster the Jobs panel already consumes; (b) persist a bounded ring of turns per job in `.skill-map/jobs/<id>/conversation.ndjson` so re-opening the UI after the job finished still shows the full transcript without re-running; (c) UI: a "Conversation" tab inside the Job inspector, virtualized list of turns, syntax-highlighted tool-call JSON, collapsible long deltas, copy-to-clipboard per turn, "jump to live" affordance; (d) a CLI mirror, `sm jobs tail <id> --conversation` for terminal users, sharing the same event stream. Implications worth flagging: provider abstraction needs to expose streaming deltas uniformly (Anthropic / OpenAI / local LLMs all stream differently, the kernel should normalize to the `turn.*` event shape, not leak SDK types); secret redaction passes over each `turn.delta` before it hits the broadcaster (today `cli-output-style` redacts post-hoc on text output, streaming needs the same guarantees in-flight); storage cap analyzers (a 100k-turn job should not bloat `.skill-map/`, so the ring is byte-capped with an "elided N turns" marker, mirroring the existing job-output truncation behavior); the conversation log is the operator's debugging surface, not a normative artifact, it does NOT feed back into the graph and does NOT get committed to git (`.skill-map/jobs/` is already gitignored). Targets post-v1.0 because the v1.0 LLM layer (Step 11) intentionally ships with the simpler "status + summary" UX; live transcript adds non-trivial provider-streaming + secret-redaction + UI virtualization work that is independent of correctness and benefits from being prioritized once the LLM verbs have real-world usage to guide the affordances.
- **MCP inspector-card enrichment (cosmetic)**. The functional MCP iteration shipped across 2026-07 (config-side discovery via the `mcpConfig` Provider capability, live tool-invocation glow on all four runtimes, and the opt-in read-only `/mcp` server; normative shape in [`spec/architecture.md`](./spec/architecture.md) §Provider · MCP config discovery and [`spec/mcp-server.md`](./spec/mcp-server.md)). What is left is cosmetic: the inspector MCP-node card should render the config-side metadata (transport, url / command, args, declared tools) instead of the placeholder body the consumer-only cards carry, so a selected `mcp://<server>` node reads its wiring at a glance rather than only its edges.
- **Codex AGENTS.md hierarchical cascade**. Codex reads `AGENTS.md` at every level from project root down to CWD, concatenates them in depth order, applies per-level `AGENTS.override.md` shadow files, and caps the total at `project_doc_max_bytes` (default 32 KiB). Today each `AGENTS.md` is just another markdown node the `core/markdown` fallback claims; the hierarchy is invisible. Two modelling options: (a) keep each `AGENTS.md` as its own node and add `extends` / `overrides` edges between them so the user reads the cascade visually (simpler, reflects the source faithfully); (b) synthesise virtual `agents-md://<dir>/` nodes representing "the effective instruction set Codex sees when working in `<dir>`" with `derivedFrom: [source AGENTS.md list]` (exposes runtime semantics, but the synthetic body has no real-file backing). Option (a) is the recommended starting point; (b) opens when concrete demand for a runtime view appears. Both need `project_doc_fallback_filenames` support (Codex can also be pointed at `CLAUDE.md`, `CONTRIBUTING.md`) and a warning when a level's concatenated payload exceeds the cap. The other half of the former Phase 6b, Codex skills, shipped 2026-06-25 via the open `.agents/skills/` standard.

---

## Discarded (explicitly rejected)

Explicitly rejected proposals (with rationale) live in [`context/roadmap-history.md`](./context/roadmap-history.md#discarded-explicitly-rejected). Use that file when wondering whether an idea was already considered and dropped, the reasoning is preserved verbatim there.
