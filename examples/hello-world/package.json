{
  "name": "@skill-map/example-hello-world",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Reference plugin for skill-map. Smallest viable Extractor + one test using @skill-map/testkit.",
  "scripts": {
    "test": "bun test ./test",
    "test:ci": "bun test ./test",
    "validate": "bun run test:ci"
  },
  "devDependencies": {
    "@skill-map/cli": "*",
    "@skill-map/testkit": "*"
  },
  "engines": {
    "bun": ">=1.2"
  }
}
