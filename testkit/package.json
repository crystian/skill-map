{
  "name": "@skill-map/testkit",
  "version": "0.3.1",
  "description": "Kernel mocks + builders for plugin authors. Test detectors, rules, and formatters without spinning up the full skill-map runtime.",
  "license": "MIT",
  "type": "module",
  "homepage": "https://skill-map.dev",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/crystian/skill-map.git",
    "directory": "testkit"
  },
  "bugs": {
    "url": "https://github.com/crystian/skill-map/issues"
  },
  "keywords": [
    "skill-map",
    "testkit",
    "plugin",
    "ai-agents",
    "claude-code"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist/",
    "README.md"
  ],
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "tsc --noEmit && bun test ./test",
    "test:ci": "tsc --noEmit && bun test ./test",
    "test:coverage": "tsc --noEmit && bun test --coverage ./test",
    "validate": "bun run typecheck && bun run build && bun run test:ci",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@skill-map/cli": "*"
  },
  "devDependencies": {
    "@types/bun": "1.3.11",
    "@types/node": "24.12.2",
    "tsup": "8.5.1",
    "typescript": "5.9.3"
  },
  "engines": {
    "bun": ">=1.2"
  },
  "publishConfig": {
    "access": "public"
  }
}
